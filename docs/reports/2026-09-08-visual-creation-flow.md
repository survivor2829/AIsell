# 内容创作四步界面

内容创作改为「选素材 → 选方案 → 确认制作 → 看成片」。素材使用可勾选的真实缩略图，方案使用素材预览与简短说明，完整文案只在确认阶段展开。首步保留目标客户和表达重点，结尾引导、补充资料、品牌与时长折叠；批量数量、声音与配乐放在确认阶段。旧批次保留原工作方式。

源码：`desktop/src/renderer/BatchCreativePage.tsx`、`BatchCreativeBrief.tsx`、`BatchCreativePage.css`，新增 `BatchMaterialBoard.tsx`。本轮仅修改这些界面文件；保留会话已有未提交工作，未提交、推送或发布。

验证：

- `npm.cmd run build:test` 通过，最终构建已加载到当前 Electron。
- 实际 Electron 在 1800×950、1180×950 窗口下无横向溢出；四步导航及空素材上传入口正常，首屏主按钮完整可见；主题继承角色紫色。
- 当前素材均已归档，实际素材列表为空，未恢复或改动归档状态。
- 在同一 Electron 的隔离页面中，使用已有归档素材和文案快照验证图片显示、素材选择、三个方案、换方案、打开修改文案对话框、确认回调和成片视图。生成与确认接口均为内存替身，未写入实际批次，也未发生付费调用。5 张素材缩略图、3 张方案预览正常；两种宽度无横向溢出；无页面异常。
- 检查证据保存在 `outputs/experience-refinement-20260908/visual-flow/`，`live-results.json` 是实际实例结果，`fixture-results.json` 是隔离界面结果，两者不能当作新增视频生成验收。

用户复验：添加素材，填写目标客户，生成三个方案后点击一个，查看全文并确认声音与配乐，再点击制作。此次未重新生成视频、未验证安装包。
