# 1.1.20 内部发布记录

2026-09-12 完成内容创作与 API 链路修复的本机覆盖升级验收、源码推送和内部 `test` 频道整包发布。用户已确认本次真实成片可作为内测验收结果，正式客户频道保持现状。

## 改动与验证

- 内容创作收敛为素材与需求、确认文案、制作与成片；默认一稿，旧一／两稿可确认；完整自供文案保留原文，零碎想法按方向整理。已选素材全部可见，修复清空与保存竞态；支持无配乐、按依赖复用、限制实际请求次数和未知结果不自动重提。
- API 链路修复能力误判、Ark／TTS／ASR 凭据混用、跨主体请求合并、会话恢复与配置同步。服务器专用 TTS 未配置时使用本机原有加密语音配置，未上传该密钥；没有变更供应商凭据。
- 包提交 `ebf73addc35471e1555899eb353b7928690665bd`，分支 `codex/content-brief-workflow`。功能提交 `a0d3e5f946ae5f1b4161cedcf8cc080e28d32c30`，服务提交 `6e428b6b4bfc525ec5dd52bc3ce500ec69e1d8c7`。源码已推送，后续发布记录提交只修改文档，不改变已验收制品。
- 同一包提交的 [Windows CI](https://github.com/survivor2829/AIsell/actions/runs/34683082992) 中 `service-and-content` 和 `source-and-renderer` 均成功，包含服务／内容定向检查、源码契约及测试／交付界面构建。
- `release:internal` 从干净提交完成完整源码自检、product-detail 本地 E2E、运行时及密钥边界检查、界面、便携包和安装器构建与解包校验。初次构建暴露公告夹具固定要求 1.1.0 出现在最近20版，已改为按上一实际版本检查；没有修改公告保留数量或应用行为，重新提交后完成干净构建。Vite 既有主 chunk 大小提示保留。

## 制品与签名

| 项目 | 验证值 |
|---|---|
| 版本／构建号 | `1.1.20 / 20260912T0824Z` |
| 包源码提交 | `ebf73addc35471e1555899eb353b7928690665bd` |
| 安装器 | `release/AI获客 V1.0版本-测试版-安装程序.exe` |
| 大小 | 527349620 字节 |
| SHA-256 | `07a77fe9545fe37c650c0ae50c4d4daf89c233591bb19357d69251b972e7d764` |
| 安装后源码树摘要 | `af43e5181a8a48ad45089e75112cd207a4b04572be45c366bf439b062af7f17e`，与包一致 |
| 制品定位 | `internal-evaluation`，`commercialReady=false` |
| Windows 代码签名 | `NotSigned`，不等同于已购买商业代码签名证书 |
| 更新清单签名 | Ed25519 签名验证通过，版本、公告、摘要与本地一致 |

本次只分发已验收的同一完整安装器，不在验收后另行重建替换；便携包及自动生成的组件构建物保留在本地。

## 本机安装与数据保留

- 覆盖升级前关闭制作程序并确认没有活动内容任务，完整备份测试版用户目录和旧 1.1.19 程序；用户目录 24353 个文件、12500130672 字节，旧程序 3884 个文件、1455547090 字节。全部逐文件 SHA-256 对比一致，备份 SQLite `quick_check=ok`。
- 从旧测试安装版升级至 1.1.20，安装器退出码0。安装后的包清单、源码树摘要及版本一致；首次启动前，24353 个原用户文件摘要全部未变。随后正常启动产生的运行日志另按实际运行保留，不声称日志永不变化。
- 实际检查安装目录中的 `file:` 界面，显式登录成功、内容引擎 ready、本机加密 TTS 配置有效；新表单的给谁看／想讲什么帮助、保留完整文案选项和默认30秒可见。原批次、批准正文和43.157秒成片可恢复，安装版视频实际播放无媒体错误。
- 自动回复保持暂停，临时调试实例正常关闭后已重新正常启动安装版，启动参数无调试端口；本次 Vite 辅助服务器已关闭。
- 用户数据和程序备份位于本地 `outputs/content-simplification-20260912/upgrade-backup/`；原 `recovery-backup-20260911-material-db/` 保留，未提交、未删除。旧构建和安装器回滚副本同样保留。

## 真实成片与费用边界

- 用户选择第一稿后，事实检查触发频率／效果断言修正；用户明确批准修订正文再制作，最终确认“成片可以，继续内测发布”。使用已有分析，未将本次成片描述为新流程从零生成。
- 成片1080×1920、43.157秒；六段字幕与批准正文一致，完整播放、音视频解码、无超过1秒静音、18个不重叠来源区间、导出和重启恢复通过。视频 SHA-256 为 `3a1ce1d5d5931235478263fa69d460c1906d8ff918f95e2e12d0e29de0c81d11`。
- 原三个素材的离线回放显示来源输入由248992字节降至44651字节，减少82.067%；这是输入规模验证。新流程首次从零写稿的实际 token、全程耗时和账单金额仍未实测，不用离线比例代替真实费用节省。
- 最后一次制作从配音阶段恢复约4分56秒，6次 TTS、6次 ASR；此前修订和失败调用继续留账，不将其从整次工作费用中抹掉。安装验收和发布没有追加付费生成。

## 依赖服务与频道发布

- 服务先于新版客户端分发完成：维护服务和网关从干净服务提交部署，TLS 健康和真实制作调用通过。维护数据库／代码备份 `/var/lib/ai-maintenance/backups/20260912T072247Z-1718156`；网关代码备份 `/var/lib/ai-provider-gateway/backups/20260912T072249Z-1718196`，运行修订 `bc6f21004a00f499`。
- 发布脚本返回 `published: true`，频道 `test`，版本 `1.1.20`，序号 `1789202394171`；清单时间为 `2026-09-12T08:39:54.171Z`（上传前生成），发布后实际读回验证时间为 `2026-09-12T08:54:45.504Z`。服务器先验证完整安装器的 SHA-256 与大小，再切换频道指针，旧签名清单保留。
- 从公开 `/v1/releases/test/latest` 读回后，包内公钥签名校验通过；版本、序号、公告、大小和摘要与本地已验收安装器一致。首尾各65536字节分段下载均返回206，Content-Range、总大小及字节内容与本地对应区间一致。本次只发布完整安装器，没有另行发布组件增量包。
- 本版只面向内部 `test`，正式客户频道保持现状。其他电脑是否更新、不同微信版本兼容及任意素材质量需各自确认；本轮没有执行真实微信发送、评论或发布操作。

详细内容流程、门槛替代依据与定向回归见 [内容创作验收](2026-09-12-content-simplification.md)。原始构建、安装、CI、备份、视频和发布证据保存在本地 `outputs/content-simplification-20260912/`，不包含在安装包或源码提交中。
