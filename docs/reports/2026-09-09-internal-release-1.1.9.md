# 2026-09-09 测试版 1.1.9 本机验收

最终 `1.1.9 / 20260909T1302Z` 已覆盖安装到本机测试版目录。详情图离线抠图、导出和真实 Electron 布局检查通过；故障电脑微信主窗口识别仍待采集结果，不能算已修复。未推送源码、运行远程 CI 或发布更新频道。

## 版本与构建

- 干净源码提交：`83bd835db27b8745840afa33978fa450920b80d6`。
- 安装器：`release/AI获客 V1.0版本-测试版-安装程序.exe`。
- SHA256：`2724fa1832354078d4d2f397a98cb231c6eaf1e8d3fe567afcfa61b8f1733381`。
- 完整源码自检在 `169e600` 通过；后续仅修改已定向验证的 E2E 默认布局断言及三行窄窗口 CSS。最终源码 E2E 完成 6 次生成、6 次恢复与 PNG 导出。最终便携包和安装器均完成自身验证。
- 首轮本机候选 `20260909T1225Z` 已由最终构建替换；判断版本必须同时核对构建编号，不能只看 `1.1.9`。

## 实际安装与功能

- 安装程序退出码 0；安装后的应用树、详情图组件树、离线模型 SHA 与清单一致。
- 原有 16 个任务、9 个归档任务保留，所监测业务文件及授权文件核对通过，自动回复仍暂停。
- 实际安装组件在隔离数据目录上传既有机器人产品图，离线抠图返回 completed：1649 × 1319 PNG，alpha 0–255；成功导出 1500 × 3632 PNG，1,382,425 字节。无付费请求及页面异常。
- 实际安装应用通过登录和产品详情图启动入口运行。真实 Electron 在 1440 × 900、1100 × 760 下检查编辑、模板、大图六种组合，无横向溢出；人工查看宽屏编辑和窄屏模板截图，模板首先可见，长预览高度受限且可滚动。最终检查未注入临时 CSS。
- 已有旧草稿图片不会自动重新抠图；重新上传产品图后走新的离线处理。其他复杂背景和细小透明边缘仍需按素材检查；付费精修未调用。

证据位于 `outputs/product-detail-diagnosis-20260909/`：`installed-1.1.9.json`、`installed-runtime-qa/visual-qa.json`、`installed-electron-ui.json`、六张 `installed-electron-*.png`、`package-final-restored-1.1.9.log`。

## 构建环境与空间

用户报告使用微软电脑管家进行空间清理；期间发现联系人工具、Electron、Playwright Node 三个构建 EXE 缺失。未取得具体删除记录，不能确认删除来源或归因杀毒。联系人工具从相同 Git blob 恢复；Electron 从官方 32.3.3 包恢复并校验归档 SHA；Playwright Node 从已验证的首轮安装组件恢复，驱动 package.json 字节一致。丢弃本次缺失 Node 的组件缓存后重建，最终包内浏览器检查通过。未关闭或修改安全防护。

四个用户确认的旧发布副本已删除，逻辑量 4.290 GiB。新构建清理自身临时目录并限制受管理回滚和缓存代数；未接管历史未知产物、用户素材或完整数据备份。

## 微信仍待闭环

已读取故障电脑 1.1.8 新反馈：唯一 Qt 候选未通过主窗口特征核验，恢复后仍失败，发送未尝试。本轮仅修正诊断计数残留并准备只读窗口采集工具，未修改识别规则或执行真实微信发送。需要在故障电脑主窗口打开时运行采集工具并回传 JSON，之后根据窗口结构证据继续定位。
