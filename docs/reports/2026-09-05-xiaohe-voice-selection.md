# 小何 2.0 接入与默认声音

2026-09-05。用户已开通语音服务，明确表示喜欢“小何 2.0”，希望后续优先使用。

## 实现

- 使用[火山官方音色列表](https://www.volcengine.com/docs/6561/1257544)核实的小何 2.0：`zh_female_xiaohe_uranus_bigtts`，资源 `seed-tts-2.0`。沿用现有 SSE 接口，不混用实时语音的 jupiter 音色或“湾湾小何”。
- 新增 `volc-xiaohe-2@1` 预置。用户实际选择声音时即时保存独立默认偏好；新建批次和重启读取，旧批次保留自己的设置与确认稿。未在默认选择逻辑中硬编码音色。
- 火山接口失败时仅从适配器完整固定模板提取业务数字码或 HTTP 状态，构造明确的公开提示；不透传供应商原文或密钥，不改未知结果停止规则。

## 实际结果

- 一次真实小何 2.0 合成成功，2026-09-05 15:31:01 中国时间返回。此前本地主窗口焦点校验失败未触发云端合成。
- 输出：`outputs/video-optimization-20260905/voice-auditions/小何 2.0.wav`。15.220625 秒、24,000 Hz、单声道 PCM WAV，统一试听文案，无背景音乐；峰值 -1.55 dBFS，RMS -16.02 dBFS，零削波采样。RMS 不等同于 LUFS；这里没有另报实测积分响度。
- 按用户已经明确给出的声音选择，通过应用按钮批准并在下拉框选定小何 2.0。刷新后确认 `approval=approved`、`preview=completed`，默认值仍为 `volc-xiaohe-2@1`，没有产生新的语音请求。
- `npm.cmd run build:test` 通过；音色目录与本地失败缓存复用的现有专项通过；公开错误提示独立专项通过。不将这些检查表述为全仓 TypeScript 检查或完整成片验收。
- 尚未生成新的完整视频；需要按既定流程确认文案和配乐后继续。

证据：`xiaohe-2-focused-attempt.json`、`xiaohe-2-selection.json` 与 WAV 均在上述 voice-auditions 目录。原声音、素材、旧样片和早先失败记录保留。
