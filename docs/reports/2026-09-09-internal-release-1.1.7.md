# 1.1.7 内部发布验收

内部测试频道的完整安装包和增量更新均已发布 1.1.7，本机覆盖安装、原授权登录及任务数据保留验证通过。原故障电脑仍需实际更新后复测。

## 版本与发布证据

| 项目 | 结果 |
|---|---|
| 安装包源码提交 | `7410bd6fed97e1576ef67416554b3ef0f2515d4e` |
| 构建号 | `20260909T0821Z` |
| Windows CI | [34328332385](https://github.com/survivor2829/AIsell/actions/runs/34328332385)，全部成功 |
| 完整包大小 | 441,674,829 字节 |
| 完整包 SHA-256 | `ed3b4827d4461bcafc3c1cd64f0da3a3165e422f6f189162bc88a7ea9abb3b45` |
| 完整包测试频道 | 1.1.7，序号 `1788943236100`，签名、大小和摘要读回一致 |
| 增量测试频道 | 1.1.7，序号 `1788943504397`，签名及四个组件摘要读回一致 |
| 增量底座 | 1.1.6，指纹 `63b9c898d4e6c75d3d0d7cfa9d2425b97edce260317d83c92c2f11b0799e067a`，与已安装 1.1.6 相同 |
| 安装版应用源码树 | `6ddc665ab0589e645305f1e13f56e58fd6c9ce3cdfd9b62d0be4a6cb5518d981`，与候选清单一致 |

完整发布入口通过源码检查、详情图本地端到端（6 个模块生成及恢复、PNG 下载）、界面构建、运行时检查、组合包校验和安装器解包内容核验。三个运行时缓存经过校验后复用。发布的是本机已安装验证的同一份产物，验收后没有重新构建或替换程序内容。

增量发布复用服务器已有归档，本次补传两个归档；这不代表每台客户端都只需下载两个组件，客户端下载范围仍取决于本机版本和已有文件。

## 审查修复与本机验证

- 正确性、安全、对抗场景、验证覆盖和仓库约定审查后，补上独立的画面滚动位移回执，避免滚动过头后把同作者的相似动态接续为目标。原目标位置无法与实际位移对应时暂停。
- 真实只读滚动验证：滚轮输入 300 刻度，画面实际移动 188 像素，原动态头像从 y=164 移到 y=352，重新定位成功；原目标滚出画面的路径拒绝继续。
- 升级前完整用户数据备份保留。安装后 `wechat_workflow/state.json`、`auto_reply/workflow-recipients.json`、`moments/publish-state.json` 三份文件与备份摘要一致，16 项成功任务和 9 项归档记录完整保留。
- 安装版源码树和 EXE 与候选包相同。原保存授权正常登录，已完成任务的删除确认可取消，取消后任务仍为 16 项；包内 1.1.7 具体更新公告和待测项可读。验证后正常退出，任务保持暂停、自动回复关闭。
- 删除归档重启保留、批量状态变化整体拒绝、成功记录保留及诊断阶段解析的定向检查通过。没有为验证而删除用户剩余成功记录。

## 配套服务

同提交维护服务已部署至现有内部测试服务器，升级脚本先备份数据库和旧代码，再替换服务并验证健康状态。备份保留于 `/var/lib/ai-maintenance/backups/20260909T082542Z-678663`。

部署后的 `service.py` SHA-256 为 `6cf024b8ba01b0eaae3ec27ca11998518fe3d0b6ce9a25fd192f739da6056aec`，与本机发布源码一致。公网 TLS 隔离技术测试记录接收、阶段及耗时读回通过，未允许窗口标题等隐私字段进入诊断。没有替用户回复或关闭真实反馈。

## 验证范围与后续复测

- 本机微信版本为 4.1.13.65。源码运行态已完成一次发布自动确认和两条新动态互动，证据见此前修复记录；审查追加的位移逻辑完成了上述真实只读核对。
- 本轮安装版验证覆盖安装、启动、原授权、数据保留、删除确认取消及包内公告，没有再次执行发布、点赞、评论或消息发送。不能将源码实测改写为安装版全部业务场景验收。
- 原故障电脑、微信 4.1.11.55、跨电脑启动性能仍需实际更新后复测。结果无法确认时继续保留未知状态，不自动补发。
- 已安装兼容 1.1.6 底座的测试电脑可检查增量更新；不兼容底座使用完整安装包。正式客户频道保持关闭。

本机证据保存在忽略目录 `outputs/release-review-20260909/`，包含安装、数据摘要、CI、服务部署、完整包及组件清单读回记录；升级前用户数据及旧安装包均保留。本文与项目状态属于发布后的文档补记，不改变以上包提交和产物。
