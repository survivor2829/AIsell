# 混剪切换火山引擎：实施与验收记录

## 当前结果

- 现有小何语音密钥调用方舟文字、看图：均 HTTP 401 AuthenticationError，未自动重复请求。
- 用户已在新增的加密入口保存方舟密钥；文字、看图：均 HTTP 200，返回模型为 `doubao-seed-2-1-pro-260628`。
- 旧语音密钥曾返回 HTTP 403；用户已保存极速版 APP ID + Access Token，新凭据实际识别成功，返回句词时间。证据为 `outputs/video-optimization-20260905/volc-asr-pair-probe.json`。
- 独立语音识别凭据经加密入口保存，不收集 Secret Key，不覆盖小何配置；缺少任一字段则停止。五项接口专项检查通过，包括静音素材的正常空识别结果。
- 五段素材已完成火山分析及镜头区间重绑定。用户依次确认两轮措辞微调，第3版标题为“我想减轻的，是重复清理的负担”，正文采用“杂物”“旁边”；前两版保留。第3版事实复核及整体画面复核均通过。
- 首条已生成：`outputs/video-optimization-20260905/sample-01-volcengine-final.mp4`，实际37.618秒、1080×1920，使用小何2.0和My Soul。20个字幕片段逐字保留确认正文，2处本地行内emoji；最终两处跨场景画面已按词时间核对。完整解码通过，人声高于配乐9.38–10.57 LU，峰值-1.54 dBTP。
- 尚待用户观看、听感及手机外放验收，第二条未制作；不能宣称两条稳定复现已验收。

## 已实施

新增火山媒体适配，复用现有文案/视觉提示词和业务输入输出；只允许方舟官方文字视觉端点，不回退百炼。录音识别上传本地短音频，转换句词时间为现有字幕格式；没有词时间时不造逐字对齐。小何2.0继续使用原语音合成适配。

方舟凭据与语音凭据分别经主进程加密保存，IPC复用RSA安全传输。测试运行时选择火山客户端，不再注入百炼凭据。相关创作设置、配置状态入口改接方舟；旧凭据与历史资料未删除。历史百炼适配保留用于兼容，未宣称所有旧创作模式完成迁移。

分析缓存区分服务商及模型；尚未制作的确认批次跨服务商时重新分析并复核，仅在原镜头区间可原样对应时继续，正文保持一致；无法对应时恢复确认稿并停止。未知结果保留标记，不自动重提。

## 已完成检查

- Python和主进程JavaScript语法检查通过；界面构建通过。
- 三项接口专项检查通过：旧密钥/旧端点不回退，鉴权拒绝不重试，句词时间合法转换。
- 迁移失败保持确认稿专项检查通过。
- 本轮正文修订留档、事实审核缓存复用及原帧引用范围专项检查通过；标准见 `docs/video-copy-standard.md`，修订记录见 `outputs/video-optimization-20260905/confirmed-script-revision-2.json`。
- 正式配音补齐服务商参数；音乐复用及最终验证传递用户已选曲目范围；对应音乐撤权/摘要验证仍通过。
- 少量ASR漏字不再丢弃整段时间，可用首尾确切词边界显示完整短句，不造逐字时间。小幅改稿保留分句镜头绑定，镜头按实际词时间切换。3项时间/绑定专项检查通过，原音频复用，无重复配音。
- 实际Electron中方舟输入框可见；用户保存后状态为configured，加密存储可用。

## 验收数据

- 新批次：`narrated_batch_75603e83b4d94be5be0b930aa54f70ee`。
- 已选第二文案：`outputs/video-optimization-20260905/selected-script-2-volcengine.json`，保留原标题与全部正文。
- 旧语音密钥探测：`outputs/video-optimization-20260905/ark-key-probe.json`。
- 方舟新密钥探测：`outputs/video-optimization-20260905/ark-dedicated-key-probe.json`。
- 语音识别探测：`outputs/video-optimization-20260905/volc-asr-probe.json`。

## 双样片验收与剩余限制

第二条续做复验：首轮因跨素材分段与时长不足停止；已将生成提示中的单段 `asset_id` 约束与校验对齐，并明确局部缩写必须在其他有容量的段落补足有效内容，Python语法检查通过。次轮通过编排，但初稿含无法由素材证明的反复事件，局部修订后达到12次审核调用上限；没有自动重复未知结果，也没有配音或渲染失败稿。随后保留镜头，人工辅助调整第二条为《我想少做的，是临时补位》，通过原有待审核流程继续。因此本轮不能宣称“无需干预的批量文案生成已稳定通过”。

用户已认可首条（“非常的不错”）。第二条已完成：`outputs/video-optimization-20260905/sample-02-volcengine-final.mp4`，47.562秒，1080×1920，小何2.0、Summer，25个字幕片段、3处行内emoji。完整解码通过，字幕正文与第二条稿件一致，画面及emoji抽查通过，人声高于音乐9.89–10.83 LU，最终-15 LUFS、峰值-1.38 dBTP；Remotion无回退。本批记录仅见火山调用。用户已反馈“可以不错”，第二条获认可；比45秒目标长2.562秒，作为本条已认可例外，不修改后续30–45秒目标。证据见 `outputs/video-optimization-20260905/second-sample-acceptance.json`。

整体审片曾错误要求三个备选稿及可选逐句字段，原因是生成指令被统一追加到审片请求。现已通过 `generation_rules=False` 隔离整体审片，并明确完整愿望、问题与已实现效果的区别，缓存版本更新为9；原稿无需再次改动即通过实际审片。Python语法检查与真实审核/制作通过。两条均获用户认可，视觉与声音基线已保存至 `outputs/video-optimization-20260905/approved-production-standard.json`；全自动写稿稳定性尚未通过。旧百炼适配代码为其他历史模式兼容保留，未宣称所有旧创作模式完成迁移。

最终验收数据：`outputs/video-optimization-20260905/sample-acceptance.json`。当前run为 `auto_mix_run_59978729d69e4ab4977c20d3be24a139`，video为 `generated_video_95499357fe1745bbb8b6df27d46deefc`。旧成片和原始素材均保留，未发布、未提交或打包。
