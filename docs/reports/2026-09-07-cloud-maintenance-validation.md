# 更新与诊断验证记录 · 2026-09-07

## 完整 AI获客 测试版实测

用户后续授权本地提交、生成两个完整测试版本，并在现有测试版安装目录完成自动升级与数据保留验证。以下不是隔离程序结果。

`1.0.1` 相比本轮基线仅递增发行版本，用于验证更新链路；不将受控错误消失解释成真实业务故障修复。

| 项目 | 基线 | 更新版本 |
| --- | --- | --- |
| 版本 | 1.0.0 | 1.0.1 |
| 源码及安装器提交 | `a41699817c84fe464a3eea68c8cb154967a115ad` | `c95bce573a9e54e7573396b4833bdd6e5d091537` |
| 构建编号 | `20260907T1018Z` | `20260907T1032Z` |
| 安装包字节数 | 427147238 | 427149194 |
| SHA-256 | `4074809da316349d57353db1dc25669d6966ea71558c2ce5e526df9c798a18a1` | `a174ad73abb06faaa79b9fb1e2d490d08516d77674e17e4aeb8724881005a08d` |

- 两版均从干净提交生成，产品详情图、内容引擎、Remotion 及媒体工具实际随包；运行时来源、便携包和 NSIS 解包内容校验通过。安装身份为 `com.aihuoke.desktop.test`，沿用测试版安装目录和 `xiaoxi-active-touch-test` 用户数据目录。
- 基线静默覆盖安装返回 0；安装前后原有 3,655 个文件、2,889,258,289 字节的哈希全部一致。关键配置另有本机备份，未解密或输出凭据。
- 基线通过真实登录按钮进入应用，`app.isPackaged=true`、版本 `1.0.0`、`canInstall=true`。诊断默认关闭；开启后通过包内真实日志服务注入 `maintenance_acceptance / controlled_failure / full_package_test_failure`，服务器收到一条脱敏错误，队列清空。预设正文、密钥样式字符串和私有路径在服务器记录中均不存在。
- `test` 频道发布 `1.0.1`，签名清单序号 `1788778017844`，HTTPS 证书与 Ed25519 签名均正常校验。完整 1.0.0 下载 427149194 字节后达到 `ready / 100%`，正常退出触发已下载 NSIS 安装器。
- 19:01:11（北京时间）新版由安装器自动启动；实际安装目录版本为 `1.0.1`，后台收到同一匿名安装编号的新版 `app_started`，构建号为 `20260907T1032Z`。随后正常关闭并再次从已安装目录启动验收：既有授权有效、登录成功、版本 `1.0.1`、状态 `current`、诊断开关仍开启、待传队列 0。已保存完整产品诊断页截图。
- 后台真实页面显示两个版本；受控测试错误可搜索，并成功保存修复版本 `1.0.1`。这只验证处理状态闭环，不代表真实产品业务缺陷已被修复。

### 数据保留

升级和正常启动退出后的原文件比较：3,647 个哈希不变，6 个变化，2 个 SQLite 临时文件不再单独存在。具体核对如下：

- 原素材及其他配置文件不变，授权数据保留。
- `touch_task.json` 及备份仅更新时间变化；`auto-reply-state.json` 仅 `last_event` 和更新时间变化；两个诊断日志追加运行记录。
- SQLite WAL/SHM 在正常关闭时合并。用备份数据库及其 WAL 的副本与升级后数据库副本比较，完整性检查为 `ok`；32 张表共 2,391 条原记录无丢失，只有 8 条内置音色记录的 `updated_at` 变化，其他内容一致。
- 新增更新缓存、诊断状态和队列文件，属于本次功能正常写入；未清理原素材、配置或旧发布包。

原始本机证据：`desktop/.build/cloud-full-acceptance-data/` 的 `before.json`、`after-install-comparison.json`、`after-upgrade-comparison.json`、`data-audit.json`、`baseline-runtime.json`、`download-ready.json`、`upgrade-processes.json`、`server-auto-restarted.json`、`after-runtime.json` 和 `admin-after.json`。构建清单在 `desktop/.build/cloud-full-release-1.0.0.json`、`cloud-full-release-1.0.1.json`。这些包含本机状态的证据未提交到仓库。

验收驱动有两处独立限制：首次注入错误的脚本使用了不可用的 `require` 求值方式，修正驱动后完成上报；更新就绪截图超时，但此前状态证据已落盘，随后正常退出、安装器启动、新版自动启动及再次登录均有独立证据。新版诊断页截图已成功。

## 先前隔离机制实测

- 服务地址：`https://115.159.88.100`，证书正常校验；健康响应 `ok: true`。服务 `ai-maintenance` 为 active。云防火墙只新增 TCP 443，未开放管理员端口。
- 17:22 至 17:23（北京时间）隔离程序从 0.1.0 自动升级至 0.1.1。旧版状态 `ready`、100%、队列 0；新版状态 `current`、队列 0。两个版本共享同一随机安装编号。
- 新版 `hadSentinel: true`，`preserve-me.txt` 内容仍为 `preserved across automatic upgrade`。
- 新版安装包 SHA-256：`0571b9477d03bc8cbd006ba4f86fc64b7868b6fb3bb83ec0b888d129b576f341`。签名清单序号 `1788772779971`；只发布到 `smoke` 频道。产品 `test` / `delivery` 频道未发布此程序。
- 服务端收到预设 `smoke / controlled_failure / smoke_failure` 一条，测试正文 `PRIVATE_CUSTOMER_TEST`、测试密钥 `sk-NEVER_UPLOAD_TEST` 与私有路径均未出现在后台数据中。
- 错误队列离线保留、重传去重、篡改签名/安装包拒绝、不同频道拒绝、低序号拒绝已完成针对性检查。后台复发判断额外验证重复记录不重新打开问题，新记录可以重新打开。
- 通过真实后台页面将上述隔离测试问题标记为已发布修复、版本 0.1.1，刷新后状态及版本仍保留；这只关闭预设测试问题，不代表产品业务故障已验收。

原始升级证据在本机 `%APPDATA%/aihuoke-maintenance-smoke/evidence-0.1.0.json` 和 `evidence-0.1.1.json`；隔离包在 `desktop/.build/cloud-smoke/1788772537115/`。以上为本机验证材料，不是客户交付文件。

## 界面与检查范围

后台以真实服务器数据在 Chromium 验证，宽度 1365 / 390，无页面横向溢出。Electron 诊断页以真实组件和样式、隔离 IPC 状态在桌面及 640px 验证，开关可操作且无页面横向溢出。隔离页面未连接原诊断日志服务，其对应提示不是完整产品运行故障。

`build:test` 通过；诊断日志及 IPC 原有专项、更新专项和服务端专项通过。新模块语法检查通过。

首次 `tsc --noEmit` 因仓库已有 React 类型缺失失败，此限制仍存在。后续完整发布阶段，依据当前代码修正内容引擎 preload 的 `batch`/火山配置接口清单、共享角色与显式登录页面、创作页当前文案/配置接口及已移除旧朋友圈操作面板的包检查。完整源码自检及产品详情图本地 E2E 通过。诊断测试数据改为拼接构造模拟密钥，原有敏感信息扫描及脱敏断言保持不变。现有联系人 helper 的头像能力经自检和模拟数据库读取验证，发布摘要同步到当前二进制，没有降级 helper 功能。

源码自检及本地 E2E 的已通过结果用于后续构建恢复；每次源码提交变化均重新生成带对应提交的配套运行时，两个版本均通过完整包校验。未手改来源 manifest 或跳过包内校验。仅版本号变化时未重复运行无关源码检查。

Code review: skipped (ce-code-review unavailable)。该 Skill 引用的 `references/entry-and-output.md` 缺失，改用人工差异审阅。精简复核重点包括状态协议、去重和资源清理；未为了复用而将大安装包的流式哈希改为同步整文件读取。Impeccable 检测器缺少 HTML 解析依赖而降级，因此视觉结论依据真实截图检查。

## 尚未验收

真实微信/视频任务执行中退出升级、跨机器安装与长期运行。完整测试版本已按授权本地提交并发布到测试频道，未推送代码。安装包是内部评估制品，未做 Authenticode 签名；正式域名/证书及正式频道尚未启用，也未加入计费。

后续客户试用应先选择指定电脑，保存编辑内容、暂停任务后正常退出升级，并核对版本与原配置/素材；长期离线队列和真实工作负载继续观察。真实微信动作继续遵守账号及联系人授权边界。
