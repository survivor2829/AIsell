# 1.0.2 内部更新验收

## 当前结论

本机已安装并验证完整测试版 1.0.2；旧正式版已卸载，两套原数据和完整备份保留。反馈服务已部署并完成实际安装版闭环。最终 CI 两个作业均成功；内部频道已发布1.0.2，公开签名清单回读通过。

## 版本与安装包

- 应用、两套业务运行时、便携包和安装器提交：`215392cc3cd0861226f9478869fec84bc5239324`，构建号 `20260908T0852Z`，构建时工作区干净。
- 安装器：`release/AI获客 V1.0版本-测试版-安装程序.exe`，441,190,823 字节。
- SHA-256：`acec8829fdec0d8b8b19f72905e7fdeda94bdfd81f079c357d764c31c9f5c501`。
- Remotion 清单摘要：`be3a39a0ef6f1977c80be568045c2972c7fbff6dd9e2ca8ded1ce04633257f5b`。
- 安装范围为当前 Windows 用户，appId 为 `com.aihuoke.desktop.test`；制品类型为 `internal-evaluation`，未启用正式客户频道。

CI 配置随后在独立工作区完善。CI 提交 `fd5b6b7d910ef7248e81b1dbdc7ed1ebcd85133a` 相对包提交只改变 `.github/workflows/ci.yml`、归档自检夹具及触达状态自检等待时间；两个自检文件均不进入安装包。已核对安装后的应用源码树、内容引擎运行时树和 EXE 与候选包完全一致，没有为 CI 编排修改正在验收的应用。

## 检查与首次 CI 适配

本轮完整 Node 源码自检、产品详情图本地 Playwright E2E、运行时/隐私边界检查通过。65 项确认稿制作测试、4 项制作聚合、4 项用量账本及2项维护服务测试通过。最终运行时均重新构建，便携包与安装器完成原有包内依赖、资源、版本、源码树、解包一致性及运行时校验，没有手改来源清单。

完整源码与本地 E2E 在 `cff3ce9` 已通过，随后仅为 PowerShell 自检夹具添加 UTF-8 标记；该变化定向复验通过，才继续从 `215392c` 重建全部运行时和包。最终云端 CI 单独执行完整检查，不把复用的本地证据写成云端成功。

最终 [GitHub Actions 34209624866](https://github.com/survivor2829/AIsell/actions/runs/34209624866) 的 `service-and-content` 与 `source-and-renderer` 均为 `completed/success`，对应提交 `fd5b6b7`。包括完整源码检查及测试/交付界面构建。

首次托管 Windows 运行暴露并处理了这些环境差异：

- 几何检查包含多张完整像素图的 PowerShell 运算，原30秒时限不足，放宽到180秒，几何断言未变。
- 两个临时 PowerShell 文件缺少 UTF-8 标记，英文系统按本地编码读取造成中文解析错误；补齐标记。
- 跨日测试按北京时间建模；Node 环境和 Windows runner 系统时区统一为北京时间，Python 明确使用 UTF-8。仅修改临时 CI 机器，不改用户电脑系统时区。
- 原生 `tar.exe` 在英文 Windows 上无法打开夹具中的中文参数。归档一致性夹具采用中性文件名，保留完整性、篡改拒绝和清理失败断言；真实中文产品安装包在本机完成打包、解包及安装验收。
- 50个模拟联系人的多次原子落盘曾超过10秒等待上限，状态检查等待改为60秒，定向检查通过，业务发送逻辑未变。
- 旧图标编辑工具不能读取中文绝对路径；先在构建目录用短相对文件名嵌入图标，再重命名 EXE。失败调用及修复调用均有独立副本对照，最终安装 EXE 内七档图标与确认的 ICO 资源逐一一致。

## 本机安装、数据与桌面

完整测试安装器静默覆盖安装退出码为0。实际安装目录为 `%LOCALAPPDATA%/Programs/AI获客-测试版`，Electron 确认为 packaged、版本1.0.2，继续使用 `%APPDATA%/xiaoxi-active-touch-test`。既有授权可用，显式“登录”入口正常。

实际安装版验证了角色设置保留、三种形象加载、离线教程、四步创作导航、制作聚合与请求用量、吐槽中心及反馈回执。当前创作页无素材为原有素材已归档状态，没有为验收新增付费生成。检查窗口宽度1426 CSS像素，无横向溢出或损坏图片；本轮没有重复早前9种形象和紧凑窗口的全部视觉验证。

安装前备份目录：`%APPDATA%/xiaoxi-update-backups/20260908-before-1.0.2/`。测试版4,697个文件、旧正式版241个文件逐一校验副本摘要一致。

安装和启动验收后：

- 测试版4,673个原文件摘要不变，24个文件变化，无原文件丢失；变化涉及缓存、日志、任务状态、云端状态及数据库。数据库原记录核对结果如下，不将全部运行文件宣称为逐字段完全不变。
- 内容数据库32张表共2,419条原记录保留，完整性为 `ok`；只有8条内置音色记录的 `updated_at` 变化，没有原记录键丢失。
- 产品详情图数据库4张表共2条原记录保留，完整性为 `ok`，内容不变。
- 旧正式版卸载器退出码为0，程序与桌面入口已移除；卸载后241个原数据文件摘要仍全部一致，备份继续保留。
- 桌面只保留“AI获客 V1.0版本-测试版”，启动目标与图标都指向已安装 EXE，解除此前项目 ICO 的临时依赖；真实桌面截图确认最新机器人图标正常显示。

## 服务部署与反馈闭环

按用户明确授权将同提交的 `service.py`、`feedback.py`、`admin.html`、`promote.py` 与 `upgrade.sh` 部署到 `115.159.88.100`。升级先使用 SQLite backup API 核对完整性并备份旧代码，再重启与检查服务。服务器备份为 `/var/lib/ai-maintenance/backups/20260908T083526Z-342996`；TLS身份、旧诊断及已发布更新文件保留。

维护服务为 active，公开 HTTPS 健康接口通过客户端真实证书校验。使用已安装1.0.2的“吐槽中心”提交一条明确标记的发布验收反馈，关闭附带诊断；后台依次设为处理中、已解决，客户端刷新后实际显示已解决。

首次验收驱动在确认回执前结束，重启后沿用同一反馈 ID 与固定内容收到了回执，没有新建第二条记录。该过程验证了实际客户端恢复与幂等接收；不将驱动时序错误记为产品验收成功的初次提交路径。

## 内部频道发布

最终 CI 成功后，从包对应的干净提交发布上述同一安装器。发布脚本退出码0，服务器摘要校验通过，原子切换 `test` 频道到1.0.2，历史签名清单保留。公开 HTTPS 健康正常，客户端同一验证器校验 Ed25519 签名通过；回读序号 `1788859753002`，版本、441,190,823字节及 SHA-256 均与本机验收包一致。

发布后自动化尝试启动第二个安装版实例，被现有实例的单实例机制正常退出（进程退出码0），未完成本次公告窗口截图。只读检查确认实际安装版已有主进程及视频渲染工作进程运行，未为补截图关闭或干扰该实例。现有公告缓存最后刷新早于本次发布，等待正常元数据刷新；不把外部清单回读写成已完成发布后的界面验收。

已支持更新的内部测试版在启动或每6小时检查后下载，用户正常退出后安装。其他电脑是否已更新须看各机实际版本；本次没有替其他电脑完成安装验收。

## 仍需人工试用的范围

本轮没有运行真实微信发送、点赞评论或朋友圈发布，也没有新建付费视频。新四步界面下的新素材整条视频质量、真实任务执行中退出升级、异机安装与长期运行仍需相应范围的试用验证。其他内测电脑是否完成安装以各电脑实际版本为准，服务器发布不等于所有电脑已更新。

正式域名/频道和 Authenticode 签名仍未启用；`tsc --noEmit` 的既有 React 类型配置问题不在本轮通过项内。当前结论限于内部试用，不是全场景或正式商业发布验收。

本机原始证据保存在 `outputs/experience-refinement-20260908/installed-1.0.2/`、`desktop/.build/internal-release-test.json`、`internal-release-1.0.2-final.log` 与 `internal-release-1.0.2-packaging.log`，未将用户数据、回执凭据或完整运行日志提交到仓库。
