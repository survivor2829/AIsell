# 1.1.21 内部增量发布记录

## 范围

- 修复精准触达按微信号搜索无本地联系人时误用回车打开“搜一搜”候选的问题；明确无结果后跳过当前微信号并继续下一位。
- 修复发送前草稿快照仍按纯文本备份剪贴板的问题；复用共享快照与恢复，覆盖文字、图片、文件及可保留的应用私有格式。
- 保留安全边界：会话身份变化、已点击发送或发送结果不明仍暂停，不自动跳过或补发。

## 构建与发布证据

- 修复提交：`e50eca8`；版本与公告提交／构建提交：`d4fb42c7243a56e57842e3481ff24c3f1288c47e`。
- 无真实发送的主动触达 dry run、消息序列、任务 IPC、微信编排及 `npm.cmd run build:test` 通过。
- Windows CI：运行 `34686391807`，`service-and-content` 与 `source-and-renderer` 均通过。
- `npm.cmd run release:internal` 从干净提交构建组件增量并通过源码自检、详情图本地 E2E、运行时门禁、测试界面与候选组合校验；组件归档合计 `70482514` 字节，底座版本 `1.1.6`，底座摘要 `8aeab74252cc4ba1258ffa46c0112429511eab3483e6595208065c25a21f7e9b`。
- `npm.cmd run publish:internal` 返回 `published: true`，上传4个缺失归档。公开端点读回的签名清单为 `1.1.21 / test`，序号 `1789206954145`，发布时间 `2026-09-12T09:55:54.145Z`；四个组件的 SHA-256 与本地候选一致。

## 待异机验收

1. 从 1.1.20 点击“检查更新”升级，确认版本显示 1.1.21，原联系人、话术、任务和配置仍保留。
2. 任务中安排一个“搜一搜”也找不到的微信号，确认该联系人被跳过，任务自动继续下一位，不显示整任务失败。
3. 使用一个有效联系人继续执行，确认文字、图片、网址按原顺序处理；若任务提示结果不明，先到微信核对，不要重复发送。
4. 在剪贴板中先放一张图片或文件，再启动精准触达，确认发送前快照不会使任务在第一位客户处停止，任务结束后原剪贴板仍可用。

本次未在构建机执行实际组件切换或真实微信发送；增量发布成功不等于异机和不同微信版本已经验收。正式客户频道未变。
