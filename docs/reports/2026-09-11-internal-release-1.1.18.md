# 1.1.18 内部发布记录

2026-09-11 完成主动触达恢复安全修复的内部 `test` 频道整包发布。正式客户频道继续关闭，本次没有推送源码。

## 修复与构建

- 复核发现恢复判断对缺失或格式异常的发送状态不够严格，可能把不明确状态当作可重试；已增加持久化 `send_attempted` 标记，严格校验重试门禁和图文分段账本，并让统一任务控制器按已保存的话术配置判断单段/多段任务。
- 修复提交：`8ddc1189ce9785b861da44b7c7f277b84dff7df8`，分支 `codex/content-brief-workflow`。
- `npm.cmd run release:internal` 从该干净提交完成源码 self-check、product-detail 本地 E2E、运行时门禁、测试界面构建、便携包和安装器构建；构建输出报告所有源码 self-check 通过。Vite 仍有既有 CJS API 弃用和主 chunk 超过 500 kB 的提示，不影响构建结果。

## 制品

- 版本 / 构建号：`1.1.18 / 20260911T0354Z`。
- 安装器：`release/AI获客 V1.0版本-测试版-安装程序.exe`，527259753 字节，SHA-256 `9cdf9d96ac1d7879fb936dbcfd4e1a360d5d8d21245094c3cbd7235b7a3fd28e`。
- 便携包：`release/AI获客 V1.0版本-测试版.zip`，719065003 字节，SHA-256 `fe6b754b2ef336fbb81c19d2b4e2ded082a17c0e3655e6e4a45a76571bc2e7ce`。
- 版本清单确认 `installerBuildCommit` 与修复提交一致；制品类型为 `internal-evaluation`，appId 为测试版，`signed=false`、`commercialReady=false`，数据目录与正式版隔离。

## 频道读回

- 发布脚本返回 `published: true`，频道 `test`，版本 `1.1.18`，序号 `1789099477938`。
- 从 `/v1/releases/test/latest` 读回并用包内公钥验证签名通过；读回公告与本地公告一致，SHA-256 和大小与本地安装器及版本清单一致。
- 本次只发布完整测试安装器；未发布组件增量包，正式客户频道未启用。

## 验收边界

- 用户对前一轮候选反馈“没有多少问题”，但未提供逐项验收清单；本次安全硬化后重新构建的候选尚未由 agent 安装，也未由 agent 执行真实微信发送。
- 当前包的微信重新登录后继续原任务、指定账号和联系人范围、当前主流微信与不同版本兼容、安装升级及其他电脑安装仍待分别验收。已发送、已点击或结果不明的记录仍不会自动补发。
