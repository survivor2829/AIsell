# 火山语音首次试听记录

2026-09-05，承接用户完成账号登录和软件 API Key 保存后的验证。

- 实际开发版 IPC 确认 `configured=true`、`secureStorageAvailable=true`。这只能证明应用能读取已保存的加密配置，不代表服务鉴权或音色权限通过；没有读取或输出密钥明文。
- 三个新音色仍为 `pending/not_ready`。先尝试直率英子，先前一次点击被本地主窗口焦点检查拒绝，另一轮在定位按钮阶段结束，均未发出云请求；恢复窗口与面板后发出一次真实试听请求。
- 该请求在 2026-09-05 15:20:50 中国时间返回失败。只读运行数据库确认 `volc-straight-yingzi@1` 的试听记录为 `failed`，`error_code=cloud_request_rejected`，没有托管音频文件。另两个候选未请求。
- 原主进程错误白名单遗漏此错误，界面误报“内容引擎暂时不可用，请重试。”。没有为重新取得错误数字码再次调用接口，也没有重置缓存、改写运行数据库或批准声音。
- 已补齐云请求与声音预览错误的静态公开提示，保留真实错误类别，结果不明明确禁止重复提交，不显示供应商原文。`content-engine-voice-preview-errors.self_check.cjs` 专项通过，开发版已正常重启加载；本次没有改界面或重新构建。
- 整体 IPC 自检仍在较早的频道清单断言失败（未包含前轮新增的三个火山设置频道），本次只报告上述独立专项通过，不报告整个 IPC 自检通过。
- 现有试听表只保存错误类别，不保存供应商数字业务码；本次无法从现存记录还原具体拒绝原因。不将服务未开通、密钥错误或余额问题中的任何一项表述为已经证实的唯一原因，也不推断实际扣费金额。
- 用户回复“还没开通／不确定”，当前需在 API Key 对应项目中确认语音合成大模型 1.0 字符版（`seed-tts-1.0`）的服务状态。已打开新版开通管理入口，等待服务可用后再继续试听。

操作回执位于 `outputs/video-optimization-20260905/voice-auditions/`，其中 `yingzi-attempt.json` 为本地点击校验失败，`yingzi-focused-attempt.json` 为实际请求后界面提示。原音频、旧视频与密钥文件均未覆盖。
