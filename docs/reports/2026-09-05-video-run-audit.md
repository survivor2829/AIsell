# 2026-09-04 成片长任务复盘

核查日期：2026-09-05。时间均为北京时间。

## 结论

- 对应 Codex 任务为「规划内容生产与创作工作台」，ID `01a06615-399b-7fb1-a05c-ba93809f4471`。
- 用户所指的长轮次于 9 月 4 日 10:18:45 开始，最后事件是 19:06:48，日志跨度约 **8 小时 48 分钟**。这是开发、等待、操作和检查的合计跨度，不能称为视频渲染耗时。
- 当天使用的测试内容引擎数据库中，9 月 4 日新增成片记录为 **0**。长轮次内创建了 **28 个批量任务、19 个新批次**。
- 最后实跑批次 `#98CCA2` 没有生成视频，随后被取消、归档。最后一轮代码修复没有后续真实成片验收记录。
- 9 月 3 日人工调整脚本后生成的两条约 40.5 / 42.1 秒样片，数据库记录仍为 completed，但原视频文件与导出样片目录现已不存在。9 月 4 日日志包含对应旧样片清理行为。
- 确实完成了代码修复与测试残留清理，但「原 5 段素材全自动生成 2 条自然、完整、至少 30 秒的成片」没有完成。

## 时间线

| 时间 | 主要工作 | 实际进展与问题 |
| --- | --- | --- |
| 10:18–10:42 | 删除 5 个测试沙箱、修复脚本与时长判断、运行检查、重启应用 | 清除 401 个未跟踪测试文件；准备首次新批次 |
| 10:42–11:16 | 新建批次、规划、升级事实分析缓存 | 出现缺乏依据的能力/数字描述；新代码又暴露字段类型、循环缩进漏镜头、空白帧阻断等问题，多次修复后重新分析 |
| 11:16–11:40 左右 | 改造逐句事实审查 | 原 90 秒超时提高到 180 秒仍超时，之后改为分段请求；拆分降低单请求负担，但增加串行请求数量 |
| 11:40–16:27 | 多轮写稿、审查、重写、改代码、重启和新建批次 | 反复遇到无依据能力描述、口播与短镜头不匹配、字数边界、正常建议被误判、运行进程重启/请求超时等问题；16:26 左右一轮仅留下 1 条 planned 候选，没有视频 |
| 16:27–18:54 | 改为结构化观察式重写，继续真实复测 | 修正分类字段矛盾、标点/“是否”格式、段落超长与重复、候选只能使用原选材等问题；最新几个批次仍未进入成片，最终批次取消 |
| 18:54–19:06 | 最后补丁、复核、归档、准备重启 | 19:02 归档 `#98CCA2`；19:02:15 发起关闭旧 Electron 的工具调用；19:06:45 只返回运行中的 cell，之后没有完成回执、重启实跑或最终交付总结 |

最后一个工具调用不能证明进程确已关闭，也不能证明停滞一定由审批、应用崩溃或用户中断造成；当前日志只足以证明没有后续完成证据。

## 运行结果口径

数据库：`C:\Users\Scott\AppData\Roaming\xiaoxi-active-touch-test\content-engine\content-engine.sqlite3`。本次通过 SQLite URI `mode=ro` 读取。

| 范围 | 结果 |
| --- | --- |
| 9 月 4 日北京时间全天 generated_videos 新增记录 | 0 |
| 长轮次 10:18:45 之后创建的 narrated_batch_v1 任务 | 28 |
| 上述任务状态 | completed 12、failed 10、cancelled 4、paused 2 |
| 长轮次新建批次 | 19 |
| 上述任务 created_at 到 updated_at 时长之和 | 12,856.9 秒，约 3 小时 34 分 17 秒 |
| 最终 `#98CCA2` | 候选为空，未制作视频，任务取消，批次归档 |

`completed` 在这里表示后台规划任务结束，不代表视频完成。任务寿命包含等待、重写和状态更新，不能当作纯 AI 调用时间，也不能与 8 小时 48 分钟作精确加减来推导编码耗时。

取数条件（数据库时间为 UTC）：

```sql
SELECT count(*)
FROM generated_videos
WHERE created_at >= '2026-09-03T16:00'
  AND created_at < '2026-09-04T16:00';

SELECT status, count(*)
FROM content_tasks
WHERE task_type = 'narrated_batch_v1'
  AND created_at >= '2026-09-04T02:18:45'
  AND created_at < '2026-09-04T16:00'
GROUP BY status;
```

按主会话原始记录的工具调用条目统计，长轮次有 1,708 次外层工具调用，其中 exec 1,357 次、spawn_agent 27 次，另有 16 次上下文压缩。这些数值用于描述开发过程反复切换与协调的规模，**不是百炼 API 请求次数、成功启动子代理数量或费用**。当前数据库没有可直接据此核算供应商请求数与账单的完整表，不能推算花费。

## 当前代码解释了哪些问题

1. **串行审查和嵌套重写拉长了链路。** `narrated_batch.py:1816` 设置事实审查单请求至少 180 秒上限，`:1871` 起逐候选逐段执行。之后还有视觉复核。`:2046` 起每候选可修复两次，`:2330` 起还有批次编排轮数；内部结构化返回还可能纠错。次数有界，整体时间和调用预算没有统一约束。
2. **素材缓存已经有，但审查缓存容易丢失。** `narrated_batch.py:1681` 的缓存键包含候选 ID 和段落 ID，`:1820–1836` 又只保留本轮匹配项。候选重建或重分段容易导致未变内容重新审查。日志 17:04 声称恢复可复用三段，17:07 又确认候选编号变化导致不能复用。
3. **合格候选不一定被制作。** `narrated_batch.py:2767` 在目标 2 条却只有 1 条时返回零生成；`:2774` 在本轮刚完成 story v2 规划时返回 ready，需要后续制作动作。数据库里的规划完成和用户要的成片交付因此存在明显距离。
4. **最后的重写方式存在口播质量风险。** `narrated_batch.py:1150` 固定拼接“准备现场试机”“观察、判断、记录”等句式。它方便限制事实与字数，却可能产出检查清单式文案。昨天 16:27 的人工观察已指出一条机器通过稿仍在泛泛凑字。
5. **最终声画对应仍有覆盖缺口。** `narrated_batch.py:1611` 使用规划阶段取证帧，最多选 12 帧；`creative_domain.py:4367` 在实际 TTS 后将各镜头裁成中央片段；`narrated_batch.py:2706` 的后续检查没有重新核对最终实际保留画面。存在原取证帧被裁掉的可能；这是代码覆盖缺口，不是对昨天某条不存在的新成片作声画错配判断。

30 秒限制已实现：`narrated_batch.py:247–303` 用已测声音估算字数，`creative_domain.py:4386` 检查实际配音至少达到目标加 100ms。现有镜头容量又使用统一 260ms/字，两个预算体系偏保守；应统一校准，不能通过静音或随意缩短规避要求。

昨天最后一批未提交修复已包含全素材池补镜头、准确计算模板占字、每段一个重点、短语标点与分类容错等。当前源文件修改时间为 9 月 4 日 19:01:03。应把它们标为“已改、未完成真实验收”，不再重新当成未修问题重复开发。

## 建议的下一轮范围

1. **先稳定交付一条，再复制到第二条。** 保留 5 段素材与至少 30 秒要求；第一条完成审核后立即走配音、最终时间轴、低分辨率样片和完整 MP4，目标数量不足应明确显示部分进度。通过后再验证第二条的差异。
2. **把反复工作缩到失败段。** 审查缓存使用文本、事实/镜头区间和审核版本的稳定指纹；保留未改段，只有发生变化的段落重新审核。以小组处理降低请求开销，设统一的耗时/调用预算。同类失败连续出现时停止整批重跑并保留具体失败数据。
3. **统一事实、文案和时长的职责。** 素材分析提供能证明的事实，AI据此写自然口播；程序分配镜头与预算，实际 TTS 决定最终时轴。修改失败句时同时保留合格内容，避免将所有题材都改成“观察—判断—记录”的模板。
4. **检查真正要交付的画面。** 审片覆盖配音后实际保留的片段，确保支持该句口播的关键画面不会在裁切时丢失。
5. **固定一次实跑的代码与进程。** 修复并编译后启动稳定进程，运行期间不热更新；每次结束交付样片或明确失败报告。清理与成片验收分开收尾，新基准片确认可用后再处理旧基准片。

下一次验证只需编译与受影响环节的必要检查，加原素材一条端到端实跑；不需要重新跑一串无关测试。验收仍是自然口播、有用内容、至少 30 秒、同步字幕、真实声画对应、可播放导出，再扩到两条。

## 证据定位

原始会话文件：`D:\OpenAI\CodexHome\sessions\2026\09\03\rollout-2026-09-03T20-11-58-01a06615-399b-7fb1-a05c-ba93809f4471_01a0672e-d7ff-7511-b0b6-991f9dc609df.jsonl`。

- 第 1420 行：9 月 3 日两条人工调整样片的交付说明及自动质量未通过的限制。
- 第 2266、2273 行：9 月 4 日长轮次开始及用户请求。
- 第 3201 行：两条规划约 41 秒产出，随即发现事实问题。
- 第 3598 行：新事实缓存漏记镜头和字段返回不兼容。
- 第 4168、4395 行：90 秒、180 秒两次审查超时。
- 第 6752、9915 行：段落与镜头容量冲突的诊断。
- 第 8938、13424 行：运行中应用重启/退出打断请求。
- 第 11845 行：机器通过的一条候选仍被人工判为泛泛凑字。
- 第 12709、12812 行：恢复缓存的预期与候选编号变化导致重审的事实。
- 第 14933 行：旧两批成片与中间文件清理约 731MB 的记录。
- 第 15277、15328 行：最终批次取消、全素材池修复。
- 第 15441 行：最后代码检查通过，但真实成片验收尚待执行。
- 第 15512–15520 行：最后批次归档。
- 第 15524–15529 行：关闭旧进程的工具调用仅返回仍运行，后续缺少完成记录。

本次仅做日志、数据库与源码只读核查，新增本复盘文件；没有修改业务代码、运行新云端生成、重启应用或清理任何历史文件。
