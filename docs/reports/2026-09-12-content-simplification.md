# 内容创作精简与 1.1.20 内测验收

## 范围与结果

旧批次三个素材拆为177个区间，60次画面事实提取、12次文案生成／复核，1483187输入和99983输出token，合计1583170；已有两份稿仍被三个方案门槛阻断。此次同时修复选择、保存、分析输入放大、制作恢复及网关凭据链。原恢复备份 `recovery-backup-20260911-material-db/` 保留且不提交，原素材和运行数据不删除。

新流程及完整文案／零碎想法的输入约定见 [PRODUCT.md](../../PRODUCT.md)。默认一稿，换方向追加；已有有效稿可确认，不再要求三个稿或固定第一稿框架。真实事实、确认正文、不可读素材和实际时长仍核验。

## 门槛替代依据

| 原门槛 | 保护对象 | 调整与证据 | 剩余限制／失败策略 | 回归 |
|---|---|---|---|---|
| 三稿且第一稿固定框架 | 备选完整性 | 按所选有效稿、版本和确认正文核对 | 备选可能较少，直接允许确认 | 一／两份及旧批次 |
| 全素材精查后出稿 | 内容真实性 | 缓存、转录、覆盖各素材的代表画面形成带来源摘要，所用镜头精查 | 抽样不证明整段事实，定向补证据 | 未选镜头不精查，原证据缓存保留 |
| 审美低分阻断 | 表达质量 | 编辑建议与事实错误分开 | 美感需用户判断；真实矛盾仍阻断 | 低分可制作、事实矛盾拒绝 |
| 配乐必选 | 音频完整性 | 无配乐贯通设置、合成、渲染和导出 | 正常口播与声音校验保留 | 真FFmpeg无配乐输出、缺失声音拒绝 |
| 结果未知后重试 | 重复付费保护 | 持久化请求状态和产物，恢复时检查 | 无回执保持未知，不自动重提 | 中断与重启保留结果 |

## 离线费用回归

使用本批制作前SQLite备份及截至备份时的实际用量记录，由 `desktop/scripts/replay-narrated-summary.py` 回放，不调用云服务。

- 原177个区间，9个代表画面区间；详细来源248992字节，精简来源44651字节，输入规模降低82.067%。
- 文案事实复核来源6808字节；对照旧12次全量文案提交，推算写稿操作输入规模减少98.28%。这是输入回放，不是供应商账单。
- 旧通用分析已缓存但代表画面未缓存时，首次文案预计5次模型请求（3次代表画面、1次写稿、1次复核）；画面证据也已缓存时预计2次。结构修正与限流重试计入真实请求预算，不另开嵌套额度。
- 新流程从零写稿的真实token、耗时、人民币金额未验收，保留未知，不用回放比例代替实测。

## 真实成片验收

- 批次 `narrated_batch_a1968f57044840a49760b1b5ca34f927`；第一稿 `narrated_candidate_0c9962f283dd4f17863cc2a7d403b4fa`，修订2；视频 `generated_video_d31d54c5b394483ca7934974de71f656`。
- 用户先选第一稿，事实检查指出频率／效果断言及镜头对应问题后，明确批准修订稿再制作；不是原稿未经调整直接通过。镜头事实提取按素材返回，曾打乱确认编排顺序，现保留原镜头顺序。
- 真实制作中发现通用Ark密钥错误覆盖本机有效TTS密钥。先修复、部署服务端能力声明，再刷新客户端会话及能力，从配音阶段继续；最后一次继续没有重新分析或调用LLM。
- 最后一次继续从07:25:29Z到07:30:25Z，约4分56秒，包括6次短语配音、6次ASR对齐和本地渲染。此前失败检查与修订制作的请求单独留账，不能删除后宣称整次制作只花这些时间或费用。
- 成片1080×1920，容器时长43.157秒（口播时间线43.199秒），18个不重叠来源区间、2个原素材；六段字幕拼接与批准正文完全一致，六段ASR对齐均匹配，完整音视频解码通过，无超过1秒的静音，音频约-14.87 LUFS／-1.22 dBTP。
- 实际Electron播放器从头到尾非静音播放，无媒体错误；自动导出MP4与原成片SHA-256相同。重启后批次、批准稿及同一成片仍可读取，未触发重做。用户反馈“成片可以，继续内测发布”，本条实际观感验收通过。
- 成片SHA-256：`3a1ce1d5d5931235478263fa69d460c1906d8ff918f95e2e12d0e29de0c81d11`。本地视频、播放、解码、用量、摘要回放和截图保留于 `outputs/content-simplification-20260912/`，不进入安装包。

## 代码与界面验证

- 143项内容流程／制作／服务测试通过，含32个子场景；最后统一制作预算修改后24项制作回归及8个子场景通过。
- 火山媒体10项、供应商用量42项、网关12项、维护服务5项及真实FFmpeg渲染相关检查通过。Node网关检查包含服务重启后有效期内旧会话恢复、能力改变同步、跨主体请求隔离。
- 真实React离线界面10个检查通过：6／9／15素材可见、保存竞态和重挂载、清空最后素材、完整原稿、旧一／两稿确认、无配乐及辅助接口失败。夹具无付费调用，不等同于真实生成。
- 仓库完整 `check:self`、测试界面构建、内容引擎IPC及文案IPC检查通过。候选构建仍从干净提交执行发布入口要求的完整检查，后续安装与发布证据独立记录。
- `tsc --noEmit` 受仓库既有React类型声明缺失影响，未将其记录为通过；本轮没有为此增加依赖。

## 服务部署及发布边界

2026-09-12 从干净服务提交 `6e428b6b4bfc525ec5dd52bc3ce500ec69e1d8c7`，按维护服务→供应商网关顺序部署；Shell脚本固定LF。维护数据库和代码备份为 `/var/lib/ai-maintenance/backups/20260912T072247Z-1718156`，网关回滚代码为 `/var/lib/ai-provider-gateway/backups/20260912T072249Z-1718196`。TLS健康检查通过，网关运行版本 `bc6f21004a00f499`。未修改服务器凭据；ASR使用已有独立配置，TTS如未在服务器配置则使用本机有效加密配置。

1.1.20 仅面向 `test` 频道；正式客户频道不变。本条验收不能证明其他素材、异机、不同微信版本或营销效果。安装包已完成本机覆盖升级、对应提交 CI 及 test 频道签名／下载读回验收，详见 [1.1.20内部发布记录](2026-09-12-internal-release-1.1.20.md)。
