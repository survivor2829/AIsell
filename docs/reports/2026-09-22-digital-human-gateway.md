# 数字人测试网关更新记录

## 授权与范围

用户同意更新现有测试网关并做连接检查。网关部署阶段仅发布供应商网关代码，不发布桌面安装包；后续的真人素材和付费样片仅在用户确认的内部测试范围内执行。

## 已完成

- 2026-09-22 17:31:44（北京时间）更新 `/opt/ai-provider-gateway/service.py`。新增视频生成、人物素材登记与按授权主体过滤的素材列表接口；非法图片地址类型在调用上游前返回 400。
- 更新前核对无网关活动连接；原服务器功能与候选代码的有效差异仅为新增数字人路由及校验。固定快照部署，运行文件与候选 SHA-256 一致。
- 新 SHA-256：`9f5c059f4f66f90d0fa89afb00ae0e2ce2b4214af65406ee55851e3c2b59a663`，健康检查运行标识 `9f5c059f4f66f90d`。
- 原 SHA-256：`b5feb65f4f377b89db2f1bc9bc010295eb276b6cc01062bfcc412d492df48db9`。
- 服务器备份：`/var/lib/ai-provider-gateway/backups/20260922T093144Z-927196`。原凭据环境文件 inode、大小、修改时间保持一致；回执数据库未删除或迁移。
- `ai-provider-gateway` 与 `ai-maintenance` 均为 active；服务器内部及桌面 HTTPS 健康入口均返回新运行标识。
- 新增接口的现有定向测试通过，覆盖固定路由、方法、素材库主体隔离及非法 URL 类型不触发上游。
- 当前真实 Electron 窗口已进入“选择形象和产品图片后，生成人物预览”状态，未就绪提示和“重新检查”按钮消失；结合页面条件分支可确认现有桌面授权会话及三项能力检查已通过。

## 尚未通过的验证

- 服务器使用现有 APIMart 配置请求免费素材列表 GET：第一次连接未成功；按实际网关代理环境（未配置代理）复查，30 秒后返回 `URLError / OSError / errno 101`。未收到供应商 HTTP 响应，不能据此判断密钥、余额或模型权限。
- 网络分层检查：服务器解析为 IPv4 `69.63.186.30`（443 连接超时）和 IPv6 `2a03:2880:f127:83:face:b00c:0:25de`（无路由）；没有 hosts 覆盖。工作站通过 Google 与 Cloudflare 的 HTTPS DNS 独立查询，均得到 `104.26.10.94`、`104.26.11.94`、`172.67.70.35`（TTL 300），与服务器解析不一致。随后从服务器对这三个地址逐一进行无密钥连接诊断，保留 `api.apimart.ai` 的 SNI 与证书校验，均在 TLS 建立阶段返回 `ConnectionResetError / errno 104`（约 0.3 秒），未发送应用层 HEAD 请求。仅改 DNS 或强制 IPv4 均不足以证明问题解决；连接重置的具体网络环节尚未确定。
- 上述地址仅用于一次性诊断，没有写入源码、hosts 或系统 DNS。未修改代理、路由、防火墙或关闭 TLS 校验。服务器上的 Google／Cloudflare DoH 直连也超时，因此可信 DNS 对照在工作站完成。
- 独立 Electron 探针使用临时资料目录读取真实授权密文时，发生本地授权读取失败；其请求未到服务器。此结果不代表真实应用授权失效，不复制或导出加密上下文。实际桌面窗口已完成能力核实。
- 后续已在用户确认范围内完成一次真人形象与产品的素材登记、12 秒视频生成和封面生成；素材审核、产品一致性、口型和动作仍按样片结果继续打磨，不把单条样片写成通用能力验收。

## 证据与后续

本地部署快照、旧代码、哈希清单、部署日志及现场截图保存在 `desktop/.build/gateway-digital-human-20260922T091430Z/`。快照记录来源提交与已审核的未提交网关差异，不将其描述为已发布的桌面版本。

## 19:16 续查与修复

- 回看 2026-08-03 已验收的产品详情图版本：当时使用本机加密的 APIMart Key 和本机网络。2026-09-20 提交 `028a847` 将测试版供应商请求切换到云端网关并移除本机密钥回退。当前服务器不会继承工作站已有的 `127.0.0.1:7890` 网络出口；这是本次“以前能用、现在接不通”的实际路径差异。
- 工作站对官方 API 的无密钥请求收到 HTTP 响应；服务器直连失败。使用仅限单次 SSH 连接的回环转发，让服务器借用现有工作站出口后，无密钥请求收到 401；在服务器内存中使用既有受保护密钥查询免费素材列表收到 HTTP 200，TLS 验证通过。密钥未显示或转存，未提交生成任务。
- 产品详情图 Python 在默认系统 CA 下访问测试网关会报 `SSLCertVerificationError`，指定内置测试 CA 后健康入口返回 200。客户端已限定仅网关固定地址使用该 CA，禁止重定向；APIMart 与 DeepSeek 产品详情请求沿用同一证书处理。定向 Python 50 项及 Node 自检通过，测试版界面 `npm.cmd run build:test` 通过。运行中的桌面进程须重启后才能加载主进程注入的新环境。
- 新增仅供 APIMart 使用的受控 HTTP CONNECT 出站配置，不影响其他供应商，不修改服务器系统 DNS/代理，不在失败后切换线路或重发付费请求。候选代码通过定向测试；2026-09-22 19:16 更新测试网关，线上 SHA-256 为 `eda46b5342575e9fb083136348073ab692f48fac6a962de9366b5219cf9a9b0d`，自动备份 `/var/lib/ai-provider-gateway/backups/20260922T111624Z-956990`。
- 实际免费列表响应使用 `ResponseMetadata` / `Result.Items`（含大写 `Name`、`Id`、`Status`），已修正网关解析并继续仅返回带当前授权主体前缀的项目。新版代码在服务器的临时回环测试会话内完成“授权 → 免费列表 → 主体过滤”，得到 HTTP 200 与空列表，未泄露共享供应商账户的其他记录。人物素材登记后的最终审核获取仍需用真实授权样片验收。
- 进一步发现共享素材库总量很大、默认只返回 20 项。实测供应商素材组的 `name` 过滤和素材的 `group_id` 过滤有效；数字人复查现在传本任务 ID，网关在当前授权主体前缀下查找完全匹配的组，再只读取该组的素材。客户端不能指定任意组 ID；组结果与素材仍作双重主体过滤。Python 网关定向测试及 Node 数字人自检通过。此修复于 2026-09-22 19:21 更新到测试网关，当前 SHA-256 为 `a61354d55ba81c5aeba61267725026199baa4f4511f908c952e761cec9296589`，前版备份位于 `/var/lib/ai-provider-gateway/backups/20260922T112123Z-959093`。真实本人素材审核尚未提交，因此尚未验收有记录的组查询。
- 桌面端在上传客户形象和产品图前，先调用本任务的免费素材组查询作为连接预检。预检失败会把任务停在“需要处理”，不会上传图片或提交生图；定向自检覆盖此失败路径。再次核对线上网关代码 SHA-256 为 `a61354d55ba81c5aeba61267725026199baa4f4511f908c952e761cec9296589`，`ai-provider-gateway` 与 `ai-maintenance` 均为 active；凭据环境文件的大小和修改时间未变。桌面端此项改动尚未安装到现有运行进程。

**当前限制：**长期可用的服务器出站线路尚未配置。本轮样片依赖内部开发机的受控 SSH 回环通道，通道随应用退出而关闭；正常在线网关仍使用原直连路径，不能把这条样片结果扩大为无人值守云端或正式客户能力。后续应在我们自己的服务端接入持续可用的受控出站，再用免费只读请求复验。不得把临时工作站转发或诊断 IP 配为长期依赖。

后续扩展素材或时长时，仍按“预览确认 → 12秒样片”顺序验收；网关健康、能力声明、免费连接检查、真实生成分别记录结果。

## 21:00 12 秒内部样片与封面

- 在现有测试授权、已登记的人物和产品素材范围内完成一条约 12 秒 Seedance 2.5 样片，并在成片中心保留视频与可保存封面；没有重复提交视频生成。
- 封面第一次在参考帧上传阶段失败，未产生图片生成调用；修正测试网关证书接入后只重试封面并完成。改标题沿用已保存底图，不重复生图。
- 样片人工检查发现顶部标题在约第 10 秒压到人物面部，人物主要展示并指向产品，尚未达到可直接发布的最终质量。该问题留给下一轮字幕布局和动作提示调整。

## 20:13 使用现有测试环境接通

- 本机原有产品详情图密钥文件及代理配置仍在，但独立 Electron 探针无法解密本机旧密文，因此没有将它用于回退，也没有把服务器密钥复制到客户端。
- 使用本机现有 `127.0.0.1:7890` 出口，通过 SSH 反向转发至测试服务器的 `127.0.0.1:47891`。只监听服务器回环，APIMart Key 继续留在网关的受保护环境中；未修改 Windows 或 Linux 系统代理。
- 测试网关环境文件仅新增 APIMart 专用 `XIAOXI_GATEWAY_APIMART_PROXY_URL=http://127.0.0.1:47891`，原文件已备份到 `/var/lib/ai-provider-gateway/backups/20260922T121321Z-relay-973620/`。第一次健康检查赶在服务监听前失败并自动恢复；修正等待后更新成功。其他供应商不走该线路。
- 在新运行网关进程中读取既有受保护密钥到内存，通过专用出口对官方素材列表执行一次免费只读 GET，返回 HTTP 200，响应结构为 `Result.Items`；没有打印或转存密钥，没有上传用户素材或提交付费生成。
- 仓库根目录的 `启动内部开发版.cmd` 及 `npm.cmd run desktop` 已接入只在内部开发环境使用的隧道管理：应用运行时建立，断开后尝试重连，退出时关闭。该方案依赖这台电脑及其已有代理持续在线，适合当前内部测试；并非给其他客户或无人值守云服务的长期出口。通道断开时桌面制作前预检会停止上传。
- 自动隧道脚本已实际启动并通过免费无密钥 GET 收到 401（证明到达供应商认证层）；内部开发版可见窗口随后启动，服务器回环通道在应用运行期间再次返回 401。随后使用运行中网关已有密钥，在应用管理的通道上执行免费列表 GET，再次返回 HTTP 200。窗口标题为“AI获客 V1.0版本 测试版 20260922T1048Z”。Node 语法检查与数字人定向自检通过；未在界面提交真人预览或视频生成。
