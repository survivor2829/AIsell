# 1.1.15 精准触达与自动回复复测

对应吐槽中心反馈 `85ddd06e-d30b-4e63-ba59-902c114b4c05`，客户端为 `1.1.14 / 20260910T0531Z`。

- 精准触达：13:55 的首个联系人在搜索前因 `wechat_clipboard_restore_unsupported` 中断，`done=0/4`、`send_attempted=false`。不是触达次数过多的证据；任务被标记为待处理后，调度器继续执行其他任务。
- 自动回复：14:02 已点击聊天，停在 `conversation_title_unresolved`，没有进入生成回复。代码只依赖一次全窗 OCR，标题失败时已消耗未读红点。本轮补同一帧的一次局部放大识别；精确接待名单、消息身份及重复发送保护继续生效。缺少故障画面，尚不能进一步断言字体、缩放或截图方式是具体触发条件。

## 复测步骤

1. 暂停任务、退出测试版后覆盖安装，确认版本 **1.1.15**。不卸载或删除数据。
2. 仅对已授权测试联系人运行一次精准触达，核对实际收到的内容与任务进度；不要将结果不明的历史任务直接重发。可分别在复制普通文字和截图后验证，检查任务结束后原剪贴板还能粘贴。
3. 单独启动自动回复，等待初始化完成后，由已授权测试账号发送一条新消息；核对是否读到消息、生成回复并只发送一次。另测已打开该聊天时的新消息；不要用启动前的旧消息当新消息验收。
4. 如果失败，保留微信当前画面并提交吐槽。后台字段尚未部署时，可使用 `collect-touch-reply.cmd` 导出已有匿名技术日志，回传生成的 JSON 和失败画面；不需要为了取日志再发一遍。

## 本地验证与边界

剪贴板回归先复现旧代码对可提供原生字节的格式报错，再验证原生备份及文字、图片、文件、独立流内容保留；无法提供数据的格式仍拦截。标题离线回归验证局部坐标、125%/300% DPI、帧内缓存、错误联系人拒绝；客户端日志、吐槽序列化和服务端匿名字段检查通过。

上述检查不等于故障机完整收发验收。朋友圈互动已有用户确认，本轮不修改其业务流程；朋友圈发布核验的旧待验收事项继续保留。后台部署、安装包生成和实机验收分别记录。
