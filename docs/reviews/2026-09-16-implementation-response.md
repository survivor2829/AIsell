# 精准触达交叉审查回应单

日期：2026-09-16
状态：回应已确认；第一档代码与完整源码自检已完成，等待从干净提交构建验收候选；第二、三档尚未开始。

## 核心判断

审查指出“新诊断码落入编排默认分支后造成全局暂停”的方向成立，但“所有失败都应继续”和“未知码默认可恢复”不成立。发送可能已发生、发送结果未知、账号或会话身份不能确认，以及任何未归类原因，必须继续 fail-closed；只有已知且明确 `send_attempted=false` 的环境类或可恢复类原因可以自动等待、重试或跳过当前联系人。

## 三档顺序

采用修正后的顺序：第一档先完成分级、未知码质量计数、明确未发送的有界恢复/未触达名单、未知结果人工账本闭环和版本证据；第二档再拆图片点击前后阶段；第三档处理认人强比对、UIA 草稿读取和常驻 PowerShell 性能。每档独立版本、独立提交、独立验收，不把未提交验证的代码打包。

## 第五节三个问题

1. 必须保留全局暂停：发送或发布结果未知、账号/窗口/会话身份可能漂移、授权或持久化完整性异常、以及任何未归类原因。
2. 同意三档目标，不同意原函“未知码默认可恢复”和把图片超时简单延长后先交付；未知码保持 fail-closed，图片超时与点击前后阶段在第二档一起处理。
3. 未公开微信号联系人继续排除；半自动兜底与认人三选二一起放在第三档设计，不能在第一档以降级匹配放宽身份门禁。

## 附录：客户机运行版本核对

### 已证实

- 干净 `1.1.33` 对应提交 `0ebf3ea69f9e646a1d6ddc7a01b94aacf7972b9a`。
- 其内部组件 application ZIP 为 `e720e2c2c92ec081f09aaa0d8170d012c51a179d0a4a6f6f6676c5e138a69b56.zip`，实算 SHA-256 与文件名一致，大小 24,796,228 字节。
- 上述提交不含当前的 `failure-evidence.cjs`、`failure-diagnostics.cjs` 或 `wechat-rule-catalog.json`。若晚间报告转录的 `rule_id` 属实，可以排除日志来自干净 `1.1.33`。
- 仓库中存在以下未发布、未现场验收的离线诊断候选；有清单的候选均以 `1.1.33` 为底座，但没有绑定完整 dirty 源码提交：

| 版本 | EXE SHA-256 |
| --- | --- |
| 1.1.34 | `e8190a5b1aaa2cd91acc9ea198b2eb2389fcdd32b66e0cddff2f826bb94dc205` |
| 1.1.35 | `301a372776b00cd5f86ee01324eee20a37b47b9ee8fac3c08c60e1f2ff249d40` |
| 1.1.37 | `7063c17b90b56eee54fc1265ec23c21a02841ba3b48709b698220be122265fe4` |
| 1.1.38 | `d003050f8dc0e9fc365a24459262352e0d093df791d413539e0240e5b05ffd61` |
| 1.1.39 | `f1050a81e915fb8e59dc72dfa539bf0f26c270325c0675028a3d2b8281ce740b` |
| 1.1.40 | `3482f1a5b009882acfcb7a7e3b80f2b7eb922fca5aef48baa11927afe951dec0` |
| 1.1.41 | `69b40ea7b5d71d35cfbdebaa4a8f421c716a5781d5f78064e52dab46bc6371d6` |

### 尚未闭环

仓库缺少审查报告引用的 4 个客户原始诊断 ZIP、4 张原始截图及客户机 EXE 的 `ProductVersion` 和 SHA-256，因此不能唯一确认客户运行了上述哪一个离线候选。若客户通过组件更新运行，还需取得客户机 `selection.json` 与当前 generation 的 `component-complete.json`。在这些证据补齐前，结论必须写为“可排除干净 1.1.33，但具体候选未知”，不能写成某一版本已被客户实际运行。
