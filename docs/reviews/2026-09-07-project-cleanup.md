# 项目清理记录

本轮在保留核心功能、原素材、已认可成片和运行数据的前提下清理本机残留与冗余代码。清理后的提交前计数由686个降至30个；本次本地提交收录这30个维护与清理文件，承接视频流程提交`efc747e`。

## 文件与目录

- 清理前：`outputs/`有665个未跟踪文件，其他代码、规则和审查记录21个。
- 删除`outputs/login-review/{profile,comfort-profile,galaxy-profile}/`中的`Cache`、`Code Cache`、`GPUCache`、`DawnGraphiteCache`、`DawnWebGPUCache`：15个可重建缓存目录、369个文件、58,073,278字节（55.38 MiB）。删除前核对了绝对路径与重解析点，未接触应用正式用户目录。
- `outputs/`中其余303个文件（含原本已忽略的过程文件）清理前后SHA-256一致，成片、图片、音频、脚本、文案和验收证据均未丢失。
- 将根`outputs/`加入Git忽略规则，避免本机视频、浏览器测试数据和截图混进提交；README明确正式工具、文档与本机输出的位置。忽略规则不删除磁盘文件。
- 核对769个已跟踪路径，没有误跟踪`node_modules`、`dist`、`.build`或`__pycache__`。唯一超过10MiB的已跟踪文件是中文字幕使用的Noto字体，保留。
- 当前运行时、安装包、依赖目录、原始素材、运行数据及回滚证据保留。未为减少计数隐藏应用源码，也未把可用功能当冗余删除。

## 代码与文档

- 合并火山TTS、方舟和ASR三处重复的preload加密传输逻辑；保留各自通道、输入格式、失败提示、异步错误传播和临时明文字节清零。
- 删除方舟页面中已无输入入口且不会发送到主进程的旧API Host状态、字段及无用的页面接口声明。旧百炼存储与IPC兼容入口保留。
- 火山接口的通道定义集中复用，总接口清单补齐已有9个实际入口，接口注册与声明一致。
- 修正现有检查中过期的音色公开字段及导航类型依据，继续检查完整字段白名单、私有字段不外泄和产品详情页真实入口；未删断言或绕过安全边界。
- PRODUCT.md更新为当前单素材池、三稿、多方向数量、确认后制作导出的流程；旧项目入口和历史任务兼容逻辑保留。
- 之前尚未提交的空日志函数、零引用mock-data删除、状态订阅防竞态、构建失败处理及模块规则整理，经核对后保留，没有覆盖或回退。
- 本轮新增复用整理2项、质量整理2项；7项收益有限或涉及额外行为变化的建议未采用，包括可选函数重命名、旧提供商入口退役及四项较宽的状态加载优化。

## 验证

- `npm.cmd run build:test`通过，1619模块。
- 12组重构前后加密传输离线对比通过，包含成功、握手失败、无效公钥及保存Promise拒绝；使用临时虚构值，无真实凭据读取或网络调用。
- 现有状态订阅、构建结果、批次IPC、内容引擎IPC、自动回复IPC、触达任务IPC、产品详情桌面集成及媒体协议检查通过。
- 内容引擎IPC检查先发现总通道清单及音色字段预期落后于现有接口，修正后通过；产品详情检查先发现类型已提取至AgentHomeTarget，按实际类型链和页面入口修正后通过。
- `tsc --noEmit`未通过：项目缺少React类型声明，导致TS7016/JSX类型等已有问题；Vite构建通过不代表独立类型检查通过。未安装额外依赖或用宽松声明掩盖问题。项目未配置独立lint命令。
- 本次未重新发送真实微信消息、调用付费生成、重打包或覆盖正式安装版。上述检查证明相关代码与协议检查通过，不把它们表述为所有真实微信场景重新验收。

本机删除清单和保留文件摘要位于忽略目录`.build/`，便于核对。清理与提交分别记录：前者实际删除缓存并整理源码，后者保存已核对的维护改动；本地提交不包含推送、打包或发布。
