# 微信拓客消融与收尾报告

日期：2026-09-03。范围：本轮未提交的微信工作流、朋友圈及配套专家资料入口；不扩展到内容生产实现。仅本地提交，不推送、不打包、不发布。

## 对照基线

- `d0a7a4a` 保存消融前源码，包含统一调度、浮窗、朋友圈修复和专家资料入口。用户在消融前已确认本机运行“验收成功”，未提供具体条数或持续时长。
- 基线 `build:test`、工作流和朋友圈控制器自检通过。消融采用逐组移除、对应检查、最后联合浮窗检查，不以删除行数代替正确性。

## 实际移除

| 组 | 删除对象 | 多余的依据 | 移除后的验证 |
|---|---|---|---|
| 1 | `App.tsx` 的 `MomentsOperations`、`ActiveTouch` 及三个仅供旧页面使用的懒加载声明 | 项目搜索只有定义、没有调用或导出；实际入口已是 `WechatWorkflowPage` | `build:test` 通过；重复静态／动态导入警告消失 |
| 2 | 旧启动／修改文案／结束／结果处理四个孤立回调，启动提示和联系人筛选计算，以及五组无展示消费者的旧 React 状态 | 原按钮已不再渲染，回调和派生计算没有消费者；旧浮窗使用自己的状态，不依赖这些状态 | 分组编译通过；保留内部验收工具仍使用的 `messageDraft` 和订阅 |
| 3 | `fillTouchTemplate`、`taskHasUnfinishedSnapshot`、`StatusCard`、旧列表上限和占位文案常量 | 仅服务已删除旧组件，全项目没有剩余引用 | 编译及版本边界检查通过 |
| 4 | `setWorkflowManaged` 方法与导出，把局部模式变量改为常量 | 无调用方；真实入口只通过构造参数设置模式 | 朋友圈控制器自检通过，既有独立／统一模式判断保留 |
| 5 | `main.cjs` 中 `runWorkflowStep` 的纯转发箭头函数 | 参数和返回值不变，目标函数不使用 `this`，直接引用即可 | 工作流自检、语法检查通过；仍需改参数顺序的 `prepareWorkflowTask` 适配保留 |
| 6 | 历史任务列表 `filter().slice().reverse()` 中的 `slice()` | `filter()` 已生成新数组，第二次复制没有作用；仍不修改源任务列表 | 编译及隔离 Electron 结果展示通过 |

类别归纳：复用 2 项（直接引用、去重复复制）、质量 3 组（旧页面、附属代码、无用模式接口）、效率 1 组（旧按钮每次渲染计算）；交叉发现合并处理，未重复计数。

## 明确保留

- 旧单项浮窗路由与 IPC 仍有调用；没有把“旧”当成“无用”。正常使用路径继续是统一浮窗。
- `moments-workflow-storage.cjs` 被发布和互动共同使用，负责路径及状态读取边界；`touch-workflow.cjs` 封装独立触达账本和执行事务，不是空壳。
- 在途记录、未知结果不重发、账号／联系人身份、真实点击、输入／发送归属和暂停保护全部保留。
- 跳过三个性能建议：账号快照缓存、无变化广播去重、进度读取参数复用。它们需要调整状态一致性或接口，不属于本轮最低风险消融。
- 没有删除配置默认值或持久化兼容字段；旧任务和用户数据仍须可读。

## 验证结果与限制

- `npm.cmd run build:test`：基线、分组和最终均通过。最终主 JS 约 401.53 kB，基线约 408.86 kB；这不是完整安装包体积对比。
- 现有工作流、朋友圈控制器、朋友圈面板、版本边界检查通过。旧页面源码断言改为现役入口、所选联系人快照、冻结进度和任务取消检查，没有移除底层授权或版本隔离检查。
- 隔离 Electron 七种状态（触达、发布、互动、完成、未完成、回复、同步）通过，窗口可见且置顶、按钮未截断，无横向溢出；已人工查看互动和完成截图。所有执行器模拟，无真实微信发送或 AI 调用。
- 证据：`desktop/.tmp/workflow-ablation-evidence-20260903/report.json` 及同目录七张图片；前一轮证据未覆盖。临时检查进程已自行退出。
- 独立 `tsc --noEmit` 在消融前即失败：缺少 React 类型声明等仓库既有问题；未增加假类型或放宽配置。项目未配置独立 lint 脚本。Vite CJS 弃用和开发 Electron CSP 警告仍在。
- 消融版未重启用户主程序、未重新实测微信、未制作安装包。手动复验：重启后加入一项小额度朋友圈互动，检查浮窗、即时点赞计数、评论输入／发送、结束状态；再检查暂停及重新打开进度。此前用户验收不自动外推到消融后的所有场景。

## 已确认并完成的临时文件清理

用户阅读本报告后回复“可以那就开始”，于 2026-09-03 完成下列 7 项清理（6 个脚本、1 个旧截图目录），合计 126,698 字节。全部移入 Windows 回收站，可从回收站恢复；不是永久删除，也不计为已释放磁盘空间。清理前核对真实路径和符号链接，清理后确认原位置不存在，09-03 原验收与消融证据、通用浮窗夹具仍保留。以下路径相对于项目根目录：

- `desktop/.tmp/check-comment-composer-20260903.cjs`：本次紧凑评论框的临时像素样例。
- `desktop/.tmp/profile-moments-entry.cjs`、`desktop/.tmp/trace-moments-open.cjs`：本次导航计时诊断，结果已收录项目状态。
- `desktop/.tmp/inspect-app-frames.cjs`、`desktop/.tmp/inspect-running-window.cjs`：针对旧进程的临时白屏诊断。
- `desktop/.tmp/workflow-floating-evidence-20260902/`：较早浮窗模拟截图，已由后续证据替代。
- `desktop/.tmp/workflow-ablation-smoke-20260903.cjs`：本次临时执行入口；通用浮窗夹具和本次报告／图片继续保留。

`release/` 只有一套现存测试安装器及清单，不是多余副本；本轮没有新安装器替代，不删除。`desktop/.build/` 的内容运行时仍用于构建，不删除。真实运行目录、联系人、密钥、素材、发送账本均不在清理范围。只登记到一个 Git 工作区，没有删除分支或工作树。

工作区另有 `docs/AI_VIDEO_MODULE_RESEARCH.md`：与微信收尾无关，本次不修改、不提交。记忆只读，不更新。
