# 项目状态

> 唯一当前能力与验证矩阵
>
> 最后更新：2026-07-22
>
> 当前目标：Windows 10/11 x64 + 个人微信 `4.1.11.54`

本文档是面向开发与验收人员的唯一当前状态说明。`desktop/release-capabilities.json` 是构建使用的机器可读清单，必须与本文一致，但不能单独证明功能可分发。历史 commit、标签、旧文档和旧 ZIP 只作为证据，不代表当前版本已验收。

## 发布结论

**当前正在生成正式命名的异机验收候选包。** 2026-07-22 构建 `20260722T0603Z` 已从全新临时目录启动，并通过便携版自动回复单消息实发验收；正式候选包仍需在本机验证主动触达，并在另一台 Windows 电脑完成自动回复与主动触达闭环后才能标记为可分发。

当前工作位于 `codex/stabilize-wechat-4.1.11.54`。正式包统一命名为 `AI获客.zip`，界面和 EXE 不再显示“测试版”；包内 manifest、源码 commit、自动检查和实机验收必须指向同一版本。

## 当前能力与验证矩阵

| 能力 | 当前实现 | 历史证据 | `4.1.11.54` 本机 live | 新便携包 | 当前结论 |
|---|---|---|---|---|---|
| 联系人同步 | 已实现；联系人清单供自动回复和主动触达只读使用 | 历史本机同步跑通 | 已验证 | 待验证 | 仅本机能力可用，不能据此放行整包 |
| 自动回复 | 修复线已实现稳定消息 occurrence、持久去重、识别波动隔离和即时状态事件 | 2026-07-14 至 2026-07-16 的旧 UIA/旧包路径曾通过本机及异机验收 | **已验证（2026-07-22）** | **已验证（2026-07-22，本机全新解压）** | 本机源码版和便携版已通过；异机仍待验证 |
| 主动触达 | 已实现任务冻结、AI/固定话术准备、共享发送事务和发送结果持久化 | 旧基线曾完成单联系人真实发送；2026-07-22 修复线完成 4 联系人连续实发 | **已验证（2026-07-22）** | **待验证** | 本机源码版已通过；新便携包与异机仍待验证 |
| 朋友圈 | 仅有测试版单帖观察、菜单检查、点赞/评论动作及去重账本 | 旧修复线上有单帖人工/自动证据 | **待重新验证** | **待验证** | 自动打开朋友圈、逐帖处理和自动下滑尚未完成 |

说明：`self-check`、renderer 构建或便携包自检通过只能证明静态契约和包结构，不得替代表中的 live 验证。

2026-07-22 自动回复本机验收记录：测试联系人 `A测试客户` 产生 3 个不同的稳定 occurrence 指纹，账本均为 `sent_verified`，没有 pending；用户确认回复行为符合预期并验收通过。随后构建 `20260722T0603Z`，从 `C:\tmp` 全新解压目录独立启动，用户再次确认便携版自动回复验收成功。

2026-07-22 主动触达本机验收记录：用户明确重新发起 4 联系人连续任务；`A测试客户`、`玺联惠客户经理炎燃19946167505`、`玺联惠清洁产品森妮19101706971`、`向锦涛` 均为 `sent_verified`，任务 `4/4` 正常完成。每次发送均记录发送前精确草稿与发送后输入框清空验证；用户确认验收成功。该结论只覆盖当前源码工作区，不自动放行尚未生成的新便携包。

## 当前修复基线

- 只正式适配微信 `4.1.11.54`；`4.1.11.24` 的历史结果不再列为当前支持承诺。
- 自动回复和主动触达复用同一发送结果语义：`not_attempted`、`sent_verified`、`outcome_unknown`。结果未知不得自动补发。
- 识别波动采用“保留待处理、隔离当前对象、继续监听”，不得把观察不确定升级为整机宕停，也不得在证据不足时发送。
- 微信窗口、DPI、坐标和识别配置归共享适配层；联系人任务、自动回复和朋友圈分别保存自己的业务状态与账本。
- DeepSeek“测试连接”必须同时跑通主动触达文案和自动回复结构化文案；简单 ping 不算能力可用。

## 实机放行顺序

1. 在新状态目录启动自动回复，确认界面明确显示“监听中”。
2. 测试联系人发送一条唯一消息；30 秒内只能回复一次，再空跑 90 秒不得出现第二条。
3. 发送第二条不同消息，仍只回复一次；重启应用后再测一条，验证 pending 不丢失、已发送不重复。
4. 主动触达先向 `A测试客户` 发送一条，再对三名明确标记的测试联系人连续执行。
5. 从同一干净 commit 生成测试 ZIP，在本机完整解压复测。
6. 在另一台电脑重新配置 Key、AI 专家资料并同步联系人，重复单消息自动回复与单联系人主动触达闭环。

任一步出现重复发送、错误联系人、发送结果未知、状态与界面不一致或需人工停止，均不得升级发布状态。

## 数据与密钥边界

- 测试版数据根目录：`%APPDATA%\xiaoxi-active-touch-test\data`。
- 交付版数据根目录：`%APPDATA%\xiaoxi-active-touch-delivery\data`。
- `contact_sync/`、`active_touch/`、`auto_reply/`、`moments/` 独立保存业务状态；`wechat_adapter/` 只保存共享适配信息。
- DeepSeek Key 使用当前 Windows 用户的 DPAPI/`safeStorage` 加密，不能随 ZIP 或源码迁移；换电脑必须重新填写。

## 洁癖清理候选（尚未删除）

以下对象只列为候选，保留现场等待用户查看本次收尾报告后再确认：

| 候选 | 原因 |
|---|---|
| `.codex-runtime-0717-3a/` | 旧会话本地运行时，需先确认不再承载唯一调试依赖 |
| `desktop/dist/`、`desktop/dist-development/`、`desktop/dist-pilot/` | renderer 生成物，可从源码重建 |
| `desktop/.build/` | 构建中间产物 |
| `启动内部开发版.cmd` | 旧启动入口；正式命令已统一到 `desktop/package.json` |
| `release/` 下 2026-07-21 的目录和 ZIP | 已知缺陷旧包，不得分发，但目前仍是复盘证据 |
| 其他构建缓存和临时生成物 | 先核实来源、可重建性及是否被 Git 跟踪 |

本轮不删除、不重命名上述对象。

## 知识收尾状态

| 事实面 | 状态 | 说明 |
|---|---|---|
| 代码 | changed-and-verified | 稳定化修复已提交，完整源码自检通过；正式候选包从干净 commit 构建 |
| 运行态 | partial | 自动回复已完成源码版与本机便携版 live 验证；主动触达源码版已验证，正式便携包与异机仍待验证；朋友圈仍待验证 |
| 文档 | changed-and-verified | 当前说明已按 2026-07-22 代码、目录和发布现实收敛 |
| 规则 | changed-and-verified | `AGENTS.md` 已移除不存在文档、错误命令和过期仅 dry-run 限制 |
| 记忆 | not-applicable | 未写入或修改平台生成记忆 |
| 工作区 | pending | 复核现场和清理候选仍保留，等待发布闭环后再报告清场 |
