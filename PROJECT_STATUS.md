# 项目状态

> 最后更新：2026-07-21

## 当前主线

仓库当前主线是“AI获客明日演示基线 + 内测朋友圈单条点赞评论”：

```text
AI获客：联系人同步 -> 自动回复 / AI 专家 / 主动触达 -> 人工交接
测试版朋友圈：只读预演 -> 菜单与评论框检查 -> 用户确认 -> 单条动作 -> 精确回读或失败锁定
```

## 已完成

- [x] Electron + React + Vite 桌面框架。
- [x] 产品品牌统一为“AI获客”，原“个微Agent”分组替换为“微信拓客”；导航保留微信拓客、内容生产、渠道运营三组，API 密钥固定在侧栏底部。
- [x] 本地登录页不再阻断客户主流程，默认进入工作台。
- [x] 微信联系人同步入口，联系人写入 `contacts.json`。
- [x] 内部测试版朋友圈点赞评论预演：兼容 `SNSWindow/sns_list` 与新版 `MMUIRenderSubWindowHW + Windows Media OCR` 两条识别路径；正常同时显示 2—3 条内容时按视口中心和稳定身份确定性选择一条，动作前以 12px 漂移容差回锁同一帖子，不再要求页面只显示一条。
- [x] 内部测试版朋友圈单条动作闭环：使用稳定帖子指纹和独立评论文案锁去重，观察锁 5 分钟有效，动作全程持有全局微信运行锁；点赞以“赞 → 取消/取消赞”自动复核，评论只点击唯一精确“发送”按钮，再以唯一新评论、直接归属的稳定右键菜单、精确“复制”和 Unicode 逐字一致回读验收。任何身份变化、重复尝试或结果未知都立即阻断且不自动重试。测试号点赞已自动验收，评论已人工视觉验收。
- [x] 朋友圈动作执行器、驱动、IPC 与操作界面仅进入测试版；动作 self-check 不进入便携包，交付版发布自检同时扫描源码和 JS/CSS 标记，防止动作能力泄漏。
- [x] 自动回复 v2 极简主页：运行状态、今日已回复、启动/暂停和异常提示；启动不再接收白名单、话术或时间配置，重启强制暂停。
- [x] 全部唯一已同步私聊纳入扫描，继续排除群聊、系统账号、文件传输助手、同名身份、自己消息和风险内容。
- [x] 最近 12 条/最多两屏上下文：UIAutomation 读取正文和顺序，内存截图只检测左右头像；遮挡、方向不明、滚动恢复失败或会话变化时 fail-closed。
- [x] AI 专家支持 `.txt/.md/.docx` 一份全局话术文件，5MB/5 万字符限制、Mammoth 纯文本提取和运行目录原子写入。
- [x] DeepSeek 返回回复、意向和人工交接结构；结构化输出或临时服务异常时改走保守确认与人工提醒并继续监听，Key、余额或持久配置错误在处理当前来信并提醒人工后暂停；客户回复验证成功后独立定位文件传输助手提醒，提醒失败即暂停。
- [x] 去重、自我循环检测和异常熔断保留；正常单联系人连续对话不触发全局暂停，全局 10 分钟 30 次才暂停。
- [x] 主动触达极简页面：话术输入、触达人数、状态卡、联系人预览。
- [x] `{称呼}` 模板变量，优先备注，其次昵称/名称。
- [x] 右下角 `启动程序` 创建任务并隐藏主窗口。
- [x] 右侧悬浮窗展示当前联系人、下一位、进度、暂停原因和操作按钮。
- [x] 任务进度持久化，暂停后可继续。
- [x] 微信窗口驱动只绑定个人微信主进程 `Weixin`、`WeChat`。
- [x] V1 只写草稿并做预检，不自动真实发送。
- [x] 测试版单联系人真实发送已完成账号、窗口、会话、幂等和结果气泡校验，并由用户使用测试微信人工验收通过。
- [x] 交付版复用同一单联系人发送事务，接入 v3 冻结快照、50 人批次授权、8—15 秒间隔和崩溃恢复对账。
- [x] 联系人预览取消前 8 人截断，显示同步全集或任务冻结全集、排除原因、所属批次、完整文案和结果状态。
- [x] 自有 `xiaoxi-contact-helper.exe`、已授权 `wx_key.dll` 与 Electron runtime 一同生成便携目录和 ZIP；目标电脑不依赖 Node、Python、Codex 或安装 `dt-ai-helper`。
- [x] 测试版与交付版物理隔离；交付版不包含单联系人开发验收入口。
- [x] 用户在侧栏底部 API 密钥页填写 DeepSeek API Key；Key 只由主进程使用，并以 Windows DPAPI 加密保存。
- [x] 本机自动检查覆盖测试/交付 renderer、全部 self-check、Helper 自检和便携包隐私扫描。
- [x] 账号管理改为抖音、小红书、快手、视频号入口卡片，本阶段统一标注“下一阶段”。
- [x] 主动触达支持搜索联系人、移出/恢复本次触达名单，一次用户点击授权整次冻结名单。
- [x] 暂停、退出和异常重启后可继续冻结任务；发送结果未知时最多自动补发一次并持久化次数。
- [x] 2026-07-14 本机自动回复实测通过：普通回复、历史追问、短时间连续对话、意向/未知提醒、人工抢先回复以及群聊/文件传输助手排除。
- [x] 2026-07-15 多联系人实机验收通过：不同一对一联系人来信均能被监听并分别完成自动回复，确认运行范围不绑定单个测试微信。
- [x] 2026-07-16 最新测试包异机终验通过：在另一台干净电脑重新配置 Key、导入话术并同步联系人后，多联系人、上下文回复、意向提醒及提醒后继续监听均通过；自动回复与 AI 专家进入稳定基线。
- [x] 2026-07-17 新版微信 `MMUIRenderSubWindowHW` 视觉链路实机验收：测试号单帖只发送一次 `自动化测试，请忽略（0717-2）` 并显示在原帖评论区，只点赞一次并显示本人点赞记录；随后只读重开菜单确认状态为“取消”。评论的系统 OCR 后验仍保守记录为结果未知，动作日志已锁定且未补发；人工画面已确认真实成功。
- [x] 朋友圈评论新增独立于视觉帖子指纹的精确文案尝试锁：`prepared/clicked/verified/outcome_unknown` 均在调用微信驱动前阻断相同文案，避免互动区变化造成指纹漂移后重复发送。
- [x] 朋友圈视觉帖子身份改为仅使用互动菜单行及其上方 OCR 文本；点赞人和评论区仍绑定本次观察快照，但不再改变持久帖子指纹。视觉观察升级为 v5，并用静态头像哈希替代跨时间的整帖画面哈希；旧观察自动失效。
- [x] 2026-07-17 测试号“已点赞”路径实测通过：互动菜单识别为“取消”后动作直接记录 `verified + no_op`，`real_action_attempted=false`，没有再次点击点赞。
- [x] 2026-07-17 测试号相同评论文案重复执行实测通过：`自动化测试，请忽略（0717-2）` 在发送驱动前被 `moments_comment_text_already_attempted` 阻断；原 `outcome_unknown` 尝试保持不变，没有新增候选尝试，也没有再次评论。阻断摘要已复验为原尝试 key 与独立候选 key，不再残留上一条点赞 key。
- [x] 旧版朋友圈评论发送后验曾要求右键菜单与剪贴板逐字回读，实机证明该链路会把已经显示在原帖的评论保守记成 `outcome_unknown`；历史尝试均已锁定且未补发。
- [x] 2026-07-20 测试号真实点赞自动验收通过：仅点击一次，attempt 为 `verified`、`real_action_attempted=true`、`no_op=false`，菜单由“赞”变为“取消”。
- [x] 2026-07-20 评论 `自动化测试，请忽略（0720-2）` 仅发送一次并在原帖视觉确认；旧自动回读因 `moments_comment_readback_popup_not_owned` 保守记为 `outcome_unknown`，文案已锁定且未重试。
- [x] 2026-07-20 评论 `回读验收QK7R` 仅发送一次并在原帖视觉确认；自动回读已证明 PID、class、title 与 direct owner 正确，但旧“菜单必须完全位于主窗内”规则返回 `moments_comment_readback_popup_outside_window`，因此仍保守记为 `outcome_unknown`，未重试。
- [x] 右键复制回读保留为可选增强验收：成功时升级为 `clipboard_exact`，失败不再推翻已经成立的原帖可见证据。
- [x] 2026-07-20 完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 通过；v2 几何用例覆盖真实 22×35px owner 越界、虚拟屏幕 3px 容差/4px 拒绝、远离锚点、锚点越出锁窗及首帧瞬态后双帧稳定。
- [x] 2026-07-21 评论默认后验改为 `visible_exact`：原帖新增唯一精确评论候选、连续帧稳定、帖子锚点和菜单稳定即可记为 `verified`；没有证据、候选歧义或帖子变化仍记为结果未知，且不自动补发。
- [x] 2026-07-21 联系人同步新增 Windows 注册表与更多常见目录兜底；微信关闭且安装在自定义目录时，也能找到 `Weixin.exe` 并直接进入重启捕获流程。
- [x] 2026-07-21 产品升级为 `AI获客 0.2.0`，测试版界面和版本清单显示唯一 buildId，并在便携包内提供新电脑首次使用说明。

## 当前边界

- 两个版本都使用同一真实发送事务；测试版额外保留单联系人验收入口。
- 一次启动授权整次冻结名单；50 人分段仅用于内部 AI 生成和进度控制。
- 当前没有远程停用、签名、安装器或自动更新。
- 不做腾讯云 SCF、PostgreSQL、服务端网关、许可证和服务端额度系统；这些方案延期。
- 不做多账号并发。
- 自动回复只处理能安全识别角色和会话的一对一文字消息；不处理群聊、图片、语音、文件或系统消息。朋友圈真实动作仅限内部测试版、用户明确授权的单条测试内容，交付版不包含该能力。
- 不把微信/RPA 操作放进 renderer，仍通过主进程 IPC 调本地执行器。

## 下一步

1. 从当前干净源码生成唯一 `AI获客-测试版.zip`，完成本机包内启动与单条朋友圈实机验收；再在一台干净电脑按首次使用说明完成联系人同步、Key、AI 专家、自动回复和主动触达演示准备。朋友圈发布仍留在下一阶段。
