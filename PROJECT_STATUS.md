# 项目状态

> 唯一当前能力与验证矩阵
>
> 最后更新：2026-09-10
>
> 当前目标：Windows 10/11 x64 + 个人微信 `4.1.11.55`

本文档是面向开发与验收人员的唯一当前状态说明。`desktop/release-capabilities.json` 是构建使用的机器可读清单，必须与本文一致，但不能单独证明功能可分发。历史 commit、标签、旧文档和旧 ZIP 只作为证据，不代表当前版本已验收。

## 发布结论

### 2026-09-10 1.1.16 启动前未读消息修复（用户确认本轮复测通过）

- 用户对上一轮复测确认：自动回复启动后新发消息能够抓取并回复；剩余问题是启动前仍有红点的消息被跳过。原因是视觉驱动将启动时已有未读预览写入 `startupUnreadBoundaries`，初次确认和后续扫描均主动排除这些会话，同时可能将同批未读问题作为历史上下文裁掉。
- 删除“启动前未读等于历史”的屏蔽状态和传参。仍有未读标记且无草稿的会话进入正常候选流程；这些会话不再建立历史消息裁剪边界。已读当前聊天的启动基线、严格接待名单、已处理消息身份和结果不明保护保持原逻辑。
- 定向回归覆盖启动前未读入队、已读/草稿排除及连续未读上下文。交付本版后，用户反馈“可以，没问题了”，记录为启动前已有未读消息抓取与回复的本轮实机复测通过；暂停再启动、退出重开后的防重复行为未获逐项实测确认，保留在 [复测记录](docs/testing/wechat-1.1.16.md)。本轮不涉及后台部署或其他业务模块。
- `1.1.16 / 20260910T0715Z` 由干净提交 `89fa736435bf6cc89723c006c9fadec587a2a3d4` 构建。完整源码自检、详情图本地 E2E、界面、固定运行时、便携版与安装器构建通过；包内版本、自动回复模块和公告与源码逐字节一致。安装器 527293780 字节，SHA-256 `b8603357c15f3b2997c321bec10add39adb0f0505f3f99694a49a18ff7e5c285`，与清单相符；旧候选备份保留。未安装本机或执行真实微信操作，未推送或发布更新频道。

### 2026-09-10 1.1.15 精准触达与自动回复修复（测试候选，实机待验收）

- 对应吐槽 `85ddd06e-d30b-4e63-ba59-902c114b4c05`，来源 `1.1.14 / 20260910T0531Z`。13:55 精准触达首个联系人在搜索前因剪贴板格式备份失败停止，未尝试发送；任务为待处理后调度继续其他任务，并非触达次数限制。14:02 自动回复进入聊天后标题识别不确定，尚未进入生成回复。
- 共享剪贴板备份增加 OLE 原生格式字节读取，覆盖 Windows 能提供但 WinForms 无法解码的注册格式；保持所有数据先物化、再覆盖、按所有权恢复的路径。内存原生格式夹具在修复前复现同错误，修复后保留字节；文字、图片、文件及不可读取格式回归通过。故障机具体格式尚不明确，不能将兼容修复写成实机通过。
- 自动回复标题不确定时，复用共享 OCR 做同一帧的一次局部放大识别，恢复裁剪坐标并沿用原精确名单判断；读取、二次确认和发送前校验共用。明确不同联系人不放行，未识别清楚不再在后续校验中误报为切错会话。补齐匿名标题状态、候选数与恢复结果的客户端及维护服务字段，未上传标题文字。
- 定向剪贴板、自动回复视觉、控制器、吐槽序列化和维护服务检查通过。用户随后确认自动回复运行后能抓取并回复新消息；启动前未读遗漏由 1.1.16 修复并获本轮确认。精准触达原生剪贴板兼容仍待 [复测](docs/testing/wechat-1.1.15.md)；维护服务字段部署待单独授权，未推送或发布更新频道。
- 测试包 `1.1.15 / 20260910T0626Z` 来自干净提交 `176a37cd9b91ac33ab775319af7a0ea5c33afc30`；完整源码自检、详情图本地 E2E、界面与固定运行时、便携版和安装器构建通过。包内六个关键文件与源码逐字节一致；安装器 527310104 字节，SHA-256 `0f34da159fae7b0718cdd4fd409723f108c32cc10c9f10a09a2a9402ea3cf420`，与清单相符。旧候选自动备份保留；未安装本机、未执行真实微信收发，后台字段仍未部署。

### 2026-09-10 1.1.14 朋友圈像素扫描优化（用户确认故障机互动通过）

- 故障机 1.1.13 / 20260910T0359Z 的 13:11 互动仍失败。原始本地耗时记录显示，30 秒是整个 PowerShell 识别的限时：进入 `second_candidates` 前已消耗 23718ms，其中首次页面 8476ms、首次候选 8856ms、第二次页面 2285ms；不能把最后阶段等同于完整 30 秒卡点。旧版缓存推测不成立，新字段已在故障机产生。
- 本机对用户同一截图的函数测量确认绿色区域、头像和菜单的 PowerShell 像素循环是热点。将这些循环移入现有 C# 辅助类，采样位置、颜色阈值、连通性与候选校验不变；总超时不延长。首次页面识别从 1212ms 降至 430ms，首次候选从 1019ms 降至 161ms；完整页面、两轮候选、OCR、坐标与指纹的离线结果一致。这不是故障机性能或真实动作验收。
- 上一轮遗漏了维护服务端字段白名单，导致吐槽中心过滤新增耗时、扫描字段及发布核验原因。本地修复通过 `validate_feedback` 路径和现有服务测试，尚未部署；失败时暂用 [复测说明](docs/testing/wechat-1.1.14.md) 中的本地日志工具。服务端上线与客户端候选包是独立状态。
- `1.1.14 / 20260910T0531Z` 从干净提交 `4ebe39db7dc171259b8866aeea2b15419ba870b0` 构建；原图一致性、菜单几何及视觉边界检查、完整源码自检、详情图本地 E2E、界面与固定运行时、整包及安装器检查通过。包内六个关键文件与源码逐字节一致；安装器 527350864 字节，SHA-256 为 `6dcb04be81cdb45abe8d39976fd7eddd8d794119d2465f9dc710001e91ab999a`，与版本清单一致。
- 用户在本版朋友圈互动复测后反馈“已经成功了，没问题”，记录为故障机本轮互动复测通过。未提供新的阶段耗时或逐项动作日志，因此不补写实测速度或单项动作次数；自动回复和发布核验的未完成事项继续保留。
- 本轮 agent 未安装本机、未执行真实微信点赞、评论、发送或发布，未推送源码或发布更新频道。

### 2026-09-10 1.1.13 读取提速与诊断测试候选（实机待复验）

- 故障机 1.1.12 的自动回复记录显示前台截图识别到对方消息，但返回 `no_unread_message`；用户确认是在界面显示启动后发来。已修正初始化尚未完成时误报监听的状态，并复用同一已确认窗口成功的前台截图方式；本次漏读的完整根因仍未实机确认。
- 朋友圈发布已由用户确认为成功并标记 `resolved_published`；失败发生在点击后的两次核验，共 19936ms，最后原因为 `moments_publish_visible_anchor_missing`。用户稍后截图可通过本机完整识别核验，不能据此判断现场是加载延迟还是 OCR 漏读，也不把已发内容再次发布。
- 互动在 `second_candidates` 触发 30000ms 超时，尚未进入点赞或评论。连续两帧整个内容区域像素和边界均一致时复用头像及菜单位置，文字仍重新读取；截图离线第二次读取由 977ms 降至 183ms，结果相同，改变像素后会重新识别。故障电脑完整互动仍待复验。
- 吐槽中心保留自动回复扫描、互动分阶段耗时与发布核验阶段耗时、正文标识存在性等技术字段，不上传正文；界面明确区分发布结果不明与未尝试发布。
- `1.1.13 / 20260910T0359Z` 从干净源码提交 `513b8dd5f3b2a1964a50522488a705dc3cefb6c3` 构建；完整源码自检、详情图本地 E2E、界面和固定运行时核验、整包及安装器检查通过。包内 10 个关键修复模块及公告与源码逐字节一致，安装器 527335226 字节，SHA-256 为 `e8fda67ac51e097534c545ca10acdfa132cbd31c78e228d76cb7179b5a72cf8e`，与版本清单一致。
- 测试步骤见 [1.1.13 分项复测](docs/testing/wechat-1.1.13.md)。本轮只生成手动安装候选，未安装开发机、未执行真实微信动作、未发布更新频道；故障机三项业务仍待用户分开复验。

### 2026-09-10 1.1.12 入口恢复与剪贴板修复（用户确认本轮触达复验跑通）

- 1.1.11 故障机日志分别确认最小化时主窗口恢复入口未覆盖，以及搜索前剪贴板快照失败；用户随后提供文字发送及气泡校验成功截图。不能据此宣称图片、网址或朋友圈均通过。
- 最小化的唯一合格 Qt 候选允许进入现有启动恢复流程，恢复后仍重新确认同进程主窗口，保留空闲、只读不激活和歧义拒绝。隔离回归已复现旧失败并验证修复后通过。
- 搜索、输入共用剪贴板快照：预先复制图片、文件列表、流等支持的数据，保留原生格式；区分特殊格式与读取失败。保留前台、输入状态及当前内容归属核对，避免覆盖用户新复制内容。本机真实剪贴板文字、图片、文件和空内容往返检查通过，原内容已恢复；现有触达和任务回归、界面构建通过。
- `1.1.12 / 20260910T0238Z` 从干净提交 `014c448f392016e26b9ee75ca20bc2c139e827e6` 构建；完整源码自检、详情图本地 E2E、运行时核验、界面、整包及安装器检查通过，最终摘要与清单一致，包内修复模块与源码一致。安装器 SHA-256：`e026c845b8c01f3517aa8a8410676cec5eed2b1247d3a596cb4234818fc64342`。
- 交付 1.1.12 并要求分别测试最小化启动触达、复制图片后文字触达后，用户回复“ok跑通了”，记录为本轮触达复验通过的用户确认；尚无逐项新日志或原剪贴板恢复截图，不扩大为图片/网址发送、自动回复或朋友圈完整验收。
- 本轮未安装开发机、未由 agent 执行真实微信操作，未推送或发布更新频道。按先试跑再写规则的约定，已在当前仓库 AGENTS.md 补充按阶段诊断、检查共用链路和覆盖实际故障条件的要求；未修改全局规则或已生成安装包。

### 2026-09-10 1.1.11 微信兼容测试候选（安装包通过，业务待验收）

- 统一朋友圈空 UIA 树时的真实 Win32 客户区识别，并保留窗口身份及页面、动作前核对；精准触达不再把不同驱动错误统一为找不到窗口。
- 故障电脑微信 4.1.13.65 的两次只读采集均成功识别客户区；打开朋友圈后页面及相机入口检测通过。此证据不代表实际发送、发布或互动通过。
- `1.1.11 / 20260910T0205Z` 从干净提交 `dd35b61a3fb6d2afdc330a6599448595864b11c9` 构建；完整源码自检、详情图本地 E2E、测试界面、整包与安装器检查全部通过，最终安装器摘要与清单一致，新共享识别模块包内字节与源码一致。尚未安装本机、未推送或发布更新频道。详见 [兼容链路记录](docs/reports/2026-09-10-wechat-compatibility-chain.md)。

### 2026-09-09 1.1.10 微信空控件树修复（本机已安装，故障电脑待复验）

- 故障电脑只读采集确认微信 `4.1.13.65` 的可见主窗口是 `Qt51514QWindowIcon`，UIA 仅返回 Win32 外壳、子控件数 0；用户完整截图确认导航栏存在。这排除了仅因固定控件名称变化的解释，但不能断言所有该版本微信均不提供 UIA。
- 主窗口识别增加独立的视觉确认：同一微信 Qt 顶层窗口的通讯录与底部菜单图标必须同时匹配；保留旧渲染/UIA 路径、窗口身份、多窗口歧义和前台遮挡检查。截图只在内存中使用导航条，诊断不保存聊天像素。
- 实际导航像素的多 DPI 回放、缺失任一图标的拒绝、窗口选择和自动回复预检回归通过；屏幕外临时 WinForms 窗口的真实 PrintWindow 路径通过。本机当前未运行微信，故障电脑真实验收仍待进行。详见 [修复说明](docs/reports/2026-09-09-wechat-empty-uia-repair.md)。
- 最终 `1.1.10 / 20260909T1347Z` 从干净提交 `ae4e651fcb1e51a9c20912f00ed59952e5d9c557` 构建；完整源码自检、详情图 E2E、便携包和安装器检查通过。本机覆盖安装退出码 0，程序及组件摘要吻合，16 个任务、9 个归档任务和授权保留，自动回复暂停。实际已安装代码的原生取图验证通过，可见应用窗口的构建编号核对通过，没有调试端口。未推送源码或发布更新频道。

### 2026-09-09 1.1.9 本机安装验证完成（未发布更新频道）

- 最终安装版为 `1.1.9 / 20260909T1302Z`，源码提交 `83bd835db27b8745840afa33978fa450920b80d6`，从干净工作区构建。本机覆盖安装、程序与组件摘要核对通过；16 个任务、9 个归档任务和授权文件保留，自动回复暂停。客户公告已内置，未推送源码或发布更新频道。
- 最终已安装组件实际上传离线抠图、浏览器导出通过；真实 Electron 界面在 1440 × 900、1100 × 760 下的三种布局均通过检查，窄窗口选择模板不再被长预览推至底部。证据见 [1.1.9 验收记录](docs/reports/2026-09-09-internal-release-1.1.9.md)。
- 恢复维护后台只读连接后，已取得故障电脑 1.1.8 / 20260909T1054Z 的最新反馈：唯一 Qt51514QWindowIcon 候选缺少主窗口特征，恢复后仍失败，发送未尝试。故障仍未闭环；只读窗口采集工具用于补齐导航控件与窗口结构证据，不执行激活、输入或发送。
- 四个已获确认的旧发布副本已删除，逻辑文件量 4.290 GiB；近期回滚、当前安装包及两份用户数据备份保留。用户报告使用微软电脑管家清理期间，发现三个构建 EXE 缺失，已从可核对来源恢复；具体删除来源未独立确认，未改动安全设置。

### 2026-09-09 详情图与构建空间修复（早期隔离验证，最终结果见上）

- 产品图上传恢复包内离线抠图，包含固定摘要的 ISNet 模型与 ONNX Runtime；失败保留原图并明确提示。工作台改为编辑、模板、大图三个布局，减少顶部重复信息。
- 界面构建、相关 Python 回归、微信窗口计数定向检查及构建保留策略检查通过。隔离组件实际上传抠图与 PNG 导出通过；完整安装包、故障电脑和真实微信未验收。本轮未提交、安装或发布。
- 构建脚本清理本次临时目录；新登记的回滚产物、组件及运行时缓存按代数保留，共用文件仍被引用时不删除。历史备份、用户数据和素材不自动接管，清理需按候选确认。
- 微信窗口枚举诊断计数改为每次采样重置，防止累加或残留误导判断；这不等于已修复故障电脑的主窗口识别问题，仍需取得该电脑 1.1.8 新版失败日志。
- 详情与历史空间清单见 [本轮修复记录](docs/reports/2026-09-09-detail-storage-repair.md)。

### 2026-09-09 1.1.8 故障电脑隔离候选（源码验证通过，尚未发布）

- 候选包含个人微信主窗口的受限恢复：仅当检测到同一微信进程中唯一、可验证的独立 Qt 窗口，且用户空闲时才请求微信恢复主窗口；恢复后必须重新识别到同一 PID 的唯一严格主窗口，不能把独立窗口作为操作目标。
- 统一任务队列会保存精准触达的安全间隔，在等待中继续可执行的自动接待检查；主页面和悬浮进度窗会显示具体任务、进度及倒计时。启动前先完成计划预检，避免失败时隐藏唯一的错误页面；有限的窗口失败原因会贯通任务诊断。
- 本地定向检查、完整 `check:self`、测试界面构建、维护服务 3 项回归和界面静态检查均已通过。尚未生成候选安装包、部署维护服务或推送更新频道；故障电脑和微信 `4.1.11.55` 的实际验收仍是下一道关口。
- 1.1.8 仅准备作为隔离测试版，正式客户频道保持关闭；未知发送、发布或互动结果不自动补发。

### 2026-09-09 1.1.7 内部完整包与增量更新已发布

- 发布包提交 `7410bd6fed97e1576ef67416554b3ef0f2515d4e`，构建号 `20260909T0821Z`；[Windows CI 34328332385](https://github.com/survivor2829/AIsell/actions/runs/34328332385) 全部通过。完整发布入口的源码、详情图本地端到端、界面、运行时和安装器解包校验通过。
- 本机从 1.1.6 覆盖安装 1.1.7，安装源码树与 EXE 均与候选包一致；完整用户数据已备份，任务、接待名单、发布状态文件摘要保持一致，16 项成功任务和 9 项归档记录保留。原授权正常登录，删除确认与取消、包内公告可读验证通过；验证后正常退出，任务暂停且自动回复关闭。
- 完整包与增量频道分别发布并读回签名清单，版本、摘要及大小一致；增量底座仍为 1.1.6，与既有 1.1.6 指纹相同。维护服务同步部署，旧代码及数据库备份保留，公网 TLS、阶段诊断上报及隐私字段过滤通过。详见 [1.1.7 验收记录](docs/reports/2026-09-09-internal-release-1.1.7.md)。
- 仅内部测试频道。下述源码真实微信验收与安装版启动/界面/数据验收分别成立；本轮安装版没有重复发布、点赞、评论或发送消息。原故障电脑、微信 4.1.11.55 和跨电脑性能仍待实际复验。

以下条目保留各实施阶段当时的状态；当前发布结论以上节为准。

### 2026-09-09 1.1.7 发布前审查记录

- 已完成正确性、安全、对抗场景、验证覆盖及仓库约定审查。修复滚动过头后可能接续同作者相似动态的问题：导航独立比对滚动前后画面行，生成实际位移回执，目标头像与卡片须同距移动。无可靠画面重叠则暂停，不继续互动。
- 本机微信 4.1.13.65 只读滚动验收：滚轮 300 刻度实际移动 188 像素，原头像由 y=164 移到 y=352，目标重新定位成功；原目标滚出画面的路径拒绝继续。任务删除检查追加重启后元数据归档保留，诊断检查追加原始阶段日志解析与范围过滤。
- 业务版本升级为 1.1.7，底座沿用 1.1.6；客户公告内置并同步发布文档。该审查完成后的构建、安装及云端结果见上节。

### 2026-09-09 计划任务记录删除（源码构建通过，未发布）

- 今日计划、精准触达和朋友圈运营增加单项删除，以及当前页面失败／已取消／已错过任务的批量清理；确认后移除列表记录，批量清理保留成功与待执行任务。运行中禁止删除，删除每日任务同时停止后续安排。
- 主进程原子归档任务元数据，保留任务资料、执行回执、防重复发送记录和接待客户。定向检查验证批量状态变化时整体拒绝、成功记录保留、重启不恢复已删除项及执行数据保留；`build:test` 通过。源码 Electron 已加载，单项／批量确认与取消界面检查通过，用户现有 25 项任务未删除；未打包或上传云端。

### 2026-09-09 测试机任务调度与朋友圈修复（源码实测通过，未发布）

- 对应 1.1.6 新反馈，已将计划改为当前可执行任务优先、空闲后接待，去除集成式朋友圈视觉超时后的重复探测，并减少导航进程查询和头像扫描的解释执行开销。固定阶段诊断贯通客户端和服务端源码。调度、朋友圈导航/视觉、dry-run CLI、本机诊断接收及界面构建通过；同图头像扫描新旧结果一致，单次样本由 4,860 ms 降至 437 ms。
- 测试机约 7.8 秒的脚本启动/公共初始化耗时尚未证明消除，原机导航与完整操作仍待复验；未生成新安装包、部署或上传云端，已发布版本仍为下述 1.1.6。详见 [修复与复测步骤](docs/reports/2026-09-09-workflow-performance-repair.md)。
- 按用户明确确认清理 41 项旧制品，可读取文件逻辑大小约 62.69 → 30.13 GiB；当前程序、最近回滚包和升级前用户数据备份保留。详见 [目录清理记录](docs/reports/2026-09-09-storage-cleanup.md)。
- 14:32–14:35 本机复测后追加：发布确认增加同一朋友圈区域的局部/放大 OCR 精确匹配；评论前为底部菜单留出空间，修正滚轮刻度误当像素的目标重定位，并保留其他完整动态旁的正文候选。发布、互动、视觉和任务控制离线回归通过；本轮未执行真实发布/点赞/评论，发布漏识别的现场原因尚缺发布瞬间图像佐证，须再次复测，未上传云端。
- 14:50–14:52 日志与进程核对确认仍运行旧安装测试版，未加载以上源码。已修正源码开发版单实例冲突时静默返回的问题，改为明确提示退出旧测试版，并补评论前下一条动态边界参数和具体失败原因；定向离线检查通过。复测必须先完全退出旧测试版，不能把旧窗口被唤起当作新源码已启动。
- 后续用户授权真实操作：源码运行复现同一正文的 OCR 错字，改为同一正文区域多视图逐段精确匹配；新文案单图发布于 15:27 自动确认 `verified`，不再依赖人工核实。发布前后共用排重规则，仍要求唯一新动态、媒体证据和时间行。首次复现帖经实际核实后人工确认，未重复发表同内容。
- 连续互动实测进一步定位到头像裁切一像素偏差、评论蓝字被误选为正文头像；分别用内部区域精确裁切锚点辅助滚动重定位、实际头像列筛选正文候选。15:41 最终任务 `completed`，本轮新增完成 2 条、评论 2 条、新增点赞 1 条、已有点赞 2 条；累计检查完成 3 条包含一条既有完成动态，该条未重复评论。底部菜单调整和正文到完整动态接续均实际通过。
- 本轮为本机微信 `4.1.13.65` 的源码实测：共新增 2 条测试朋友圈、分次新增 3 条评论和 2 次点赞，测试内容保留，结束后程序暂停且自动回复关闭。相关构建、定向回归和实际截图复验通过；未打包、提交、上传云端或替换已发布版本，目标微信 `4.1.11.55` 及其他电脑仍待复验。证据见上述报告和 `outputs/moments-runtime-20260909/acceptance.json`。

### 2026-09-09 内测 1.1.6 完整包发布与本机覆盖验收完成

- 完整包测试频道已发布 **1.1.6**，公开签名、大小和 SHA-256 读回一致；旧组件频道仍为 `1.1.5`，本次底座变化通过完整包升级。安装包提交为 `c47217ce0a0e6f028cad3ffd5f2b2080f67be976`、构建号 `20260909T0326Z`，对应 [Windows CI](https://github.com/survivor2829/AIsell/actions/runs/34306966856) 全部成功。完整发布检查、便携包和安装包内容校验通过。
- 本机已覆盖安装 1.1.6，原授权可登录，原有 13 个任务、35 条朋友圈发布尝试记录及自动回复计数保留；安装后的源码目录和 EXE 与候选包一致。安装版真实单联系人任务完成 `1/1`，文字、单图、网址分别为 `sent_verified`，顺序正确，网址在微信浏览器实际打开。首次在朋友圈页面启动时，文字输入前停止且没有发送；切回聊天页继续原任务后成功。**发送前需切回聊天页，该页面切换问题尚未修复。**
- 维护服务同提交代码已部署，旧代码和数据库已备份；公开 TLS 隔离诊断上报、回查和窗口标题脱敏通过。仅验证技术测试记录，未替用户回复或关闭真实反馈。发布与异机复验见 [1.1.6 验收报告](docs/reports/2026-09-09-internal-release-1.1.6.md)。
- 发布链路检查复现：兼容底座下，覆盖安装 `1.1.6` 后仍会优先加载已选中的 `1.1.5` 组件。启动器现以新整包取代较旧或同版的组件选择，保留更高版本组件；仅重置选择指针，不删除旧组件和业务数据。底座版本递增为 `1.1.6`，本次走完整包。
- 本机微信 `4.1.13.65` 收到 A测试客户新消息后自动生成并发送一条回复，发送回执 `sent_verified`，微信聊天窗口可见；验证后暂停并关闭持续接待。
- 一次性单条朋友圈互动完成 `1/1`，在本账号刚发表的测试帖实际点赞一次并评论一次，微信现场与任务记录一致；没有设置每日重复任务。
- 朋友圈单图加正文已实际发表，但自动确认返回 `moments_publish_visible_anchor_missing`、结果为 `outcome_unknown`。通过微信现场核实后，使用应用“已核实，已经发布”完成记录，没有重复发布。此项只算实际发布和人工核实闭环通过，不能写成自动确认已修复；已在 1.1.6 客户公告保留限制。
- 自动回复和朋友圈的上述补充实测来自源码运行态，安装版本轮复跑了文字/单图/网址链路，不能混作安装版全部场景验收。其他电脑、目标微信 `4.1.11.55`、多联系人和多图组合仍待复验。证据位于忽略目录 `outputs/wechat-feedback-20260909/`。

以下同日“未发布”条目保留各实施阶段当时的状态；当前部署与发布结论以上节为准。

### 2026-09-09 文字、图片、网址真实微信流程通过（源码测试版，未发布）

- 用户授权后，通过电脑插件操作真实应用，只向“A测试客户”执行一项任务，自动回复关闭。本机微信可执行文件版本为 `4.1.13.65`、DPI 120；窗口准备及精确联系人校验通过。聊天中依次看到一条测试文字、一张图片、一条独立网址；点击网址后，微信浏览器实际打开 Example Domain。
- 任务最终进度 `1/1`，文字、图片、网址均为 `sent_verified`。过程中多次中断、重启并继续，只补做尚未发送部分，已发送文字未重复。未验证收件人设备的读取状态、多个联系人批量发送、真实多图连续发送或其他电脑。
- 实跑修复了分段目录读取任务上下文错误、系统 `Get-FileHash` 不可用、图片透明通道与剪贴板像素转换不一致，以及会话截图区域包含消息内容而随图片草稿高度变化的问题。保留任务身份、图片像素和发送结果核验；两处会话校验共用按 DPI 缩放的标题区域边界。
- 定向 CLI/工作流与主动触达回归通过，图片摘要离线验证通过；先前界面构建及隔离 Electron 检查通过。本次后续修改位于主进程/RPA，最终真实运行已通过。尚未提交、打包、部署后台或分发客户端；原故障电脑仍需独立复验。证据见[实测报告](docs/reports/2026-09-09-wechat-feedback-diagnosis.md)。

### 2026-09-09 共用微信窗口识别与准备超时修复（本地验证通过，未部署发布）

- 用户确认实施后，修正“已知主窗口仍因缺少固定渲染子窗口被丢弃”的缺陷。保留旧渲染类兼容，新增受主窗口边界约束的渲染类族识别；已知原生主窗口类可独立提供证据，无渲染子窗口的 Qt 窗口须额外确认左侧聊天/通讯录导航控件。归属、工具窗口、布局和唯一候选检查继续保留，多个主窗口不按面积择一。
- 窗口准备改为一次读取微信进程，再由 Win32 枚举过滤所属 HWND；移除准备前不必要的同步安装路径发现。完整准备采用 20 秒有界预算，保留子进程退出确认与终止未确认时的隔离；启动、编译、进程、枚举、恢复和前台切换均有分段诊断，并记录候选/隐藏/最小化/布局拒绝数量。
- 窗口选择缺陷已先复现再验证修复，主动触达、自动回复适配、朋友圈导航、诊断脱敏及后台本地接收检查通过，界面构建通过。完整 PowerShell 语法、原生定义与 UIA 条件构造通过；本机只读检查无微信进程，最终样本准备脚本约 460 ms，不能据此宣称原故障电脑已恢复。
- 后台新增诊断字段接收规则已在本地完成，**尚未部署**；应先部署接收规则，再以递增版本分发客户端，避免新日志被旧白名单过滤。窗口修复初验阶段未执行真实发送，后续授权实测见上节；未提交、打包、安装或发布。原电脑复验与发布顺序见[排查与修复报告](docs/reports/2026-09-09-wechat-feedback-diagnosis.md)。

### 2026-09-09 跨电脑故障排查与触达图片、网址（本地实现，未发布）

- 只读核对后台 12 条反馈和最近 126 份诊断，当前 1.1.5 已确认窗口准备超时、主窗口识别失败两个分支；一份反馈的朋友圈故障实际发生在 1.0.1，尚无升级后的操作复验。三项微信业务共用窗口准备器，固定子窗口类名及准备开销是优先排查方向，原故障电脑的实际窗口结构和超时内部分段仍待取证。首次排查仅提供诊断与建议；随后获得修复授权的实施结果见上节。未部署或处理真实反馈，详见[排查报告](docs/reports/2026-09-09-wechat-feedback-diagnosis.md)。
- 主动触达新增可选图片与对应网址，顺序固定为话术、逐张图片、独立网址消息。支持最多 9 张 PNG/JPG，单张不超过 20 MB；图片由主进程冻结保存，界面支持预览、排序、移除与任务重开。文字、每张图片和网址分别持久化结果，已确认部分不重发，未知结果停止；尚未发送的部分可按界面提示继续。
- 已通过界面构建、相关工作流与发送回归、图片 PowerShell 语法及原生类型编译、隔离 Electron 常规/紧凑窗口操作检查。新图片驱动已加入打包来源清单，但本次没有打包或发布；后续真实单图草稿、独立网址点击与中断续跑已通过，完整范围和未验证项见上节。

### 2026-09-09 内测 1.1.5 发布与本机增量验收完成

- 完整包与组件频道均已发布 **1.1.5**，回读签名及本地产物哈希一致。发布提交为 `369e7d5b537dc72265a9081e59ac4e7ad95d63a2`，构建标识 `20260908T1705Z`；完整安装器 441,669,461 字节，SHA-256 为 `033c84ee502c99e0e376533202c4e5a52a7ccffd2210cc82b55f8cc3993cb28b`。
- 1.1.1 实际增量验收暴露媒体协议初始化晚于 Electron ready 的启动错误，失败后回退成功并撤下组件指针。已将协议声明前移到同步启动入口，同时修正发布验收资料目录的隔离顺序；修复回归及最终源码检查通过。稳定底座为 1.1.2，旧底座通过完整安装包升级。
- 本机已完整安装修正后的 1.1.2 底座，并连续完成 1.1.2 → 1.1.3 → 1.1.4 → 1.1.5 真实组件升级。最终选中 1.1.5，启动回执成功、待切换状态清空，授权正常。
- 实机验证发现更新助手被 Windows 隐藏启动；已修正业务控制器的启动方式。从修复后的 1.1.4 发起 1.1.5 更新，原生可见窗口检查通过并捕获窗口，安装与启动耗时 20.9 秒。截图受显示缩放影响只记录部分窗口，不能作为完整窗口排版验收；本次确认的是实际可见及升级成功。
- 最终组件构建 329.2 秒，随后从同一批已验证组件生成完整包，额外组装及完整包校验 615.7 秒，组合流程共 944.9 秒；该总数是两个连续阶段相加，不是另一次独立完整构建测速。三项运行时缓存命中；底座指纹一致，仅应用组件变化。
- 网络下载样本为 1.1.3 → 1.1.4：24,707,594 字节，下载与准备 20.6 秒，安装与启动 20.3 秒。最终 1.1.4 → 1.1.5 重试使用已准备的本地组件，准备 7.6 秒；不把该次零新增下载计为网络测速。此前验收脚本两次因按钮歧义及完成面板遮挡在安装前停止，已修正脚本，未更改应用或重复安装。
- 真实安装版公告按版本去重，历史内容可展开，全部阅读后重启仍保持已读；实际媒体协议在隔离会话中加载包内测试图片成功，该项不代表客户原始素材可用性验收。
- 升级前已备份并逐文件校验 4,697 个用户资料文件。最终核对无原文件丢失；3,889 个原业务文件中 3,883 个哈希未变，授权、API 配置、专家与角色偏好均保留。两个业务数据库完整性正常，36 张表原始主键全部保留；任务文案、结果及排除名单、自动接待已处理记录未变。
- 用户明确确认公开目的地与源码范围后，最终程序及服务源码已推送至 `survivor2829/AIsell` 的 `codex/ci-110` 分支，远端提交核对为 `189254bdd508c542946572f9cea5fb30d0b7d424`。该提交与发布提交的程序及服务源码一致；[实际 GitHub CI](https://github.com/survivor2829/AIsell/actions/runs/34296976531) 的源码与界面、服务与内容两组任务均通过。运维文档未随该源码分支公开。
- 另一台电脑的联系人同步、计划精准触达与内部测试发送仍需真实复验；“实际发出但无法确认”尚未确认修复。诊断已补齐阶段、耗时和确认依据，不包含客户聊天原文；结果不明时不会自动补发。本轮未将既有两条真实反馈改为公开，也未替用户回复或关闭。
- 本地证据位于忽略目录 `outputs/release-110-20260908/`：`final-115-verification.json`、`incremental-115-accepted.json`、`installed-1.1.5-ui.json`、`retained-profile-final115.json`、`retained-task-records-final115.json` 及对应构建/签名记录；异机复验步骤见同目录 `1.1.5-test-machine-checklist.md`。

### 2026-09-08 内部 1.1.1 公告与诊断补齐（打包与 CI 通过，增量启动失败已撤下）

- 每个版本必须维护具体客户更新公告，整包、组件包及发布清单共用同一份内容；缺失或不一致时构建/发布直接报错。手动安装和离线启动也能查看当前版本说明，同版本整包与组件公告合并，已读状态持久保存。标准见 [更新公告规范](desktop/docs/release-announcements.md)。
- 微信联系人同步记录重启、等待登录、捕获与读取阶段，以及可读取的版本和进程状态；精准触达和内部测试发送记录步骤耗时、发送前检查及回执确认依据。日志按操作关联，不含客户聊天原文；重复等待不刷屏。反馈附带诊断时包含同次失败的技术步骤，服务器保留允许的技术字段。
- 界面构建、相关客户端定向检查及维护服务本地 HTTP 检查通过；代码审查无剩余阻断问题。消息已发出但显示无法确认的异机问题仍需新日志定位，本次不宣称修复；确认不明保持终态，不自动补发。
- 前一版 1.1.0 已完成本机安装，原有业务记录及授权配置保留；1.1.1 已完成打包与实际 GitHub CI，但真实增量启动失败并撤下，见上节。异机升级与真实微信复验尚未完成。

### 2026-09-08 内部 1.1.0 过渡版（实现与隔离验证完成，待打包安装及发布）

- 合并新机器人图标、“你的AI专家”、反馈社区、开发机反馈管理和微信短步骤引导。顶部“更新公告”旁新增“检查更新”；非微信页面空闲时隐藏微信任务按钮，没有任务时显示“带我开始”。常规及紧凑窗口隔离验证与最终界面构建通过。
- 新反馈可公开或仅开发者可见；历史反馈保持私密。公开投影不含设备、诊断、任务关联或回执凭据。开发者管理通过本机既有 SSH 身份访问回环后台，测试版安装本身不授予管理权限。现有两条真实反馈仍待处理，未回复或关闭。
- 新增签名组件协议、稳定启动入口、统一资源路径、独立更新窗口及失败回退；更新前检查工作任务，普通退出不自动安装。隔离 Electron 已验证等待进程退出、切换、启动回执、中断启动及损坏代际回退；本地 HTTPS 断点续传、签名及兼容性验证通过。商品详情更新内部接口的空闲、占用、暂停/恢复、控制令牌和回环边界验证通过。
- 发布构建复用经过源码、依赖和完整性核验的运行时缓存，保留原构建来源。独立组件构建不生成全量 ZIP 或安装器，旧 v1 频道继续提供完整过渡包。实际安装、服务部署、异机升级和真实前后版本节省量仍需后续记录，不能用隔离夹具代替。
- 下方 1.0.2 是此前已发布状态；本节尚不表示 1.1.0 已上线。协议与操作见 [内部更新标准](docs/internal-release.md)。
- 新收到的异机同步反馈排查确认：CIM 查询失败可能跳过微信退出，停止失败也可能继续等待；已修复并用模拟 PowerShell 复现前后差异。客户端与服务器保留技术失败阶段，自动接待重名阻断增加可操作提示与启动诊断。隔离回归通过，原故障电脑是否命中这些分支及修复后结果仍待实机确认，见 [异机排查记录](docs/reports/2026-09-08-contact-sync-cross-machine.md)。

### 2026-09-08 新主图标与专家入口（源码完成，未重新发布）

- 用户重新指定机器人原图作为主图标，按明确授权进行像素级去白边；RGB内容不变，四角为真实透明。应用左上角同步换新图标，相关入口与专家页标题统一为“你的AI专家”。界面构建及隔离Electron常规/紧凑窗口检查通过。
- 桌面测试版快捷方式新图标已实际显示，启动目标和参数保留；目前图标引用项目资源目录，安装EXE内嵌图标与应用内文案仍是已发布1.0.2。当前没有为这些改动重新打包或发布，也没有实施增量更新器。
- 已核实现有更新链路为全量EXE；组件增量发布建议与兼容、回退和过渡版本边界见 [快速更新评估](docs/reports/2026-09-08-incremental-update-assessment.md)。下方1.0.2仍是最新已发布版本。

### 2026-09-08 内部 1.0.2 收尾（CI、本机安装与内部发布通过）

- 用户确认本轮只更新本机与内部测试用户；本机已安装并验证最新测试版1.0.2，旧正式版程序已卸载，两版原数据及完整备份保留。正式客户频道继续关闭。
- 已建立 `.github/workflows/ci.yml` 与 [内部更新标准](docs/internal-release.md)，新增完整内部安装包构建命令；[最终 CI](https://github.com/survivor2829/AIsell/actions/runs/34209624866) 两个作业全部通过。包对应干净提交 `215392c`，后续 CI 提交 `fd5b6b7` 只调整编排及不入包的自检夹具，实际安装源码、运行时和 EXE 与候选包一致。
- 收尾发现并修正遗漏的角色/反馈总自检、制作聚合 preload 契约、旧文案生成测试替身、角色配置迁移断言及写死的安装版本号。65 项确认稿制作回归、4 项制作聚合、4 项请求用量及维护服务本地 HTTP 检查通过；这些为隔离验证，不是新的付费视频或真实微信验收。
- 图标已按用户最终提供的带“AI获客”文字图片更新，圆角外棋盘格转为透明；安装 EXE 七档图标与资源一致，桌面实图已验证，入口不再依赖项目 ICO。原图与旧图标保留。
- 完整安装器与运行时检查通过；反馈服务已部署并完成安装版提交、后台处理、客户端显示已解决的闭环。内部测试频道已发布1.0.2，公开清单签名、版本、包大小和摘要回读通过。数据审计和未验收范围见 [1.0.2 验收记录](docs/reports/2026-09-08-internal-release-1.0.2.md)。发布后的公告窗口未补验；真实微信、新素材全流程视频、异机安装及长期运行仍需对应范围试用。

以下同日及更早小节为当时的阶段记录；其中“未打包/未部署/未推送”不覆盖上述最新结果，视频质量及真实场景未验收项仍按各自范围保留。

### 2026-09-08 视频样式修复（本地重渲染与播放通过，待用户复核）

- 已重建并切换实际默认 development 视频运行时，源码、包摘要及 Chromium 合成检查通过。复用原底片和原音轨重渲染同一条成片，1080×1920、36.933秒，音轨仍为36.829秒且音频包哈希与修复前一致；未新增LLM、TTS或ASR调用。
- 恢复白字黑描边的一至两行字幕，结尾引导改为无色块的描边文字；修复按宽度分页产生的末尾单字闪现，保留原始正文及观测时间。4项相关字幕回归通过，完整解码及Electron临时播放器1倍速有声播放到结尾通过；用户最终视觉认可尚待复核。
- 已更新成片中心原位置，旧成片和旧运行时均保留备份，未新建业务批次或修改确认稿。未打包、发布或部署。证据见[修复记录](docs/reports/2026-09-08-video-render-repair.md)。

### 2026-09-08 界面调整与视频样式回退诊断

- 角色首页取消人物外层面板、阴影及放大裁切，完整适配原图并柔化图片边缘；成片中心直接展示可用作品，移除批次概览及已删除入口，制作记录独立进入。未删除历史记录或文件。界面构建通过，Electron 紧凑窗口三角色、9条可用作品、搜索及制作记录导航检查通过。
- **用户指出本次36.829秒视频排版劣于9月5日、7日已认可样片，原版视觉质量未通过。** 三批次均指定 `reference_narration`，但当时实际加载默认 development 旧渲染包，其中没有新版字幕组件；实际运行时摘要与当时成片 manifest 一致。应用界面构建不更新 Remotion 视频包。此诊断阶段未重渲染；随后获用户授权完成上方所列修复。详见[调整与诊断记录](docs/reports/2026-09-08-experience-refinement.md)。

### 2026-09-08 角色外观、客户支持与制作记录（本地工程验收通过，视频视觉质量未通过，未打包部署）

- 三位数字员工各有 3 种形象，共 9 种；用户可修改 1～20 字名字，头像、主色、背景纹理及相关工作区随形象切换。角色目录由共享配置约束，偏好经主进程原子保存，保存失败不伪报成功；重新打开保留已保存设置。
- 原日志入口整合为“吐槽中心”，包含“我要吐槽”“我的反馈”和折叠的日志诊断。反馈保存独立草稿与发送队列，提交时冻结正文、任务关联和可选脱敏诊断；相同 ID 重试沿用原始快照，服务端回执后才显示待处理、处理中或已解决。回执凭据仅在当前 Windows 账户下加密保存，服务端只留摘要；本次附带诊断不等于开启持续自动上传。
- 顶栏新增更新公告与离线使用教程。公告复用签名版本清单，本机保留最近收到的 20 条及已读状态；每 30 分钟刷新元数据，手动刷新公告不下载安装包。网络失败保留缓存，旧序号、篡改或同序号冲突清单不会覆盖记录；安装包检查仍沿用每 6 小时的独立流程。教程随客户端提供，涵盖快速开始、微信客户运营、内容创作、素材与成片、常见问题及对应入口。
- 首页“待处理制作”和创作工作台改用 `production_summary.py` 的业务制作聚合：先读完整记录，按批次、引导会话和明确的重试关系归并，再筛选、统计和分页。内部分析/审核步骤、同一制作的旧失败、已完成及归档记录不重复计入待处理；同一项目下独立制作仍分别保留。历史入口按原 `taskId`、批次/会话/运行 ID 恢复，候选在数据库查询阶段按任务过滤，归档批次只读查看。
- 新增 `provider_usage.py` 实际请求账本，将已接入的文本生成、ASR、TTS 请求与制作任务关联，记录开始/结果、耗时、实际模型、重试、请求标识及供应商返回的用量。缺失指标保留未知，未完成请求保留 `outcome_unknown`；不保存提示词、正文、密钥或路径，不由任务步骤数估算实际费用，金额仍须供应商账单确认。
- 本地关键自检已通过，包括角色偏好、签名公告/缓存、制作聚合与反馈持久化。反馈已完成隔离回环 HTTP 联调，覆盖幂等回执、凭据拒绝、公开端无列表、后台权限、状态更新和诊断到期保留正文；551 条内存候选记录验证能找回最近 500 条之外的原任务视频。真实 Electron 已检查 9 种角色形象、紧凑窗口及离线教程；这些结果不替代反馈云端部署或真实视频质量验收。
- **本次新增反馈云端接口及后台尚未部署。** 原批次已完成1/1条36.829秒真实成片，1080×1920、7段配音和字幕，正文只增加明确获准的“现场讲师介绍，”；Electron中1倍速开启声音完整播放到结尾，媒体及页面错误为空。第4段同音ASR转写差异使用段级字幕计时，其余6段字级对齐。本次8轮调试与验收累计131次LLM、7次TTS、7次ASR及1,908,930 token，配音计费195字；金额须供应商账单核对，不宣称降本比例。 本轮未打包或发布，2026-09-07 的测试安装包升级与已认可样片保留其原有版本和验收范围。新增服务的部署文件及端点见 [维护服务说明](server/maintenance/README.md)；已验证范围、实际成片、全部用量和未部署项见 [本次验收报告](docs/reports/2026-09-08-customer-experience-acceptance.md)。

### 2026-09-07 测试更新与云端诊断（完整测试版本机升级通过）

- 测试版源码已接入签名清单校验、后台下载、退出安装、可关闭的脱敏上报及离线队列。上报默认关闭，正式版云配置保持关闭；未增加计费功能。
- 上海测试服务器已部署 HTTPS 443 服务和仅回环 8081 的管理后台，本机通过 SSH 隧道访问。实际收到完整测试版 `1.0.0`、`1.0.1` 的运行记录和受控异常，预设客户正文、密钥与路径均未进入上报记录。
- 隔离 NSIS 程序实际完成 `0.1.0 → 0.1.1` 下载、退出安装、新版启动；本地标记文件与匿名安装编号保留，待传队列清空。其 appId 为 `com.aihuoke.maintenance.smoke`，不能视为完整 AI获客 包或真实微信任务验收。
- `build:test`、更新签名/篡改拒绝/默认不上传/脱敏/离线队列专项、诊断日志专项、后台去重和修复后复发专项通过。后台 Chromium 桌面/390px、诊断组件 Electron 桌面/640px 已检查；后者使用隔离 IPC 展示状态，未操作微信。
- 后续完整包阶段已修正与现有接口、显式登录页及统一任务流程不符的旧检查，并同步现有支持头像的联系人 helper 发布摘要。完整源码自检、产品详情图本地 E2E、配套运行时和两版安装包内容校验通过；全仓 `tsc --noEmit` 仍受已有 React 类型缺失影响。专门代码审查 Skill 的引用文件缺失，已完成差异人工检查和精简复核。
- 从干净提交 `a416998` / `c95bce5` 分别生成完整测试安装包 `1.0.0` / `1.0.1`。本机实际完成签名更新下载、正常退出安装及新版自动启动；后台同一安装编号出现两个版本，诊断开关保留且队列为零。当前测试频道为 `1.0.1`，未启用正式频道。
- 安装基线后原有 3,655 个文件哈希全部一致；升级及正常启动退出后，素材、配置和业务记录保留。数据库 32 张表共 2,391 条记录无丢失，仅 8 条音色记录更新时间变化；SQLite 临时 WAL/SHM 正常合并，任务状态时间及日志正常更新。已有授权仍有效。真实微信/视频任务执行中升级、跨机器及长期运行尚未验收。
- 已按用户授权本地提交代码和版本变更，未推送。安装包仍为内部测试制品，无 Authenticode 签名，不等于正式客户发布。部署、使用与签名密钥维护见 [测试服务说明](server/maintenance/README.md)，实测证据见 [验证记录](docs/reports/2026-09-07-cloud-maintenance-validation.md)。

### 2026-09-07 视频制作流程与验收样片（本地开发版通过）

- 已跑通导入素材、生成三份完整文案、选择方向及各方向数量、确认正文与声音配乐、自动编排检查、配音字幕合成及自动导出。各方向首条保留确认正文，后续作品沿用方向；未知请求结果不自动重提。
- 本次第三方向完成1/1条，实际40.352秒、1080×1920、14个真实镜头；第二版确认正文逐字一致，9段配音与字幕对应，无循环或补尾凑时长。Electron内1倍速完整播放到结尾无错误，用户明确认可，作为后续同类视频验收样片。
- 火山方舟、语音识别与配音经软件配置调用；素材、配乐及相应授权仍由使用者提供。新增样片证明本次完整流程，不代表任意素材、多方向大批量或跨机器稳定性均已验收。
- 构建及相关定向检查通过；尚未重新打包、覆盖正式安装版或对外发布。当前标准见[成片标准](docs/video-production-acceptance.md)，验收详情见[本次收尾记录](docs/reports/2026-09-07-video-production-closeout.md)。

以下2026-09-05及更早条目是对应版本的阶段记录，其中“待验收”“尚未出片”等描述不覆盖上述最新结果。

- 2026-09-05 火山混剪已完成同批两条：首条37.618秒获用户认可；第二条47.562秒、小何2.0、Summer、25个字幕片段及3处行内emoji，字幕正文、完整解码、画面抽查和混音检查通过，用户已认可两条。第二条比45秒目标长2.562秒，作为已认可例外保留，文案经辅助调整，不能宣称无人干预批量写稿已稳定。已修复生成/审片指令混用及愿望误判，首条、原素材和旧版本保留，本批记录仅见火山调用，未发布或打包。详见 [火山迁移验收记录](docs/reports/2026-09-05-volcengine-migration.md)。

- 2026-09-05 用户已选择第二份《刚清理完又有垃圾，才是让我头疼的事》。新批次 `narrated_batch_bd062df0e45144bfb5194f1b2451a3db` 已通过应用确认按钮保存正文第1版，确定性映射估计口播35秒；记录见 `outputs/video-optimization-20260905/selected-script-2.json`。启动后首个百炼事实复核请求返回HTTP 403，任务 `task_1cb8677e3106485b933790094fd24204` 已失败，未调用配音和渲染，不自动重试；当前尚不能交付样片。正文复核已修复为保留未改文字的逐句场景绑定，Python编译和定向绑定保持检查通过。403通用提示不能区分密钥、业务空间或模型权限，须核对服务端后再恢复，不能绕过检查宣称成片通过。

- 2026-09-05 完整样片验收已接入 `inline-emoji-20260905` 新运行时，合成检查通过且模板摘要与源码一致。五段文件存在、整理出32个可用镜头区间；两个新验收批次在自动编稿阶段因镜头重复分配、段落超时或映射缺失未得到合格三稿，已暂停后续重试，未配音或渲染。已简化初稿为自然段镜头映射并将新版规划预算收紧至总计12次调用/300秒，但实跑仍未通过，不能宣称流程跑通。助手另行整理的三个待确认草稿见 `outputs/video-optimization-20260905/standard-script-options.md`，不是系统自动生成成功的证据。

- 2026-09-05 字幕 emoji 已按用户纠正改为插入相关词语后、与正文同行排版；参与换行宽度计算，不进入配音正文。Remotion 编译与静态渲染验证通过，输出 `outputs/video-optimization-20260905/implementation-preview/subtitle-inline-emoji.png`；旧样式图保留，完整动态成片尚待验证。

- 2026-09-05 用户提供的 Summer（154.067 秒）与 My Soul（140.066 秒）已复制入当前测试版本机音乐曲库，并设为新批次默认候选。真实 Electron 中两首均可勾选且已勾选；构建、文件分析及明确选曲检查通过。非商用本地曲目仅在明确选定池中参与制作，本轮未生成新成片或发布安装包。详见 [本机曲库验收](docs/reports/2026-09-05-user-music-library.md)。

### 2026-09-05 成片四步流程与声音接口（小何 2.0 真实试听成功，完整样片待验收）

- 新批次采用选素材 → 三份完整文案 → 选择方向并确认 → 自动制作本批；确认正文和版本单独保存，首条完成后再生成同方向后续作品。旧批次、已批准声音及缓存保持兼容。
- 已接入火山预置音色接口与 Windows 加密配置入口；指定配乐池、本地试听、批次轮换及实际选曲记录已接通。用户保存 API Key 并开通服务后，明确选定小何 2.0；新增对应官方预置，原三个候选保留。
- ASR 词句时间与确认正文对齐，新流程字幕采用白字黑描边、稳定两行及本地 Noto 装饰；实际 Electron 读取、本地音乐试听、独立 Remotion 构建、字幕样式图、宽窄界面及必要专项检查通过。
- 首次直率英子真实试听请求返回 `cloud_request_rejected`，没有输出音频；另外两个音色尚未请求，未自动重提或批准任何声音。账号的语音合成 1.0 服务开通和权限仍待确认，不能据此断言密钥无效。详情见 `docs/reports/2026-09-05-volc-audition-first-attempt.md`。
- 后续小何 2.0（`zh_female_xiaohe_uranus_bigtts` / `seed-tts-2.0`）实际合成一次成功，生成 15.221 秒、24kHz 单声道 WAV，峰值 -1.55 dBFS、零削波采样。依用户明确选择，通过实际界面批准并保存为新批次默认，刷新后确认保留；未改旧批次或确认正文。详情见 `docs/reports/2026-09-05-xiaohe-voice-selection.md`。
- 当前尚无新增已确认可导出的热门曲目；两条完整样片与手机外放验收仍待文案和曲目选择。全仓 `tsc` 因现有 React 类型声明缺失未通过，本轮界面 `build:test`、音色目录专项及错误提示专项通过。未打包或发布，接口实施范围见 `docs/reports/2026-09-05-video-flow-implementation.md`。

### 2026-09-05 成片链路定向优化（已有基准样片，稳定发布质量未通过）

- 新增稳定的逐段事实审查复用、失败段落局部改稿、格式失败后的事实反馈继承和单条规划预算；生成样片在规划通过后直接进入实际制作，部分可用时保留缺额并先制作可用项。
- 原 5 段素材已实际完成一条 36.685 秒、1080×1920、H.264/AAC、Remotion 基准样片，配音/字幕/授权音乐齐全，实际区间没有循环或补静音，12 个审查关键帧在 TTS 裁切后均保留。文件保存于 `outputs/video-baseline-20260905/`。
- 抽帧发现基准样片存在“清扫对象与避障主题混淆”和段落内部场景错位，不认定发布质量合格。随后最新 story v2 已改为保留 AI 的逐段镜头映射、拒绝跨素材段落、停止按总字数补镜头；该版对照实跑在 93.271 秒、6 次规划调用后返回具体段落容量不足，未生成第二条视频。
- 基准样片源码摘要为 `d6b7703cf2c0db34b39dd9dbbbe2963bcfd8649c7945887a29abeaedce397500`；最新映射版为 `36750e69c4a95381a726521a64427c709a3ecd471b5e660a50c64294ec10c708`。两者验证结果分开记录，详见 `docs/reports/2026-09-05-video-optimization-validation.md`。
- `build:test`、Python 编译与受影响领域检查通过；最新映射版另完成 7 项相关现有检查。实跑使用测试进程的 `qwen3-max` 写稿配置与已批准的声音，产品默认 `qwen-plus`、最新源码完整出片和用户手机盲听盲看尚未通过；未重新打包、覆盖安装或发布视频。
- 后续优化已统一声音预算：36 段有效、去重且匹配当前声音配置的真实 WAV，字符加权 P20 为 195、P90 为 233.269 毫秒/字；同一任务冻结采样，分别用于最低口播字数与镜头容量。场景/任务对象/障碍物上下文按素材进入编稿、改稿与原有审核，不能作为功能或效果证据。源码摘要 `89b7edc14b044363216c41a12f2a7311cc7b7f42171c702c5c4c12f25c105178`；Python 编译、4 项相关现有检查及真实缓存校准通过。点击真实生成前被自动审批拒绝，理由是缺少明确的云端付费和素材传输授权；本版本尚未发起新生成、没有新增成片。

### 2026-09-03 三模块 AI 批量创作（源码实现，真实成片待验收）

- 创作工作台默认开头／中间／结尾自由选材，素材集复用原文件引用，支持缩略图、预览、多选与跨区移动。旧商品单片、课程工具及历史项目入口保留；成片中心新增批次入口。
- 新增独立 `narrated_batch_v1` 任务及持久化方案，不改变旧项目协议。推荐值、已找到可用数与 300 条运行上限分开；目标不足时先返回原因，配音和渲染不启动。先做最多 3 条样片，确认后继续，已完成结果复用。
- 分析缓存、独立 AI 审片、逐句单镜头证据绑定、完整镜头覆盖、实际配音后时间线复核和跨批次近重复过滤已接入。批量模式不截短解说来凑成片，原音静音；使用现有配音、授权音乐、字幕、品牌和本地真实画面封面。
- 支持局部修改、暂停／恢复／取消，以及视频、封面、标题和发布文案目录导出。可查询的声音设计未知结果先查询；同步文本／配音若服务不支持查询，保留未知状态并禁止盲目重提，不承诺自动恢复。
- 验证：`build:test`、Python 7 项领域／服务专项、新增批量 IPC 专项与原 IPC／媒体协议检查通过。专项覆盖 1／2／5／6 条、推荐低于可行数、50 条不足零配音、100／300 条离线方案容量、暂停恢复、第 10 条局部重做和无效镜头引用拒绝。AI／TTS／编码使用测试替身，不能当作实际成片验收。
- Electron 隔离数据实测已覆盖仓库预览与选材、2／2／3 分组保存目标 6、素材集保存、缺少云服务时明确提示且保留数量。真实 AI 成片的连贯性、声画匹配、批次差异和引流效果尚待用户素材及后续发布数据验证；未重新打包、覆盖安装或操作发布平台。操作清单见 `docs/NARRATED_BATCH_ACCEPTANCE.md`。
- 明确标注的合成样片状态另用于 Electron 播放、确认继续和导出验收，实际导出 6 个视频／封面／文案及批次清单；不作为 AI 制作质量证据。本地 H.264 MF 预览代理检查通过。
- 独立 `tsc --noEmit` 因仓库未安装 React 类型声明而报大量现有类型错误；本轮 Vite 构建通过，不将两者混为一项结论。

### 2026-09-03 内部试用覆盖升级打包

- 用户要求沿用桌面原版，不安装第二个同名软件。`release:upgrade` 使用 delivery 界面及原 `com.aihuoke.desktop` 身份、`Programs\\AI获客` 安装目录、`xiaoxi-active-touch-delivery` 数据目录和原快捷方式，生成内部评估制品；既有测试版不作为此次升级目标。
- 制品明确记录 `artifactType: internal-evaluation`、`commercialReady: false`，不伪造商业许可或签名；原商用发布入口的授权与独立签名检查保留。打包继续要求干净提交、固定依赖、隐私扫描、包内容一致性及运行时检查。包生成、覆盖安装及异机验收分别记录，不由源码验收代替。
- 发布前检查修正两个旧测试问题：只读扫描注释中的关键词误报，以及纯色模拟头像被既有纯色排除规则拒绝。头像样本加入纹理，原位置与边界断言保持不变，未修改已验收的微信执行逻辑。审查修复普通发布继承内部升级标记的问题，入口现在明确拒绝用途串用。

### 2026-09-03 旧帖页脚遮断长帖接续修复（用户本机验收通过）

- 14:43:31 日志确认：首帖点赞、评论各 1 次，下滑 1 次后观察返回 `moments_post_identity_missing`，未进入正文到菜单接续。用户新截图离线回放：上方旧帖只识别出“8天前”和菜单，下方长帖正文可读，但菜单尚未完整露出。
- 评论模式下，只有页脚而没有正文的候选不再占据完整帖子分支；转而读取最后可见菜单下方的新帖正文，触发已有的小幅下滑及同帖菜单定位。正文候选同一行优先真实头像评分，避免把旁边昵称当成另一帖。仅点赞模式不改变。
- 新截图离线读取确认新帖真实头像及正文；几何回归执行真实的页脚排除／正文回退分支，互动协调回归验证正文到菜单接续，`build:test` 通过。
- 验收收尾：代码基线 `7f09a7e`（包含 `4263289` 的框选与正文精修）已重启至本机测试版，用户随后明确反馈“验收完毕”。本轮修复记为用户本机验收通过；未提供逐项实测明细，不外推为所有帖子、跨机器／DPI 或其他模块均通过，也不代表安装包已重新生成或对外发布。任务记录与防重账本保持不变。

### 2026-09-03 朋友圈框选与评论正文精修（已随本轮完成本机验收）

- 原因证据：用户两张真实截图离线回放中，互动按钮已找到，但头像横向扫描漏掉居中布局的实际头像，并将靠近页脚的图片／文字块当成头像，导致正文裁剪从帖子中间或底部开始。`body_only` 结果缺少菜单位置却进入动作校验，日志因此报 `moments_observation_snapshot_invalid`。
- 修复：按头像尺寸扩展估计位置左侧的扫描覆盖；修正 PowerShell 哈希表排序；结合四侧留白、头像评分配对，不再以距菜单最近作为首选。读取区域的下边界按同一头像列确定，保留完整可见正文，不强制展开全文。
- 普通帖先定位结构、再读取一次正文，用头像和菜单几何绑定两帧，避免重复 OCR 的文本差异导致完整帖被丢弃。内容字段单独传给评论生成，排除作者行、菜单行及以下区域；仅正文回退时识别时间行截断，避免带入点赞评论区；修正 OCR 空白归一化的转义。
- 仅正文结果先保留当前帖，使用现有小幅滚动与定向识别接续菜单，不执行无按钮的点赞评论、不计为已处理。仍不能定位时暂停并保留原始原因，不循环跳过或伪造成功；保留原有未知结果禁止补发与防重账本。
- 诊断在正文回退等提前返回路径也记录菜单候选、头像数、配对数、OCR 帖子数、框选边界及阶段耗时，不记录正文或截图。减少重复扫描计算，复用已有观察执行入口。
- 验证：两张用户截图的目标正文／菜单配对恢复；离线几何回归、互动协调回归（含正文到菜单接续与纯正文 AI 输入）、嵌入式 PowerShell 语法及一次 OCR 几何绑定检查、`build:test` 通过。随后随 `7f09a7e` 重启并取得用户本机验收确认，见上节。未运行全量测试／独立类型检查或生成安装包；OCR 仍可能有错字，评论语义效果不能用构建结果代替验收。
- 后续回归关注：完整居中帖及长图帖的正文与评论对应关系；正文先出现而按钮尚未露出时的同帖菜单接续；已互动帖不重复评论，达到目标后结束。若再次失效，查看上述分段诊断。

### 2026-09-03 朋友圈连续执行与重复评论修复（待真实复验）

- 用户确认：一批朋友圈任务连续执行，完成后再检查客户消息，不再每处理一条就退回调度并重新打开朋友圈。取消单条切片及四次扫描强制让出，保留暂停、未知发送结果停止和连续滚动仍为同一已处理帖时停止。
- 本机最新任务记录为评论 2 次、下滑 0 次；两条 OCR 正文从 97 字变为 143 字，较长正文完整包含较短正文，头像哈希变化，稳定片段均为空。旧判断返回不匹配；新增仅用于防重复的长文本包含匹配后，这对记录返回匹配。
- 同任务已处理标记和跨任务评论账本共用防重规则：归一化后的较短片段至少 24 字且覆盖较长文本一半，完整包含时跳过，不再要求头像哈希一致。已确认评论不会因当前画面读不到旧评论而补发；弱相似候选仍保留只读 OCR 检查。此宽容规则只用于抑制重复，不授权点击或发送；不同帖子大量复用相同文案时可能保守跳过。
- 验证：连续批次回归覆盖一次打开、评论失败继续下滑、同帖正文扩展与头像变化跳过、完成两帖以及完成任务再次调用不重开；跨任务回归覆盖持久化旧评论直接拦截。互动协调、评论动作自检及 `build:test` 通过。没有重启、操作微信或改写用户任务记录。
- 边界：未新增通用的本人手动评论识别；无程序历史记录或正文变化超出本次匹配范围仍需单独处理。真实验收需从刚评论过的页面新建任务，确认不重复评论、持续下滑、达到目标停止，随后恢复客户消息检查。

### 2026-09-03 聊天返回朋友圈的入口识别耗时修复（待真实复验）

- 13:38:45 本机日志：打开朋友圈在 `discover_entry` 阶段耗尽 45000ms，未开始扫描或互动；日志尚不足以区分单次慢扫描与多次匹配失败。
- 侧栏背景统计、前景行扫描和绿色回退改为直接读取冻结截图的 BGRA 数据，移除逐像素函数调用、像素对象和字符串分桶；保留颜色阈值、图标形状、窗口归属和 45 秒总预算。
- 导航诊断返回每次发现入口扫描的耗时、候选数和匹配数，成功、普通失败及超时均可保留已完成扫描信息，不记录截图和聊天文本。
- 离线同图对比：125% 缩放、未选中发现图标，旧实现 3451ms，新实现 552ms，候选位置和全部识别字段相同。此结果仅证明该样本提速，不代表真实微信切换成功。
- 导航自检、现有图像识别回归、数值诊断回传检查与 `build:test` 通过；没有重启程序、修改用户任务账本或执行微信操作。真实验收应覆盖聊天→发现→朋友圈，以及完成一次消息读取后再次返回朋友圈。

### 2026-09-03 本轮运行后的修复（源码与隔离检查，待真实微信复验）

- 用户反馈：发布已发生但任务显示 `moments_publish_outcome_unknown`；新互动任务可能重复评论；自动回复停在侧栏。
- 发布确认改用发表前已排除旧帖的 24 字可见片段，不再误传整篇文案作为结果查找条件；保留发表前完整内容、媒体核验和未知结果禁止重发。未找到结果时区分 OCR 无正文、片段缺失、多候选、新帖证据不足。
- 跨任务评论防重复用原有持久化动作记录：头像像素变化不再排除相似正文/稳定片段，候选旧评论经只读 OCR 检查后跳过。不把“评论过一个人”扩大成永久跳过此人所有新帖。已有评论而本轮补点赞计入新完成，不增加新评论数。
- 返回聊天优先使用 UIA 或已识别侧栏图标的排列；保留归属和切换结果验证、悬停 OCR 回退。导航失败保留原始错误码，连续三次后暂停自动回复，其他任务继续。浮窗显示实际读取、生成、发送阶段。
- 验证：本地测试界面构建及互动动作、发布驱动、导航、自动回复、互动协调、统一工作流隔离检查通过；原生 Electron 七种浮窗状态检查通过。没有执行真实发布/评论/客户发送，未修改用户运行记录。
- 限制：防重依赖已有程序评论记录及可读页面；此前手动评论而程序无记录、旧评论折叠不可见、相似内容匹配不到等情况仍需真实验收。旧发布未知记录不会自动改成成功，请通过已有发布结果核对入口确认实际结果。

**微信拓客核心功能开发基本完成；朋友圈单条发布已在微信 `4.1.11.55` 完成真实发表与自动结果确认验收。新版逐帖互动已经真实执行过点赞和 AI 评论，用户确认微信侧动作成功；2026-09-03 修复并重启后，用户确认本次朋友圈互动验收成功，跨场景和异机仍需单独验收。主动触达修复已由用户使用真实微信复验。产品详情图已完成集成、覆盖安装和真实在线验收。创作工作台已在长课程精剪与三段式批量混剪上接入第一版“一键网感包装”、百炼批量语义编导和可选 Remotion 高质动态渲染；普通任务支持可见 FFmpeg 回退，三风格对照严格失败关闭。素材驱动“一键混剪 V2”已完成“解析素材→5 项内引导填写→百炼 AI 脚本→成片”的源码实现，并已用用户提供的 5 段素材完成一次真实链路：素材解析、百炼脚本、实际配音、授权选曲、Remotion 渲染和播放核验，输出 48.818 秒、1080×1920、H.264/AAC 的成片。该证据只覆盖当前开发运行时的一次真实任务；干净提交的内部评估包、手机盲听盲看和商业安装器仍未完成。现有 15 条旧成片均缺少持久化动效计划，零付费 3×3 验收因此按门禁停止。5+5 付费素材验收、30 条混剪和平台批量生成仍未完成。** 构建和 self-check 不能替代安装包的真实微信或真实素材验收。

AI获客 2.0 的实施基线为 `df0aa49`。产品详情图最终交付安装器对应提交 `f25aa0e`，产品详情图与内容引擎固定运行时、便携包和安装程序均由该干净提交重建，并通过事务构建、整树哈希、隐私扫描、真实 Electron 下载回归和无头 Chromium 工作台回归。2026-08-03 用户完成真实 AI 精修、覆盖安装和桌面下载验收；本轮收尾后以 `main` 作为唯一继续开发主线。

### 2026-09-03 朋友圈互动与统一工作流（用户本机验收通过）

- 修复后已重启本机测试版，用户明确确认“验收成功”；对应收尾基线为 `d0a7a4a`。该确认覆盖本次运行，不外推为所有任务组合、长时间运行、不同 DPI、异机或安装包均通过。
- 浮窗先验证 React 控件和 preload 就绪，再隐藏主窗口；开发页面加载失败回退本地编译页面。联系人同步、自动回复、精准触达和朋友圈任务使用统一进度窗。
- 朋友圈视觉识别不再因同时存在 UIA `sns_list` 而拒绝有效窗口；保留窗口、前台、目标和发送归属检查。已打开朋友圈时省去重复“发现”搜索；绿色区域直接读取位图像素。此前同图入口识别由 4262ms 降至 903ms、仅导航由 21.756s 降至 11.233s；不能据此断言首次超时的唯一原因，45s 超时预算未放宽。
- 评论允许依据局部正文，不要求先展开全文；按可见框线定位评论输入框，不再要求它位于旧菜单下方或占整窗固定比例。点赞确认后立即持久化；评论跳过不清空已点赞数量，有新帖则继续寻找可评论对象。
- 有限任务结束后停止调度并保留结果；只有开启自动回复才持续接待。运行中先暂停再调整计划。重启通过持久化 `lastTaskId` 恢复显示；无任务时显示“查看任务”。只有账本证明尚未发生互动、无在途或未知结果的朋友圈失败任务可“重新加入”，不会自动补发或清除记录。
- 消融收尾：移除无调用的旧页面和附属状态、未使用配置接口、纯转发及重复数组复制，保留仍有调用的兼容浮窗和业务适配层。逐组编译、工作流／朋友圈／版本边界定向检查通过；最终隔离 Electron 七种浮窗状态通过，互动与完成截图已检查。用户确认清单后，7 项临时脚本／旧截图目录已移入回收站，最新证据保留；详情见 [消融报告](docs/reviews/2026-09-03-workflow-ablation.md)。
- 验证边界：消融后的源码只完成本地和模拟验证，没有重启用户主程序、重新执行真实互动或生成发布包。独立 TypeScript 检查在消融前已因缺少 React 类型声明等问题失败；未配置独立 lint 脚本。保留 Vite CJS 弃用及开发 Electron CSP 警告。旧运行时、安装包、最新验收证据和用户数据均未删除。

### 2026-09-02 微信拓客今日计划（源码、编译及隔离 Electron 检查通过，真实串联待验收）

- 统一启动悬浮窗遗漏已修复：原 `wechat-workflow:start` 只启动调度，只有另点“查看进度”才创建窗口。现在先加载并显示进度窗，再启动调度；自动回复、精准触达、朋友圈发布与互动复用该窗口，联系人同步也接入同一进度窗。运行中返回主页面保留悬浮窗，暂停后可收起。窗口尺寸、位置、无边框置顶行为复用已有共享配置，并使用不抢焦点的显示方法。
- 新浮窗接回朋友圈当前识别／评论步骤及计数，联系人同步显示连接、登录、读取和完成状态；底部固定为暂停／主页面两个按钮。针对性 IPC 回归已复现旧版“启动后窗口数为 0”，修复后通过；五类任务均使用临时数据与模拟执行器检查了真实 Electron 窗口，未执行真实微信发送、点赞、评论或发布。真实串联验收仍待用户执行。

- 五个微信拓客页面接入“今日计划”与统一启动、暂停、右下角进度浮窗。时间选填，普通任务按加入顺序执行，到期任务优先；每次完成一位触达客户、一次帖子互动或一次发布核验后交还操作权，优先检查客户消息。程序重开保持暂停，用户启动后恢复剩余任务。
- 协调层仅保存任务编号、排序、时间与进度；触达、接待名单、朋友圈素材与结果继续保存在各自模块目录。有效触达名单在加入计划时累积到当前账号的接待范围，账号间隔离，允许手动移出；没有接待对象时明确展示“暂无接待客户”。
- 发布正文与图片／视频复制为稳定快照，加入计划即表达执行意图，保留底层身份、素材、去重和发表结果核验。完成后不重发；发布结果未知仍由用户核实后登记，协调层据核实结果更新原任务。触达未知结果保持需处理，不自动补发。
- 每日互动完成当天数量后等待次日；已完成的每日安排仍可修改或取消后续执行。修改后不重做当天已完成内容。未开始的往日定时任务标记重新安排，未定时任务持续保留。
- AI 专家新增业务访谈与可编辑摘要，继续沿用“专家规则＋业务知识”和已有导入资料。对话草稿不覆盖使用中的资料，保存后供下一次模型调用使用；当前回复使用已经读取的资料。
- 消息扫描前补齐内嵌朋友圈返回聊天：复用共享导航适配，核实聊天入口、受保护点击及选中态后再扫描，保留原消息基线；独立朋友圈沿用主窗口恢复路径。导航／扫描现有自检、调用顺序模拟与 PowerShell 解析通过，实际微信的悬停提示与页面读回仍待验收。
- `npm.cmd run build:test`、`npm.cmd run build:delivery` 与定向调度检查通过。调度检查覆盖客户优先、到期顺序、暂停续跑、重启、跨日、账号范围、未知结果不重试，以及每日安排编辑／取消。既有触达、自动回复、朋友圈和专家模块定向检查通过；本轮没有运行全量仓库 self-check。
- 实际 Electron 使用临时用户目录、模拟联系人／模型／执行器，已检查五个页面、任务添加、专家保存、统一启停、浮窗与 1100 像素宽布局；preload 和 IPC 正常、关键按钮未被进度内容遮挡。这是实际界面检查，不是真实微信链路验收。
- 未启动真实发送或发表，未覆盖安装、提交或制作发布包。后续需在明确授权的测试联系人上验证：无时间任务连续执行后接待；触达／互动中收到消息后回复并续跑；定时插入；暂停／重启／跨日与未知结果；图文、视频及朋友圈与聊天往返。各单项既有成功记录不能替代本次串联验收。
- 朋友圈任务串行补齐：修复“点赞成功但评论跳过仍继续扫描下一帖”导致客户优先窗口错位的遗漏；补充自检场景覆盖“workflow 单帖先 yield、再次调用继续并完成剩余一帖”的关键行为。该修复仍未在正式微信链路复验，仅覆盖隔离脚本与现有编译检查。

### 2026-09-02 日志共享依赖引发启动接口缺失（用户本机验收通过）

- 用户重启后出现“当前版本未连接自动回复执行器”。隔离的真实 Electron 重现 `preload.dev.cjs → preload-api → content-engine-ipc → diagnostics → visual sender → window driver → contact_sync_cli` 加载链；新日志依赖把 CLI 文件带入 preload，首行 shebang 在 Electron 渲染器模块包装中触发 `SyntaxError`，随后自动回复和诊断接口均为 `undefined`。这是上一轮改动引入的启动回归，窗口能打开和 Node 模式自检不能证明 preload 成功。
- 回执字段规则与清洗函数移到不加载任何驱动的 `src/shared/visual-send-receipt.cjs`，发送器和日志模块共同引用。保留原有回执语义与脱敏，不修改发送、扫描或模型；便携包复制范围及已有包检查同步覆盖共享模块。
- 现有日志自检新增隔离依赖检查，旧依赖失败、修复后通过。实际 Electron 的测试版/正式版 preload 均无加载异常，自动回复、AI 专家、联系人与诊断方法存在，自动回复状态查询通过隔离 IPC 往返；诊断/控制器定向自检与 `npm.cmd run build:test` 通过。这部分为隐藏窗口、临时配置和测试状态下的验证，不连接真实微信。
- 2026-09-02 17:36 已重启本地测试版，启动检查确认窗口正常且自动回复保持暂停；随后用户自行测试并明确确认“验收成功”，授权收尾提交。本次验收属于当前开发运行时，不代表安装包、异机或全部四态场景均已验收。

### 2026-09-02 自动回复发送后误报修复（用户本机验收通过）

- 用户确认 17:04:46 报 `visual_send_outcome_unknown` 的回复实际已发送。旧日志未保留确认子步骤，不能断言该次具体失败分支；当天 11 次成功均依靠草稿消费证明，未依靠气泡 OCR。隔离模拟复现旧流程先做四轮气泡 OCR、期间输入控制权变化后完全跳过空草稿读取的误报路径。
- 发送后立即检查已精确核验的草稿是否被消费，并要求同一窗口与未中断的输入控制权；通过即结束确认。读不到时才使用有界、只读的气泡核验；控制权变化后不重新获取控制权操作输入框，备用核验须重新证明目标会话。鼠标恢复和截图释放属于清理，不推翻已确认结果。
- 发送前目标/草稿检查、模型、消息扫描和防重发策略未修改。未知仍保持 `outcome_unknown`，不自动补发；不把点击完成、读取失败或历史故障改写成成功。
- 回执阶段、结果码、草稿读取阶段、窗口内目标确认、读取/清空状态、输入控制权、气泡确认和次数贯通发送器、控制器、故障状态、统一日志及诊断 ZIP，只保留固定枚举、布尔值和有界计数，不保存客户或回复正文。
- 生产 PowerShell 确认函数的隔离回归、发送桥接注入检查、控制器/诊断定向自检及 `npm.cmd run build:test` 通过；启动依赖修复并重启后，用户于 2026-09-02 确认本轮本机验收成功。未提供本轮消息条数或持续时长，不据此宣称长时间稳定性和多客户并发已验收；未制作或发布安装包。

### 2026-09-02 自动回复漏消息根因修复（真实截图回放通过，连续会话实机待验收）

- 最新运行记录只有一条客户消息进入模型并完成 `sent_verified`；后续前台截图扫描仍判定 `latest_role=assistant / no_unread_message`。没有对应的模型空输出或输入失败事件，故本次漏回复发生在消息读取之前。
- 真实截图离线回放确认：整窗 OCR 成功但遗漏底部两条客户气泡，原解析把较早的绿色回复当作最后消息。现复用聊天区域高对比度 OCR 补齐遗漏行，保留原图中的发送方判断与绿色回复；读取失败不再当作空会话推进基线。
- 视觉候选、核验与恢复改为传递连续客户消息批次；主进程按客户接续已回复历史和未发送上下文。启动旧消息边界贯穿后续扫描和核验，真实重复气泡不会因同文被整体去重。末尾发送目标、串行微信操作及 `outcome_unknown` 不补发约定不变。
- 脱敏扫描日志新增区域读取来源、边界来源及位置、整窗与补充行数、气泡数、输入批次数、末尾位置与区域 OCR 成功状态；成功候选同样记录，未观测到的绑定状态不再误写为 false。不保存客户原文或资料正文。
- 三张用户真实截图离线回放：两张原先误判已回复的截图均找到两条连续客户消息；一张末尾确实已回复的截图仍保持 assistant、空待回复批次。现有视觉驱动/控制器定向自检及 `npm.cmd run build:test` 通过。未调用模型、操作真实微信或重新启动应用；不把离线结果等同于连续会话和多客户实机验收。

### 2026-09-01 自动回复红点绑定、悬浮进度与可见诊断（源码/构建通过，真实微信复测待完成）

- 用户实测显示测试版点击了所选联系人的未读红点，但没有进入 AI 或发送。脱敏运行证据只有 `start_requested → baseline_ready → started → paused_by_user`，没有候选、模型或发送事件；根因是红点兜底仍标记为 `messageDriven`，而测试联系人严格范围会在进入模型前拒绝所有 `messageDriven` 候选。
- 测试联系人红点路径现先打开会话，再将聊天页标题与该联系人的唯一别名做精确匹配，并在第二帧重复确认；只有两次绑定均成立才把候选升级为具名会话并继续回复。不匹配、缺失或歧义继续保持零模型、零发送；发送器原有精确会话、草稿、按钮归属和 `outcome_unknown` 门禁未放宽。
- 自动回复新增独立置顶悬浮窗和脱敏实时阶段，展示启动检查、监听、发现消息、生成、发送、核验、暂停/错误、阶段耗时、测试联系人、最近结果和今日回复数。启动后主窗口隐藏；暂停后浮窗保留状态；关闭或返回主页面会先暂停自动回复再恢复主窗口。持续监听不伪造整体完成百分比。
- “日志诊断”页面现独立读取当前及轮转的 `auto-reply-diagnostics.jsonl`，最多展示最近 50 条严格白名单事件；自动回复链路、近期异常和近期运行事件分别展示，不再互相隐藏。候选在联系人范围前被拒绝时也会留下匿名 `reply_candidate_rejected / conversation_not_eligible` 终止码；客户原文、联系人明文、专家资料和 Key 不进入页面或 ZIP。
- 自动回复控制器、红点视觉驱动、视觉发送、诊断 IPC 等定向自检，以及完整 `npm.cmd run check:self` 和 `npm.cmd run build:test` 均通过；测试 renderer 转换 1605 个模块。320×420 Electron 实际渲染截图已检查，关键状态和按钮无截断。本轮没有启动真实自动回复、调用 DeepSeek 或发送微信消息；仍需由用户对明确授权的测试联系人完成“红点→生成→草稿→发送核验”的实机复测。

### 2026-08-31 AI 专家两资料与四态自动回复（源码/构建通过，真实微信四态待验收）

- AI 专家已拆分为“专家规则”和“业务知识”两个必填资料槽，继续支持 `.txt / .md / .docx`、单文件 5MB 和两份解析正文合计 5 万字符上限。旧单文件专家会原文迁移为专家规则，业务知识待补齐；两份资料齐全才显示就绪并允许启动，自动回复运行期间禁止替换或删除。
- DeepSeek 自动回复合同已收敛为 `action / reply / reasonCode` 三个字段和 `answer / clarify / handoff / silent` 四种动作。一般行业问题默认由 AI 回答；产品参数、公司价格、库存、售后政策和承诺只可依据业务知识；“问题较难”不能单独触发转人工或静默。
- 每个客户独立保存一次澄清流程和人工接管状态：澄清后下一条实质消息不能再次追问；`handoff` 只暂停该客户并进入员工待处理列表，其他客户继续由 AI 服务。“已处理，恢复 AI”会清除该客户旧上下文、澄清状态、待办和积压消息，避免恢复后补发。
- DeepSeek 空输出或格式错误只允许一次结构修复；仍失败，或发生超时、网络、Key、余额、过滤等系统故障时，客户零发送、不伪造人工接管、暂停整个自动回复任务，并在界面显示脱敏错误码和原因。现有 `sent_verified / outcome_unknown`、草稿校验、会话校验和防重复发送边界保持不变。
- 自动回复诊断增加匿名追踪编号，可串联消息发现、模型决策和发送结果；诊断 ZIP 只纳入脱敏后的结构化字段，不包含客户原文、联系人明文、两份资料正文或 DeepSeek Key。
- 定向 AI 专家、DeepSeek、自动回复与诊断自检、完整 `npm.cmd run check:self` 和 `npm.cmd run build:test` 均通过；测试构建共转换 1605 个模块。代码审查发现的在途 handoff 恢复竞态、视觉会话身份漂移、崩溃恢复状态丢失和草稿人工复核丢失接管均已修复并复核通过。本轮未调用真实 DeepSeek、未发送微信消息，也未执行安装版或真实联系人四态验收；后者只能使用明确授权的测试联系人完成。

### 2026-08-29 测试版 AI 精修修复与发布边界

- AI 精修现会在 APIMart 返回 provider task ID 和结果 URL 时增量、原子落盘；页面刷新或服务重启后只继续查询原任务、下载原结果和本地拼装，不会新建付费订单。恢复接口后台执行，同一任务只允许一个恢复 worker；损坏图片必须通过 Pillow 完整解码，否则保持 `recovery_required`。
- `outcome_unknown`、提交前取消、明确失败和本地恢复失败已分开处理。只有桌面付费账本明确记录同一个 `task_id` 为 `outcome_unknown` 时，状态文件丢失的 404 才会进入人工核对解锁；普通 404 不允许解除付费锁。
- 产品文案和标题增加前后端统一长度与空值校验；产品参考图读取失败会在任何付费调用前失败关闭。DeepSeek 输出继续按不可信 JSON 校验，数字参数、单位、认证和商业承诺不得凭空改写；真实本机路径不会进入 prompt。
- Python 定向回归全部通过：最大组合为 `230 passed, 73 subtests passed`，P1 收口对应套件为 `113 passed`；整套源码 self-check 与测试版 renderer 编译通过。上述验证均为 mock/离线验证，本轮没有调用真实 APIMart/DeepSeek 生图、没有产生费用，也没有发送微信消息。
- 本轮目标是从本次干净提交重建并覆盖安装无签名测试版；安装包身份、哈希和构建提交只以随包版本清单为准。第二台电脑上的联系人同步、自动回复、主动触达和 AI 精修仍待异机验收；真实付费生图如需再验收，必须另行明确授权。
- 正式版前还需收敛未完整验收的朋友圈和占位入口，改进首次启动准备页、安装版专用说明、微信路径原子保存和跨崩溃运行锁；这些不阻断本次内部测试包，但在完成前不能宣称正式多设备或完整商用就绪。

### 基础能力矩阵（更新至 2026-09-02）

| 板块 | 已确认事实 | 尚未完成 |
|---|---|---|
| 微信拓客 | 联系人同步、自动回复、主动触达已有本机单项实测；逐帖互动和单条图文发布已有用户确认。今日计划、统一调度与五个页面简化已完成源码、测试／正式 renderer 编译和隔离 Electron 检查 | 新版完整串联、客户优先后续跑、图文／视频与聊天往返、定时插入、跨日／重启、不同 DPI 和异机仍待真实微信验收；旧单项结果不能替代新版整条流程验收 |
| 日志诊断 | 统一诊断日志及自动回复独立脱敏链路的轮转、查看和 ZIP 导出源码已实现；诊断页分别展示自动回复链路、近期异常和近期运行事件 | 新界面和导出结果仍需在本次源码构建或后续安装版本中由用户手动复验，不能沿用旧正式包结论 |
| 产品详情图 | CSRF 登录、显示比例、预览裁切、隐藏模块恢复、结果下载和历史结果恢复均已修复；DeepSeek/APIMart Key 使用 Windows `safeStorage` 加密；后台保留单任务账本、provider task/result 增量断点、非付费恢复和 `outcome_unknown` 防重复扣费；用户此前已用本人 Key 成功生成并下载 AI 精修结果，本轮 mock/离线回归与编译通过 | 本轮未执行新的真实付费生图；新测试安装包仍需本机安装冒烟和第二台电脑验收，正式版前再按发布门禁收敛未验收入口 |
| 内容生产 | 原片原地索引、断点任务、语义片段、长课程精剪、三段式批量混剪和受控预览保持可用；六套语义模板继续决定内容结构，三套 Remotion 视觉风格通过版本化组件注册表决定表现；素材驱动“一键混剪 V2”现为素材解析、5 项内引导填写、百炼 AI 脚本确认、可选 1 张场景辅助图、TTS/字幕双轨、授权选曲、动态混音、局部重做和失败关闭；横版素材会完整保留在竖版画布中。2026-08-27 已以 5 段真实素材完整跑通一次百炼脚本、实际配音、授权音乐和 Remotion 成片（48.818 秒、1080×1920、H.264/AAC） | 仍未用干净提交重建内部评估包，也未完成 60～90 秒手机盲测；补图只可由用户明确点击并确认可能计费后生成，不能充当产品事实；旧库存没有持久化动效计划，不能执行公平的零付费 3×3；Remotion 商业许可、浏览器再分发依据、5+5、30 条混剪、跨素材 B-roll、AutoCut/FunClip 对比和渠道发布仍待完成 |
| 渠道运营 | 已明确首期采用“总部生成发布包、学员本人手机确认发布”的合规路径 | 学员与账号、发布任务、AI 检查、回执、线索回流和数据复盘目前仍主要是界面占位或尚未建设 |

事实边界：

- 本节只描述截至 `2026-08-03` 的可验证状态，不把方案、页面占位或已选择的组件计为已实现能力。
- 产品详情图外部源码基线见 `docs/ai-huoke-2/product-detail-source-baseline.md`；当前可宣称“产品详情图已集成，离线工作台、显示与隐藏恢复、单击生成、真实 AI 精修、结果下载到桌面和安装版用户验收均已闭环”。尚未单独执行的净机测试不得外推为所有 Windows 环境均已验证。
- 产品详情图已接入桌面安全配置：用户只填写 DeepSeek 与 APIMart Key，密钥由当前 Windows 用户的 `safeStorage` 加密，安装包不携带真实 Key。DeepSeek 固定使用官方 `https://api.deepseek.com/v1/chat/completions` 与 `deepseek-v4-flash`；APIMart 固定使用官方 `https://api.apimart.ai/v1` 与 `gpt-image-2`。两把 Key 均可用时才开放 AI 精修；开始前会显示预计 `8–15` 屏、可能产生多次 APIMart 计费并要求明确确认。前端 busy 锁、后台原子单任务账本、provider 断点和 `outcome_unknown` 状态继续阻止双击、并发或结果不明时自动重提。离线契约与用户此前使用本人 Key 的真实生成均已验证，但本轮没有再次调用付费接口。
- 2026-08-03 收尾前主工作区约 `29.34 GiB`，主要由多轮运行时备份、旧发布包、解压目录和测试中间物构成；这些都不是用户下载体积。用户真实验收通过后已完成授权清理，主工作区降至约 `2.60 GiB`，最终安装器为 `326,841,584` 字节（约 `311.70 MiB`）。
- 内容素材底座只索引用户原文件并生成可重建的元数据；不会复制、移动或删除原片。ffprobe 未进入正式运行时，不能把开发机可分析误写成净机可分析。
- 微信真实发送和平台发布的验收继续遵守明确测试账号、明确对象及结果未知不重发的安全边界。

### 2026-08-13 创作工作台 AI 长课与批量混剪

- Electron 创作工作台新增“长课程精剪”和“AI 批量混剪”两个入口，统一经过素材导入、代理/音频/关键帧/转写分析、AI 生成、成片卡片验收和内部队列；渲染进程只接触素材与成片不透明 ID，预览由 `xiaoxi-content://` 受控协议提供。
- 内容引擎新增可复用衍生文件、`MediaSegment`、创作项目、候选成片、渲染配方和内部发布队列表；百炼 Key 沿用当前 Windows 用户 `safeStorage` 加密保存，只在内容引擎子进程启动时注入，原片仍留在原位置。
- 指定素材 `DJI_20260725151537_0029_D_A001.MP4` 已在开发机做真实 8 秒精确区间渲染冒烟，输出经 ffprobe 确认为 H.264、AAC、1080×1920、30fps、8.000 秒，并人工查看封面确认有真实课堂画面和烧录中文字幕。该证据只覆盖渲染器，不覆盖百炼选段质量或整批验收。
- 当前环境没有 `DASHSCOPE_API_KEY`，也没有固定版本的 AutoCut/FunClip 隔离环境，因此不能生成并评价计划中的 5 条真实课程候选、30 条真实混剪，也不能完成两条开源路线的同素材评分。现已按画面证据支持课件聚焦与课堂全景画中画，但跨素材 B-roll 自动编排、保守删除内部口头语仍未达到计划中的完整效果，继续作为后续实现与实机验收项。
- 本轮不自动发布到微信、抖音或快手，不虚构课程效果、宣传数据或学员评价；固定运行时与对外发布包必须在代码提交后从干净工作树重建。
- 2026-08-13 用户已用本人百炼 Key 和指定 25 分钟课程原片完成首轮 5 条真实课程候选渲染；链路可运行，但用户明确判定内容选择重复、旧评分失真、字幕过大且不具备发布质量，因此本轮验收结论为“功能起步可用、成片质量未通过”，不能把 5 条生成成功写成课程精剪验收通过。
- 针对实测反馈，课程选段已改为候选转写文本的百炼主编复评（开头吸引力、独立价值、完整性、语言质量、主题相关性），并增加源时间重叠和文本相似硬去重；成片卡片公开分项分数与推荐理由。字幕改为 1080×1920 明确画布上的短语级动态 ASS，同时保留对齐 SRT，支持字号与底部距离配置，默认不再把整句铺满画面。
- 第二轮已用相同真实素材生成 5 条 57～63 秒成片，源区间不再完全重复，任意两条的最高时间重叠约 23%；人工抽帧确认字幕由旧版四行大字改为单行短语并移至底部。但当次百炼文本主编请求网络失败，第二轮使用的是本地内容信号，不得把它标作 AI 推荐质量通过。当前已取消静默降级：百炼主编不可达时明确失败，不再以本地评分冒充 AI 推荐。71 个内容引擎回归、完整源码 self-check 与 renderer 构建通过；百炼主编在线后的同素材内容质量仍需用户再次实看验收。

### 2026-08-14 创作工作台一键网感包装第一版

- 正式工作台继续只保留“长课程精剪”和“AI 批量混剪”两个入口；SupoClip 仅作为设计参考。已把有效能力收敛为六套版本化本地模板：课程 `knowledge_focus`、`slide_teacher`、`classroom_value`，混剪 `hook_impact`、`process_rhythm`、`result_close`。
- 生成接口支持自动、指定模板和不包装三种模式，并接入品牌包、主辅色、字体预设、图片素材 Logo、老师参考照和约 0.8 秒片尾签名。旧配方不含 `recipe_json.packaging` 时仍走原渲染路径；批量自动模板受控轮换，不连续堆叠同一效果。
- 本地包装已接入低位逐词动态字幕、标题卡、轻推近、关键词/步骤/结果卡、受控语义贴纸、分段课件/老师画中画、课堂宽景、Logo、课程提示音、混剪程序化自有音色 BGM 和人声压低。六套模板的滤镜图均不同；`slide_teacher` 已用指定真实课程素材完成 6 秒非付费冒烟，输出为 H.264/AAC、1080×1920、30fps，并抽帧确认老师—课件画中画—老师按相对时间切换。课程音频实测约 `-16.8 LUFS / -2.1 dBFS`，混剪约 `-16.9 LUFS / -2.4 dBFS`。旧配方仍可按原规则从源素材取本地帧，避免历史任务恢复时意外产生付费调用。
- 正式工作台只提供 AI 封面：只要启用包装，每条新成片固定计划 1 次 APIMart 图片调用，界面在生成前显示预计总数；不再向用户提供免费本地封面选项。图片模型只生成无文字背景，中文标题和 Logo 在本地准确叠加。同一 planned/submitted 操作重复点击、取消、重启或下载/合成失败后只恢复原任务并复用 `provider_task_id`，不得再次提交。提交结果不明进入终态 `outcome_unknown`；“换包装”复用既有 AI 封面，不调用百炼或 APIMart，“重做封面”才新增恰好 1 次付费调用。本地付费账本使用 SQLite `synchronous=FULL`，但 APIMart 当前没有已验证的请求幂等键，因此机器在网络提交瞬间断电的极端边界仍必须进入人工核账，不能宣称跨断电绝对 exactly-once。
- APIMart Key 继续由 Electron `safeStorage` 管理，仅以子进程环境变量注入；IPC、日志、数据库和公共结果不返回明文或本地真实路径。百炼生产客户端固定官方 origin；认证重定向、公共下载地址、响应大小和图片内容均有边界校验。品牌 Logo 和参考照必须是已导入的图片素材不透明 ID。
- 离线包装资源已有版本化 `ASSETS.json`：随内容引擎打包 OFL-1.1 的 Noto Sans SC 中文字体及许可证；贴纸、BGM 和提示音为本项目程序化原创资产，不在线抓取第三方素材。资源文件进入内容引擎源码哈希和 PyInstaller 固定运行时。
- 本轮 Python 全量 `138 passed`，桌面 `npm.cmd run check:self` 与 `npm.cmd run build:test` 通过；只执行假 HTTP 的 APIMart 自动化测试和本地真实 FFmpeg 冒烟，没有发送真实 APIMart 请求，也没有重新产生百炼费用。另已用指定课程原片生成六套模板的同帧对照图，供用户确认模板方向；该对照图不是 AI 封面。5 条课程片＋5 条混剪片、30 条混剪、手机端“至少 3 条可直发”及干净提交安装包仍是后续实机验收项；跨素材课程 B-roll 自动编排尚未完成，不能把本轮写成最终发布质量验收通过。

### 2026-08-15 Remotion 事件驱动动态包装与正式工作台接入

- 用户明确否定第一版六套静态对照图的审美方向后，新增隔离的 Remotion 开发试验，不替换现有 FFmpeg 正式渲染链。试验采用“内容理解决定什么时候出现什么，版本化设计包决定如何出现，Remotion 负责逐帧稳定实现”的结构；同一份语义事件清单可渲染 `social_pop`、`neo_editorial`、`tech_motion` 三种完全不同的运动语言。
- 三套试验均使用指定真实课堂原片的同一段 15.68 秒完整观点与原声，包含词级动态字幕、钩子、关键词、场景、结果、焦点框、本地程序化提示音及 SVG 图标；社交款另含弹性卡片和手绘表情，新锐杂志款使用大字号编辑排版，科技款使用网格、扫描线和 HUD 信息卡。事件只在对应语义时间出现，不全程堆叠无关特效。
- 三条真实 MP4 已输出到用户素材目录，均为 H.264/AAC、1080×1920、30fps、15.744 秒；人工抽查开头、过程和结尾共 9 帧无乱码、无黑帧，音频实测约 `-15.9 LUFS`、True Peak `-3.9 dBFS`。原始视频未移动、覆盖或删除。
- 随后已用当前加密保存的百炼 Key 对同一真实片段执行 1 次 `qwen-plus` 动效编导请求，生成带真实文本、时间、语义区域、尺寸、优先级和理由的版本化计划；Remotion 使用该计划重新渲染真实样片。百炼不能输出任意像素坐标，模型计划会经过最多同屏两项、重复文案去除、主体/课件保护区和固定网格约束。
- 正式课程与混剪任务已接入同一百炼批量编导合同：每个任务最多一次批量请求，结果在渲染前写入任务配方；暂停/恢复和换包装复用计划，提交结果未知时禁止自动重提。返回文案还需通过原转写字符依据校验，避免把无依据的宣传说法直接烧进视频。
- Remotion 现已接入普通课程/混剪生成和已有成片三风格对照：FFmpeg 先生成无视觉包装的 9:16 中间片，Remotion 叠加字幕与语义事件，再无损复用中间片音频。普通任务只在能力或本地瞬时故障时可见回退 FFmpeg；对照任务不允许回退冒充三种风格。取消与关机不会触发回退，并可终止活动 FFmpeg 进程。
- 动效不是三段硬编码成片模板：版本化注册表当前提供 7 个组件族、每族 6 个变体，共 42 个允许列表内的组合单元；AI 只决定有素材依据的事件、文字、时间和表现意图，本地布局网格决定安全区域与尺寸。新增组件不需要改课程/混剪业务流程。
- 开发运行时固定 Remotion `4.0.512` 及 Windows x64 原生依赖，使用显式本机 Chrome 完成 composition smoke；打包态忽略环境路径覆盖，installer 还要求独立 RSA-SHA256 信任记录绑定便携包整树和运行时摘要。商业许可、浏览器再分发依据和干净提交未满足时仍禁止生成交付安装器。
- 用户指出 `social_pop` 左上角空心黄色圆圈无意义后已移除，并用原百炼计划本地重渲染，不产生第二次云端调用。新样片仍需用户播放确认；Remotion 商业许可与依赖审查、多段真实内容手机端质量验收仍未完成。
- 当前内容引擎全量 `188 passed`，桌面完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 通过；开发 Remotion 运行时已按最终源码重建并验证。只读库存盘点确认 15 条旧成片的视频和封面都存在，但有效持久化动效计划为 0，因此真实 3×3 输出为 0/9，实际百炼、APIMart 和本地新渲染调用均为 0。该门禁结果不等于手机端审美验收通过。

### 2026-08-17 真实素材验收门禁与费用预检

- 生成前新增分项云端预检：分别列出百炼语音识别、画面理解、课程选段评分、动效编导和 APIMart AI 封面预计次数；已有分析缓存会显示为不重复调用。用户确认后才提交生成任务，取消确认不会提交云端任务。
- 高质内测默认先走 Remotion-only。Remotion 不可用时任务明确失败，不把 FFmpeg 回退结果算作首条验收样片；普通旧任务仍保留可见 FFmpeg 回退。
- 新增手机验收记录（绑定成片摘要、设备、验收人和时间）。三风格对照现在同时要求：实际 Remotion 成片、已完成并有供应商回执的 AI 封面、当前手机验收通过、原动效计划仍完整；对照本身仍为 0 次百炼、0 次 APIMart、3 次本地渲染。
- 混剪配方新增可复核的 `skeleton_id`，项目结果公开已识别的骨架数量；混剪界面增加 1 条、5 条、30 条试跑档位。任务恢复会带回项目 ID，避免重启后只看到孤立任务。
- 本轮内容引擎测试 `191 passed`，桌面 `npm.cmd run check:self` 和 `npm.cmd run build:test` 通过；尚未提交真实百炼/APIMart调用，也未宣称手机实机验收通过。下一步是用户确认第一条云端预检后，用指定 DJI 素材做一条 Remotion-only 成片并记录手机结果，再进入三风格和 5+5 批量验收。

### 2026-08-24 素材驱动一键混剪 V2（源码实现与本地验证）

- 新建 V2 项目只接受 `specVersion`、素材 ID、标题和文案框架；后台固定为素材适配、智能口播、仅 TTS、授权自动选曲和单条输出。成片最长 120 秒，合格素材不足时自然缩短，不循环镜头、不慢放凑时长、不补静音尾巴，也不生成 AI 画面。旧 V1 项目、传统课程和批量混剪数据继续按原路径读取，没有迁移历史记录。
- 当前 V2 分析版本使用固定 5 秒视觉质量窗口：一次 FFmpeg 以 0.5 fps 解码 64×64 灰度证据帧，并另取最多 12 张代表图用于内容理解与缩略图；黑屏、严重模糊、冻结、无意义、未测量和单样本窗口均失败关闭。245 秒回归覆盖 49 个完整窗口，全部至少有两个区间内样本；显式空多帧签名不再回退单帧哈希。恢复和局部重做会核对素材指纹、分析版本与配置摘要，版本漂移后重建全下游，不能读取“最新旧分析”冒充当前结果。
- 用户框架和素材事实先生成同一语义时间轴，再派生口播短语、真实 TTS 字幕、标题/卖点/CTA、镜头、停顿和音乐段落。`speechCaptions` 只对应实际合成音频，正式配方拒绝 `timing=estimated`；`visualTextItems` 只允许 hook、callout 和 CTA。每个口播窗口按 `evidenceRefs` 使用对应且未重复的源区间，callout 只能出现在其证据镜头内，完整文案框架不会直接铺上画面。
- TTS 固定使用私有版本化 persona 目录映射到百炼 CosyVoice 3.5 Plus；内置自然生活、可靠商务、沉稳叙事、轻松抽象四种声音设计模板，但不硬编码供应商音色 ID。首次使用由用户明确点击“生成声音”，百炼返回的私有音色 ID 只保存在内容引擎；同一次声音设计返回的 WAV 直接进入“试听→批准”安全门。主进程还要求当前应用会话实际取得合法 WAV 后才允许批准，重新设计、批准尝试和应用重启都会清除这次试听资格。提交、响应或本地落盘结果不明时持久化为 `outcome_unknown`，刷新和重启后都不会自动重提。每句口播仍按实际 WAV 时长拼接并由 ASR 核对品牌词、数字和关键卖点；素材原声在 V2 正式渲染中静音。
- 音乐通过受管授权曲库导入，保存来源、商用范围、有效期、授权证据、BPM、情绪、能量、LUFS、True Peak 和循环点；音频与授权证据复制到受管目录并在选曲、恢复和完成前复核摘要。无有效授权曲目时进入 `needs_attention`。混音先分别归一化人声与音乐，再按真实说话窗口和素材能量曲线动态压乐，正式闸门要求综合响度 `-16～-14 LUFS`、True Peak `≤ -1 dBTP`、说话窗口人声高于音乐 `8～12 LU`。
- 产品页只保留素材、标题和文案框架，并提供“换声音、换音乐、重做文字”三个局部重做入口；每个可能触发云端/资源变更的操作使用独立可信点击令牌。正式 V2 要求 Remotion 成功，禁止将 FFmpeg 视觉回退标成完成；输出合同为 1080×1920、H.264/AAC、30 fps。
- 最终本地验证为：V2、声音资源与供应商安全专项合计 `75/75`，内容引擎全量 `336/336`；“本次会话必须先试听再批准”的主进程门禁通过顺序、伪 WAV、重新设计、重启和一次性消费测试。内容引擎 IPC、sidecar、桌面集成和产品页自检通过，完整 `npm.cmd run check:self` 通过，`npm.cmd run build:test` 成功，Impeccable 页面检测无问题，Python `py_compile` 与 `git diff --check` 通过；限定范围只读终审无残留可操作问题。这些证据只覆盖源码、确定性夹具和本机 FFmpeg/renderer，不包含真实百炼声音设计/TTS 调用、用户批准的实际音色、真实授权曲目复现参考片、手机 4/5 盲测或安装包验收。
- 当前工作树仍高度 dirty；本轮失败的沙箱全测新增了 90 个受 ACL 影响的 `tmp*` 测试目录，已在核对根路径和时间范围后全部清理。此前工作树中的其他未跟踪内容未动；本轮未提交、未构建对外安装包，也未从 dirty checkout 发布。

### 2026-08-26 一键成片引导式脚本与竖版适配（源码实现与本地验证）

- 一键成片页面不再要求用户填写长篇“文案框架”。用户先解析所选素材，再填写视频标题与最多 5 项信息：公司名称、产品或服务、应用场景、最想表达的一句话、需保留或避开的信息；产品名称为唯一必填项。脚本仅由已配置的百炼或 DeepSeek AI 生成；供应商不可用时明确失败并保留填写内容，不以本地模板冒充 AI 脚本。
- 已确认的脚本才会进入正式 V2 成片，并作为口播字幕、画面文字和声音生成的唯一文本来源。素材时长会在脚本请求前推导口播字数上限；素材过短时在 TTS 前停止并提示补充素材，避免先调用配音再发现无法承载。
- 横版素材在 V2 中使用“完整前景 + 模糊延展背景”的 1080×1920 画布，竖版素材保持全屏；这是对桌面参考片排版的实现，不代表已完成参考素材的真实云端成片或手机验收。应用关闭、重启、暂停或取消期间的引导任务会与会话状态原子同步；进行中的外部结果变为 `outcome_unknown`，不会自动重提。
- 本轮定向验证通过：`69` 项 V2 Python 测试、产品页/IPC/sidecar/桌面集成与受控媒体协议自检、`npm.cmd run build:test` 与 Impeccable 页面检测。验证没有发起真实百炼脚本/TTS、授权音乐、付费补图或成片渲染；这些仍需在用户明确确认成本与素材/版权范围后进行真实验收。
- 可选 AI 补图只会为当前已确认脚本生成 1 张 9:16 场景辅助画面，用户必须明确勾选可能计费并点击；其独立账本会在提交成功前持久化，任务恢复只查询原供应商任务，结果未知绝不自动重提。补图不允许文字、Logo、人物、数字或未经证实主张；仅在成片最后最多 3 秒作为非事实辅助画面，真实素材仍承担产品展示和卖点依据。

### 2026-08-27 素材驱动一键混剪 V2（真实素材开发运行时闭环）

- 用户提供的 5 段清洁机器人素材完成全量解析：可用画面 1 分 23 秒、18 个片段。用户填写的产品信息经过百炼生成脚本；系统没有使用本地脚本文案兜底。
- 同一任务实际完成配音、字幕时间对齐、授权音乐选择、音量平衡和 Remotion 渲染。最终文件为 H.264/AAC、1080×1920、约 48.818 秒；本机播放已确认画面、声音和字幕均存在。
- 本次还修复了 Remotion 子进程兼容当前 Chrome 的问题，并把浏览器环境的必要系统变量加入受控子进程允许列表；修复后的真实任务不再因旧 `--headless=old` 或子进程缺少 `PATH` 而中断。
- 该记录不是商业发布或干净安装包验收。后续打包必须从干净提交重新构建 sidecar 和完整浏览器运行时，并以内部评估或商业许可记录决定制品类型；不复制用户密钥、素材或当前任务数据库进入安装包。

### 2026-08-27 微信链路门禁、朋友圈回读与诊断包修复（源码验证）

- 自动回复和主动触达此前以 Windows 会话级最近输入时间要求桌面连续静默 15 秒；RPA 自身的聚焦和点击也会刷新该时间，因而会被误判成“人在操作”。当前仅移除动作前的整机静默门禁；精确 PID/HWND、联系人及会话身份、动作期间输入变化检测、发送结果未知不补发均保持不变。主动触达原有 8～15 秒联系人间节流继续保留。
- 本机最近一轮主动触达实际为 4/4 `sent_verified`；界面中的旧“未触达原因”来自 2 个在任务创建前因缺少公开微信号而排除的联系人，不是 4 人任务失败。界面现改为“未进入本次任务”，明确排除项不计入触达失败。
- 朋友圈发布后仍要求人工确认的原因是严格结果回读仅等待约 1.4 秒。当前改为最长约 12 秒的严格证据轮询，仍不使用“编辑器关闭＋页面刷新”的弱成功兜底；期限内读到唯一完整帖子会自动完成，否则继续进入人工收口且绝不二次发表，并记录轮询次数、耗时和最后拒绝原因。
- 诊断包导出不再依赖外部 PowerShell `Compress-Archive`，改由打包内 Node 依赖生成、校验后替换 ZIP；失败不会先删旧包，摘要不再包含用户绝对路径，并列出各日志文件的大小与 SHA-256。环境快照只进入安全摘要，故障恢复后会追加终态记录，减少只保留失败造成的误导。
- 用户提供的异机包来自 `20260827T0653Z`，仅包含 3 条完整统一事件：朋友圈动作前 `powershell_timeout`、联系人同步 `capture_timeout_wx_hook_then_memory`、自动回复 prime `powershell_timeout`。该旧包缺少 commit、微信版本、hook 子阶段和运行环境，不能据此证明当前代码已在该设备通过；当前已补 PowerShell 超时耗时/终止字段、联系人 hook 阶段与配置能力摘要。源码全量 self-check 和测试 renderer 编译通过，但本轮未执行真实微信发送、真实朋友圈发表或第二台物理电脑验收。

### 2026-08-03 产品详情图闭环（生成与下载均由用户真实验收通过）

- 用户当前安装版已经可以完成桌面 bootstrap、登录并进入工作台，先前的 `The CSRF session token is missing` 已不再出现，因此 CSRF 登录问题已由用户复验闭环。
- 本轮截图中的“大块空白”来自外层 iframe 只有最小高度、没有填满剩余页面；中央预览又固定为 750px，宽度不足时被裁切。源码现改为 iframe 填满剩余区域，详情预览和模块主图按中栏可用宽度等比缩放；宽屏首次打开默认使用“预览”布局，已手动选择的布局仍会保留。
- 以前点击“隐藏”只改变当前页面 DOM，不会永久删除模板，也不会在重启后继续隐藏。现在每个已隐藏模块都有“恢复显示”，并提供“全部恢复显示”。
- 没有出现在本次预览里的模板与“已隐藏”是两种状态：前者通常是资料不足，被空模块规则过滤。界面现会明确提示“资料不足未生成”，避免用户误认为模板被删；不使用未经确认的默认宣传填充空模块。
- 桌面本地解析现可从用户明确填写的适用场景和含数字参数生成对应场景/KPI 模块，并修正 VS、品牌、客户案例、价值计算、视频封面等空模块判定；不会调用 DeepSeek，也不会虚构认证、售后或客户案例。
- AI 精修不是管理员权限功能。桌面“API 密钥”中分别保存 DeepSeek 与 APIMart Key 后，两把 Key 齐全才允许新建任务；打开页面、读取本人历史结果和普通模板预览不会自动提交付费请求。点击“AI精修”后会先显示预计屏数与计费提示，用户明确确认后才开始；同一时间只允许一个付费任务，快速双击和并发请求不会重复提交。结果不明时进入 `outcome_unknown`，优先恢复原任务；本地状态丢失时必须人工核对并明确解除，不能静默重提。
- 当前产品详情图 Python 全量回归为 `550 passed, 1 skipped, 134 subtests passed`，APIMart 与付费账本专项为 `20 passed`；本地无头 Chromium 回归通过。自动化测试阶段拦截了付费接口，随后用户使用本人 Key 完成真实 AI 精修生成并验收通过；本任务未执行真实微信发送、点赞、评论或发布。
- 2026-07-31 本地无头 Chromium 双视口回归通过：`1440px` 宿主下 iframe 内容宽 `1090px`、缩放 `0.7053`；`1920px` 宿主下 iframe 内容宽 `1570px`、缩放不低于 `0.99`。生成、单个隐藏恢复、全部恢复和重启恢复均通过；导出 PNG 为 `2,196,564` bytes，SHA256 为 `d6a7d882bac2a0fd8183658957ad1ade4f2ca372c09abe691d9842ebbf52b901`；无外部请求、无付费请求、无控制台或页面错误。
- AI 精修结果过去通过受保护 URL 直接下载，Electron 下载请求没有带上 sidecar 的分区登录会话，因此服务端返回 401，表现为后台已生成但桌面没有文件。当前改为页面带会话取回文件，再由 Electron 限定来源和图片类型后保存到桌面；重名文件自动增加序号，界面显示保存中、已保存或失败状态。
- 最终覆盖升级安装器包含显示修复、隐藏恢复、单击直生成、AI 安全配置和桌面下载修复。2026-08-03 用户使用自己的 DeepSeek/APIMart Key 成功生成 AI 精修结果，安装新包后确认详情图可以直接下载到桌面；产品详情图至此完成验收和收尾。

### 2026-08-05 内部开发版启动与本地组件回归修复（源码/运行组件通过，用户界面待验收）

- 用户进入产品详情图时看到“运行组件未配置”，进入素材仓库时看到“素材底座未就绪”。两个运行目录实际都存在；开发运行时解析器把组件构建提交 `40fcf89...` 与当前全仓库提交 `cff2995...` 做严格相等比较，朋友圈等无关提交也会让组件失效。
- 提交区间核对显示内容引擎源码未变化，属于纯误伤；产品详情图的 `templates/workspace.html` 确实在构建后变化，旧组件应当失效。解析器现要求构建提交必须是当前提交祖先，并且只比较对应 sidecar 源码目录的已提交差异和当前未提交差异：无关提交不再阻断，组件自身变化仍保持阻断。
- 产品详情图运行组件已从当前干净 sidecar 源码重新构建并通过自检；旧运行目录和清单没有删除，分别保存在 `.build/product-detail-runtime.backup-20260805-1028` 与对应 backup manifest，可恢复。素材底座沿用原已验证运行组件。
- 额外创建的桌面 `启动AI获客测试版.bat` 已撤回。唯一开发入口继续使用仓库根目录 `启动内部开发版.cmd`；入口只预检整机启动必需的 renderer 与 Electron。产品详情图、素材底座按模块独立校验和降级，单个可选组件不可再阻断联系人、自动回复、主动触达或朋友圈。实际页面状态仍由用户启动后验收。

### 2026-08-07 旧版诊断日志回归与启动门禁修复（源码/构建通过，用户界面待验收）

- 对用户提供的两份 `20260730T0717Z` 旧版诊断包逐项回归。旧版 `wechat_window_not_found`、朋友圈 pane 识别、输入框粘贴失败和旧任务跨构建恢复，当前源码已有精确 PID/HWND、内嵌/弹窗双形态、输入租约/回读和旧构建任务隔离修复；`outcome_unknown`、`atomic_draft_changed` 继续作为防重复发送保护保留，不能因旧日志而放宽。
- 产品详情运行时文件、清单和哈希均有效，实际误判来自 `docs/` 下说明图片变动被当作运行源码变化。产品详情构建与开发 resolver 现共享同一源码范围：顶层 Python、`ai_refine_v2`、`pubsub`、`static`、`templates`；文档、测试和缓存变动不再使组件失效，真实运行源码变动仍会阻断旧组件。
- `启动内部开发版.cmd` 不再把产品详情图或素材底座当作整机硬门禁，只保留 renderer 与 Electron 两个启动必需检查；可选组件缺失时由各模块独立显示不可用，不影响微信拓客和朋友圈。
- 主动触达诊断不再把 `API_KEY_MISSING/API_KEY_INVALID` 固定话术降级记为顶层错误，也不再把用户暂停或正常批次暂停记为故障；真正的 `blocked`、`outcome_unknown` 仍以实际工作流原因进入错误列表。完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 已通过，真实界面由用户通过唯一 BAT 入口验收。

### 2026-08-13 主动触达启动静默期修复（用户真实微信复验通过）

- 交付版 2026-08-12 日志显示，主动触达连续两次在联系人搜索前暂停；同一台机器上的自动回复随后在 `1400×950`、120 DPI（125%）微信窗口完成 `sent_verified`。因此失败与大窗口 OCR 无关，两条链路的差异发生在主动触达独有的窗口预处理阶段。
- 根因是用户点击“启动触达”会刷新 Windows 最近键鼠活动时间，而主动触达约 2～3 秒后立即要求桌面已连续空闲 15 秒，必然先得到 `wechat_user_active`；上层又优先显示统一的“窗口未固定到左上角”文案，掩盖了真实原因。
- 当前保留 15 秒防止与用户抢键鼠的安全门禁：首次仅因 `wechat_user_active` 失败时等待静默期并重试一次，等待期间任务仍可暂停；窗口身份、前台状态、联系人精确搜索、发送授权、结果确认和未知结果不重发均未放宽。其他窗口错误不重试。
- 任务状态和统一诊断日志现保留精确 `blocked_reason`，已知错误码优先映射为对应提示，不再被通用窗口文案覆盖。主动触达专项、IPC 专项、完整 `npm.cmd run check:self`、`npm.cmd run build:test` 和 `npm.cmd run build:delivery` 均通过；用户随后使用真实微信完成触达并确认修复有效。该结论覆盖当前账号和机器，不外推为异机验收。

### 2026-08-13 朋友圈互动锚点与同屏队列收口（用户真实微信部分验收）

- 点赞路径改为共享的结构互动锚点：在 DPI/视口动态计算的右侧通道识别两点、留白和头像归属；执行前只验证预演位置附近的局部锚点、头像、窗口和点击归属，不再重做整屏帖子 OCR。评论仍只对选中帖子读取正文。
- 一次观察保留同屏全部安全候选并从下往上处理；单条局部锁定失败不会立刻滚动，本屏候选完成后才滚动。连续两个新屏幕没有已验证动作时结束，不再使用“目标数 × 5”持续下滑。
- 用户已用真实微信确认朋友圈识别与单条发布可用；评论仍有轻微已知问题，因此本次只把识别/发布和已观察到的点赞动作记为 live 证据，不把评论链路写成完全验收。
- 朋友圈视觉、几何、动作和 campaign IPC 专项及完整 `npm.cmd run check:self`、`npm.cmd run build:test`、`npm.cmd run build:delivery` 均通过；不同 DPI、长批次和评论已知问题仍需后续专项验收。

### 2026-07-31 朋友圈启动唤醒修复（当前安装已观察通过一次，计时复验待完成）

- 旧版 09:51 日志曾出现 `daily.initialize → daily.run_due → campaign.open_started`，确认是“错过计划时间后在下次启动立即补跑”导致微信和朋友圈被前置。
- 当前安装版在本地时间 15:36:24 只记录 `daily.initialize → daily_startup_deferred`，没有 `daily.run_due`、`campaign.started` 或 `campaign.open_started`，因此本次启动没有自动打开朋友圈，启动修复已获得一次真实安装观察证据。
- 启动约 19 秒后出现一轮独立 `campaign.started` 并完成每日目标 1/1；日志没有 `daily.run_due`，不能把这轮独立任务认作启动自动补跑。随后另一轮目标 2 完成 2/2，处理 5 条、跳过 3 条，`outcome_unknown=false`。
- 当前运行配置仍是每日 `1` 条、09:00、只点赞、不评论，并不是用户期望的 10 条。目标 10 应在后续统一流程进入验收时通过正式设置写入，不能直接修改运行状态文件冒充完成。
- 最新任务稳定出现四类安全阻断：`moments_menu_ambiguous`、`moments_comment_editor_changed`、`moments_menu_surface_ambiguous`、`moments_post_changed`。对应记录均为 `real_action_attempted=false`，说明程序在无法确认菜单、编辑框或帖子身份时没有误点、误发。
- 当前源码继续把“启动状态恢复”和“到点执行”分开；用户确认“立即执行今日剩余”后才按当日额度和帖子去重继续，结果未知的动作不自动重试。
- 尚需完成计划到点、继续剩余、当天不重复和跨重启验证，才能把每日自动计划完整标记为已验收。朋友圈发布首版已经实现，但真实微信发布与结果核验尚未验收。

### 2026-08-03 朋友圈发布逆向调研与首版实现（源码/构建通过，真实发布待验收）

#### 2026-08-05 独立朋友圈编辑窗口续接修复（待用户 live 验收）

- 用户验收 attempt `50b40ac6-f407-414a-b13c-5df9b3d1f29e` 已成功把 1 张确认素材送入微信，随后在 `stage=file_dialog` 以 `moments_window_not_foreground` 停止；状态明确为 `action_attempted=false`、`outcome_unknown=false`，没有填写正文、没有点击“发表”，不存在重复发布风险。
- 现场 Win32 证据表明微信 `4.1.11.55` 在文件选择完成后创建了同一 Weixin PID、由主微信 HWND 直接拥有、标题为“朋友圈”的独立顶层编辑窗口。旧驱动却再次校验主微信 HWND 必须位于前台，因此把微信正常的窗口交接误判成失焦；这属于旧版窗口模型遗留的技术债，不是图片上传失败。
- 独立编辑窗口不暴露可写 UIA 子控件。当前驱动改为先保留主窗口锁，再唯一重绑定同 PID、同 class、精确 owner、精确标题且位于前台的朋友圈编辑窗口；正文采用该窗口内受控点击后 `SendInput` Unicode 输入，并通过同窗 OCR token 回读。该做法与只读逆向得到的 `dt-ai-helper`“锁定发布窗 → 键盘输入正文 → OCR 定位发表”核心路径一致，但未复制对方代码。
- 文件清单哈希、私有快照、最终点击前 marker、同一“发表”按钮二次 OCR、点击后完整帖子核验和结果未知不重发均保留。针对性发布驱动检查和完整 `npm.cmd run check:self` 已通过；真实发表成功仍必须由用户重启开发版后快速验收，不能以源码自检替代。
- 14:18 的后续验收 attempt `d4cb7577-9534-4629-a323-95a151ae22b9` 在 `moments_open` 阶段以 `moments_discover_open_timeout` 停止，`action_attempted=false`、`outcome_unknown=false`。截图证明微信稍后已显示“发现”选中、“朋友圈”选中和真实内容区；根因是点击“发现”后只允许 2 轮昂贵视觉检查，慢帧尚未完成二级菜单 OCR 就提前返回。现将同一共享、有界检查预算扩为 6 轮，识别成功仍立即返回，不增加固定等待；窗口/PID/归属、唯一入口及最终发布门禁均未放宽。

- 只读分析对象为本机已安装的 `dt-ai-helper 1.8.37`。其 Electron renderer 负责计划编辑和 HTTP/WS 调度，`app.asar.unpacked/resources/main.exe` 是 PyInstaller/Python 3.12 本地执行器；朋友圈发布核心不是微信私有协议，而是 `uiautomation + Win32 + 截图/OpenCV 模板 + OCR + 剪贴板/文件对话框`。原安装目录未修改，也未启动真实发布。
- 对方执行链为：打开并捕获标题“朋友圈”的独立窗口，按 DPI 找顶部相机入口，向系统文件对话框写入最多 9 个文件，输入正文，滚动并 OCR 精确定位“发表”，点击后等待“正在处理”消失。其 `main.exe` SHA256 为 `7EE0C4728AA3BD20823497391CBCF7BECE41825A0C267E2E0115A7CC7E65006D`，`app.asar` SHA256 为 `850391B12849ADBFC037409264F7CB6635BF28FA1DAB75531547D7BCC0696585`。
- 对方实现不能直接照搬：顶部模板失败后会退化到窗口相对 `(70,25)×DPI` 点击；单个文件异常可能被忽略；点击“发表”后只等待处理提示消失或 15 轮超时，既不确认编辑器关闭，也不回读新动态，仍可能把任务标成成功。
- 当前项目复用了已有朋友圈窗口适配器和 Windows OCR，没有复制对方代码或图标模板。相机入口优先使用 UIA；DPI 相对锚点只在局部像素签名成立时作为集中式适配 fallback。文件对话框必须同时匹配微信 PID、`#32770`、打开标题和朋友圈窗口 owner；发布按钮必须位于已锁定 composer 的底部动作区，优先由唯一绿色动作候选定位，视觉不唯一时才允许底部局部 OCR 唯一精确识别“发表”。正文只允许写入 render pane 内唯一、可见且占据编辑区尺度的可写 `Edit + ValuePattern`；编辑器必须先为空，使用 `SetValue` 原子替换，并同时做完整值回读和编辑区 OCR token 回读。
- renderer 只能看到 opaque 素材选择 ID、文件名、大小和类型，得不到绝对路径；正文文本和原始/暂存媒体绝对路径不写入状态、marker 或诊断日志。发布指纹由标准化换行后的精确正文及有序媒体 `sha256/size/ext` 生成，正文或任一文件变化都必须重新确认。
- 用户必须先点击“生成发布确认”，再次核对摘要和指纹后再点击“确认发表”。页面卸载或重挂后，旧确认会失效并要求重新核对，不能用主进程内残留草稿盲确认。确认时先把素材复制到按 attempt 隔离的私有快照目录，复核副本哈希/大小/类型，并只把副本交给微信；不可逆点击前要求每个素材分别对应一个可见、位于 composer 内的叶子 UIA 对象，正文写入后和最终点击前都必须重新绑定相同 runtime identity，聚合/隐藏伪证据不能放行。
- 实际不可逆点击前，执行 PowerShell 在 `data/moments/publish_markers/` 原子落盘 marker；marker 持久化后还会重新核对前台 HWND、PID、命中点、外部输入 tick，并在同一 composer 底部动作区重新观察和唯一重绑定同一“发表”按钮，之后才允许点击。PowerShell 进程绑定精确父进程句柄；终止未确认时全局微信运行锁保持占用直到子进程真实退出。重启遇到遗留 `publishing` 或孤立 marker 一律恢复为 `outcome_unknown`，不再把“暂未看到 marker”当成可安全重试。
- marker 后中断、点击失败、子进程终止未确认或发布后证据不足时统一进入 `outcome_unknown`，同一指纹不得自动重发，只能由用户在微信中核对后明确标记“已发表”或“未发表”。自动成功要求连续两帧定位到同一条完整朋友圈 post candidate，并在该同一候选内同时证明完整标准化正文、逐项唯一媒体、精确“刚刚”，同时证明 composer 已消失且 feed 相对发布前发生变化；短正文前缀、任意头像或全窗口零散证据不再算成功。
- 首版有意要求至少 6 个可 OCR 核验的中英文数字字符，并只接受 1–9 张 JPEG/PNG 图片或单个 MP4/MOV 视频；纯图片朋友圈暂不放行，因为当前没有足够可靠的唯一正文锚点证明是否已发布。
- `npm.cmd run check:self`、`npm.cmd run build:test` 和 `npm.cmd run build:delivery` 已通过；这些结果只证明源码契约和 renderer 构建。尚未生成便携包，也未在微信 `4.1.11.54` 做真实发布。本机 live 验收必须覆盖图片、视频、100%/125%/150% DPI、系统文件对话框 owner、唯一正文成功回读，以及故意制造核验失败后的 `outcome_unknown` 不重发。

### 2026-08-04 首次授权实机发布流程（点击前安全阻断，兼容修复待再次确认）

- 本机实际 `Weixin.exe` 为 `4.1.11.55`。用户明确授权一条含 154 字正文和 1 张图片的真实发布；2026-08-04 09:35:58–09:36:08 的 attempt `93bc4250-eeaa-4846-a952-69b009ae32c3` 在打开朋友圈阶段以 `moments_window_open_timeout` 结束，内容指纹前缀为 `0501e39b2ec1`。
- 运行态明确记录 `action_attempted=false`、`outcome_unknown=false`；`publish_markers/` 和 `publish_staging/` 均为空，微信编辑器和“发表”按钮均未操作。因此本次只验证了点击前安全阻断，不能记作发布成功或结果未知，也不得自动补发。
- 实机证据显示该补丁版本把朋友圈嵌入标题仍为“微信”的同一主 HWND；旧导航器只等待标题为“朋友圈”的独立顶层窗口。`dt-ai-helper` 静态逆向的新版入口同样包含 `mmui::MainWindow` / `Chrome_WidgetWin_0` 内嵌面板回退和 OCR“朋友圈”证据，进一步确认应适配页面形态而不是依赖单一窗口标题。
- 当前源码已为发布链路增加 `standalone` / `integrated` 双模式：内嵌模式保持原 HWND/PID/进程和捕获到的标题/class 精确重绑定，并在任何相机点击前要求整窗左侧栏 OCR 唯一精确识别“朋友圈”且同一行具有微信绿色选中背景；相机 UIA 与像素 fallback 仍只相对 `MMUIRenderSubWindowHW` 右上角定位。marker、最终按钮重识别、发布后同帖核验和 unknown 不重发链路未放宽。
- 新增/修订的导航、发布驱动和 IPC self-check 已通过，完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 已通过。修复后的同一正文和素材已重新生成确认，但再次真实发表必须取得新的最终确认；发布成功、编辑器/媒体兼容和 `outcome_unknown` 仍未完成 live 验收。
- 2026-08-04 的后续 attempt `c68b1245-8e60-4050-9af0-840dcdb02e82` 已越过新版内嵌朋友圈相机识别，但在文件选择框枚举阶段以 `moments_publish_driver_failed` 安全结束；运行态为 `action_attempted=false`、`outcome_unknown=false`，没有 marker，也没有重复发布风险。
- 离线执行生产文件框函数确认这是既有 PowerShell 技术债：`[ref]$pid` 与只读自动变量 `$PID` 冲突；修复后同一回调中的候选列表 `$matches` 又会被正则自动变量 `$Matches` 覆盖。两处现已分别改为 `$candidatePid` 和 `$dialogCandidates`，并由真实执行回调的离屏 Win32 `#32770` 合成回归覆盖；不是微信 `4.1.11.55` 本身直接导致。
- 发布终态现在只额外保存受控的 `stage`、`failure_kind`、`exception_category`、`exception_type`，方便用户快速实机验收后直接按日志定位；异常消息、堆栈、正文和素材绝对路径仍不落盘。修复后针对性检查、完整 `npm.cmd run check:self` 和 `npm.cmd run build:test` 均通过，真实发表仍保持“待用户验收”。
- 2026-08-04 19:29 的用户验收 attempt `89ec3d72-ee6b-4be7-8404-d8b4a44c20b9` 在 `openMoments` 导航阶段以 `moments_user_input_detected` 结束；耗时 11.464 秒，`action_attempted=false`，没有 marker。该 reason 与发布驱动的 `moments_publish_external_input_detected` 不同，可确定尚未进入图片选择、正文输入或最终发表。
- 根因是会话级 `GetLastInputInfo` 被错误当成全流程租约：旧导航在耗时视觉扫描前取 tick，并要求“发现→朋友圈”的被动等待期间始终不变；旧发布驱动又把相机点击后的 tick 跨文件框、媒体处理、正文、滚动一直带到最终发表。本程序自身的 camera click / wheel 也会推进该 tick，因此既会误判用户正常鼠标移动，也存在把自身延迟输入误判为外部竞争的竞态。
- 当前门禁已按阶段收口：导航点击只绑定最新视觉帧的 tick，被动轮询每次用新帧重建证据；文件框靠唯一 owner/PID/class/title/foreground 和文件值回读，正文靠唯一可写编辑器、runtime identity 及完整值/OCR 回读，滚动靠前台 HWND 与 owned point。仅最终不可逆“发表”点击在 marker 前后保留短窗口 input tick，并在 marker 后重新观察和重绑定同一按钮、重取该新证据的 tick；错窗、错对象、错内容、重复发布与 unknown 不重发边界未放宽。
- 新失败若仍发生在打开朋友圈阶段，状态会记录 `stage=moments_open`、`failure_kind=navigation_result`；不记录正文、路径或原始 tick。收口后导航/发布/IPC 针对性检查、完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 均通过，等待下一次用户 live 验收。
- 2026-08-04 19:49 的后续 attempt `d3456aa9-8e7f-4af0-a66d-341ada206503` 用时 22.627 秒后返回 `powershell_timeout`，持久化阶段为 `moments_open/navigation_result`。其中约 15 秒是导航 PowerShell 的精确硬超时，其余为窗口准备；`action_attempted=false`、`outcome_unknown=false`，marker 与 staging 均为空，可确定仍未进入选图、正文或最终发表。
- 根因是新版内嵌导航沿用了旧版独立窗口的 `16 × 250ms` 轮询次数，但轮询体已从轻量窗口枚举变成整窗截图与 OCR，外层却仍只有 15 秒。当前每次“发现展开”或“朋友圈选中”最多读取 2 个新视觉帧，并把仅用于防止 PowerShell 卡死的 open 硬上限集中为 45 秒；正常识别立即返回，不固定等待上限。滚动、窗口身份、最终发表 marker、幂等和 unknown 不重发门禁均未放宽。
- 时间预算修复后，导航/发布驱动/发布 IPC 定向检查、完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 再次通过。真实微信发布仍须由用户执行下一次快速验收，不能用自检冒充成功。
- 2026-08-04 20:00 的 attempt `ed43aa18-cf64-4e33-9da3-42e5bdac45c8` 已越过新版内嵌导航和相机入口，并由微信实际弹出系统文件选择框；运行态确认本次已有 1 张图片、125 个正文字符。驱动在 `file_dialog` 阶段返回 `moments_publish_file_dialog_missing`，`action_attempted=false`、`outcome_unknown=false`，没有 marker，尚未写正文或点击发表。
- 截图和代码比对确认根因是微信 `4.1.11.55` 的中文文件框标题为“选择文件”，而驱动只接受“打开/Open”前缀，因此同一个微信 PID/owner 下的真实 `#32770` 被过滤。系统文件框本身是微信添加本地图片的正常步骤；AI 获客应自动填写已选择素材的私有快照路径，用户无需再次把路径交给 Codex，也不应在驱动已经停止后手工继续该残留文件框。
- 文件框标题兼容现集中为“打开/选择文件/Open”三个已知前缀；微信 PID、精确主 HWND owner、`#32770` class、前台窗口、文件名 ValuePattern 写入与完整回读、唯一“打开(O)”按钮和点击前再次租约核验均保持不变。新增的离屏 Win32 可执行回归证明“选择文件”可进入租约检查，同时“另存为”等无关公共文件框仍被拒绝。导航/发布/IPC/renderer 定向检查、完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 均通过，等待下一次用户 live 验收。
- 2026-08-05 的用户验收 attempt `e70f3324-2bdf-43c2-aa51-b172bde533fb` 已进入微信真实“选择文件”窗口，但旧驱动从全部可写 `Edit` 中按纵坐标和宽度全局评分，误把文件列表内联重命名框当成底部“文件名”输入框；完整 Windows 路径中的 `\`、`:` 随即触发系统“重命名”非法字符提示。终态为 `moments_publish_file_name_set_failed`、`stage=file_dialog`、`action_attempted=false`、`outcome_unknown=false`，没有点击微信发表，也没有重复发布风险。
- 文件名定位现改为先取得唯一“打开/Open”按钮，再按标准文件名身份及与该按钮的底部操作区几何关系筛选可写输入框；顶部搜索框和文件列表内联重命名框均不能再仅凭位置较低而胜出，候选同分时安全返回歧义。针对性回归、完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 均通过；真实朋友圈发表仍保持“待用户验收”，不以源码自检冒充 live 成功。
- 2026-08-05 的下一次用户验收 attempt `655248fb-254a-411a-ad80-008d8cf590fc` 已越过文件名输入框定位，但 Windows 11 公共文件框向 UI Automation 暴露了多个名为“打开/Open”的可调用节点；旧代码错误要求全棵 UIA 树只能有一个，因而以 `moments_publish_open_button_ambiguous` 在 `file_dialog` 阶段停止。终态仍为 `action_attempted=false`、`outcome_unknown=false`，没有点击微信“发表”。
- 2026-08-05 的后续用户验收 attempt `1df89126-f55f-4727-a9c3-187a34dd7bce` 已正确打开微信“选择文件”窗口，但旧实现只扫描 UI Automation 的可写 `Edit`，而 Windows 标准文件框可将 `cmb13`（控件 ID `1148`）作为“文件名”组合框外壳暴露，导致以 `moments_publish_file_name_field_missing` 在 `file_dialog` 阶段停止。终态为 `action_attempted=false`、`outcome_unknown=false`，未点击微信“发表”，没有重复发布风险。
- 文件名定位现改为使用 Windows Explorer-style 文件框的标准原生控件契约：只接受 `cmb13`（`1148`/`ComboBox`）或 `edt1`（`1152`/`Edit`），通过原生 `WM_SETTEXT` 写入已选素材的私有快照路径并完整回读，不再全局扫描搜索框、地址栏或内联重命名框，也不依赖 UI Automation 是否暴露组合框内部的可写节点。离屏 Win32 回归已证明存在干扰 `Edit` 时仍只绑定 `1148`、成功写入完整路径并逐字回读；完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 均通过，真实发表仍等待用户重启内部开发版后验收。
- 2026-08-05 用户随后验收 attempt `17235fac-2745-4267-b3b5-f3ad6cd31b52` 证明上述“标准控件是 `#32770` 直接子项”的假设仍不成立：真实微信文件框底部输入区可见，但 `GetDlgItem(1148/1152)` 均未返回符合直接子控件约束的目标，仍以 `moments_publish_file_name_field_missing` 停在 `file_dialog`。终态为 `action_attempted=false`、`outcome_unknown=false`，未点击微信“发表”。
- 经用户授权只读逆向 `dt-ai-helper` 的 PyInstaller Python `3.12` 运行组件，`scripts.automation.actions.MomentPublishManager.PYQPublishAction.choose_file` 的真实字节码显示其核心并不遍历文件框控件：对素材路径执行 `pyperclip.copy(file)`，随后依次 `auto.SendKeys('{CTRL}{V}')` 与 `auto.SendKeys('{Enter}')`。当前实现据此改为文件框键盘入口：在精确微信 PID/HWND/owner/title/前台租约和标准“打开”按钮身份成立后，用 `Alt+N` 聚焦截图中明确标注的“文件名(N)”、`Ctrl+A` 覆盖、Unicode 键盘输入私有快照路径、`Enter` 确认，并要求原文件框在两秒内关闭；不再依赖 Windows 11 私有后代控件树，也不占用或覆盖用户剪贴板。真实发表仍待下一轮用户验收。
- 对 `dt-ai-helper 1.8.37` 的 PyInstaller 归档补充只读逆向已直接定位 `PYQPublishAction.choose_file`：对方从任务的 `filePath` 取路径，逐项执行 `pyperclip.copy(file) → uiautomation.SendKeys("{CTRL}{V}") → SendKeys("{Enter}")`，并不枚举或唯一识别“打开”按钮。当前项目只复用“把已选路径交给微信原生文件框”的核心原理，不照搬依赖焦点的全局盲按键。
- 当前文件框提交改为绑定标准 `#32770` 对话框的 Windows 原生 `IDOK=1` 子控件，同时核对 child/visible/enabled/control-id/class/title；文件名仍通过唯一可写 ValuePattern 精确写入和完整回读，提交前仍复核同一微信 PID/HWND owner、文件框身份和前台租约。最终使用有界 `BM_CLICK`，不再受同一按钮的重复 accessibility wrapper 影响，也不增加坐标硬编码。
- 发表前安全失败现在只作废已经消费的发布确认，原始文件未变化时保留内存中的素材选择与正文，用户可直接重新生成确认而无需再次选图；只有素材变化、不可用、格式失效或选择过期才清空素材。确认发表仍保留为真实发布授权。新增错误文案不再把内部 reason 裸露给用户。定向发布驱动/IPC 回归、完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 已通过；真实发布结果继续等待用户快速验收。
- 2026-08-05 18:59 的用户验收 attempt `9a7e996d-6d1d-4cca-9f7b-08a0122e57ac` 使用构建 `20260805T1039Z`，在 `prepublish_verification` 以 `moments_publish_button_not_found` 停止；`action_attempted=false`、`outcome_unknown=false`，marker 为空，确定没有点击绿色“发表”。同一失败截图按当时生产绿色识别器离线重放，能稳定得到唯一候选 `{left:82, top:624, width:140, height:38, greenRatio:0.957}`；Windows 全窗 OCR 对白字绿底的“发表”则为零命中。因此按钮颜色阈值和继续增加滚动次数已被排除，旧日志缺少 live 每帧候选证据才是无法进一步唯一归因的直接诊断缺口。
- 对 `dt-ai-helper 1.8.37` 的只读逆向进一步定位到 `PYQPublishAction.publish_moment`：对方先用 `moment_person_icon` 锚定独立编辑窗结构，再裁剪窗口相对 `360×130` ROI，内置 OCR 严格匹配“发表”，最后把 ROI 内坐标换算到窗口坐标点击；不是全屏 OCR，也不是裸屏绝对坐标。当前实现只复用“已锁定 composer → 局部按钮观察 → 相对坐标”的核心原理，未复制对方代码或图标，并继续保留前台 HWND/PID/owner、点击点归属、最终 marker、二次按钮重绑定和 unknown 防重发边界。
- 当前按钮阶段已拆成专用快速观察：每帧先扫描 composer 底部相对区域的绿色动作候选，唯一时不再先运行昂贵整窗 OCR；视觉不唯一时才对底部动作带做局部 exact OCR。旧版“纯 OCR 只允许右半边发表”的布局门禁已移除，仍要求底部区域唯一精确文字；滚动稳定等待对齐为 1 秒，抵达底部后再做 3 次不滚动的短暂复查。若仍失败，诊断日志只记录帧宽高、扫描线、视觉/OCR 候选数、候选相对 bounds 和 green ratio，不记录正文、图片或绝对路径。定向回归、完整 `npm.cmd run check:self` 和 `npm.cmd run build:test` 已通过，新开发构建为 `20260805T1147Z`；绿色“发表”的真实点击与发布结果仍必须由用户快速 live 验收，不能以离线重放或源码测试冒充成功。
- 2026-08-06 09:28 的用户复验 attempt `0d1b9002-81bf-4e06-b222-13b597639be7` 确认运行开发构建 `20260805T1147Z`，在 `prepublish_verification` 仍以 `moments_publish_button_not_found` 安全停止；`action_attempted=false`、`outcome_unknown=false`，未写 marker、未点击“发表”。新增诊断证明 13 个 scroll 帧和 3 个 settle 帧始终复用同一 `513×660` 裁片，且视觉/OCR 候选均为零；失败截图按生产规则离线却稳定得到唯一约 `140×36`、green ratio 约 `0.967` 的绿色按钮，排除了颜色阈值、OCR、等待时长、PowerShell 数组和 IPC 序列化。根因是文件框关闭后仅绑定一次 composer，随后长正文令微信编辑窗重新排版/改变几何，但按钮观察仍复用写入正文前的旧 `$lock.rect`；共享 `Get-MomentsVisualFrame` 只按传入矩形抓屏，不会自行刷新窗口。当前在正文写入完成后、每个 scroll 观察前和每个 settle 观察前重新绑定同一个 HWND/PID/owner/title/class/foreground 的 composer 实时矩形；没有扫描微信主窗，也没有放宽不可逆点击、marker 或结果未知防重发边界。对应回归先在旧实现上失败，修复后朋友圈发布专项、朋友圈视觉专项、完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 全部通过，新开发构建为 `20260806T0230Z`；真实发表仍由用户操作验收。
- 2026-08-06 10:41 的用户复验 attempt `ad7abbf7-94f2-4b9c-887a-946f5e91200a` 已首次完成绿色“发表”的真实点击，微信朋友圈顶部随后出现对应正文和图片，用户确认真实发表成功；旧结果裁决仍因新版独立 composer 只能提供 `visual_presence_only`、而自动成功硬性要求 `uia_one_to_one`，在 `postpublish_verification` 进入 `outcome_unknown`。当前只收窄这一发布后适配器门禁：独立 composer 路径可由发布前最终素材 manifest、实际加载后的媒体视觉收据、精确正文完整回读、durable marker，加上发布后 composer 消失、信息流变化、受控朋友圈 viewport 内唯一 24 字正文锚点和连续两帧同一 receipt 共同证明成功；锚点缺失/重复、viewport 无有效像素证据、素材收据缺失、编辑窗仍存在或 feed 未变化仍保持 `outcome_unknown`，不会自动补发。生产 PowerShell 决策正例和六组反例、朋友圈发布专项、完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 均已通过，新开发构建为 `20260806T0306Z`；自动结果确认仍待用户快速 live 复验。
- 用户使用构建 `20260806T0306Z` 又完成多次真实发表，最新 attempt `03230b68-0803-453c-9362-34f7bb9e7676` 的微信结果同样由用户确认成功，但长文和图片令完整 post candidate 的菜单行/“刚刚”不在同一可见 viewport，新增正文锚点 fallback 仍在约 58 秒后进入 `outcome_unknown`。这证明“发布后必须重新拼出完整帖子”本身是错误作用域的门禁，而非继续调 OCR 即可解决。当前自动成功改用微信客户端提交回执：点击前的完整正文、最终素材 manifest、媒体加载和 durable marker 全部保留；点击后最多 4 轮、每轮 350ms，只要求同一主微信/朋友圈 surface 恢复、独立 composer HWND 消失、页面像素相对基线变化且未出现上传/发布/发送/网络失败或重试提示，首帧成立即返回 `verified`。完整帖子重建保留为诊断函数，不再阻断主流程；编辑窗仍存在、页面未变化、surface 失真或失败提示仍进入 `outcome_unknown` 且不自动补发。生产 PowerShell 客户端回执正例和错误提示/页面未变/composer 未关/surface 错误反例、发布专项、完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 已通过，新开发构建为 `20260806T0327Z`；客户端回执自动成功仍待用户快速 live 复验。
- 2026-08-06 用户使用开发构建 `20260806T0327Z` 完成真实单条朋友圈发布验收：attempt `906e7e4d-fa48-4874-a19b-cfd980fbf0a3` 在点击微信绿色“发表”后自动进入 `verified / moments_publish_verified`，阶段为 `postpublish_verification`，`action_attempted=true`、`outcome_unknown=false`；用户同时在微信端确认内容确已发表。全程无需人工选择结果，也没有重复发布，朋友圈单条发布及其自动结果确认在微信 `4.1.11.55` 上验收通过。
- 2026-08-06 11:48 的新版连续互动复验 run `218ef935-4977-4b31-9e21-9b074fe180dd` 暴露长帖局部对齐回归：首轮滚动后已唯一识别目标，位置证据为帖子顶部约 `22%`、三点菜单约 `96%`，但 `target_partial_visible` 路径错误复用了翻到下一帖的 `-540` 大步滚动，将头像滚出视口；后续视觉识别要求头像和菜单同屏，因此连续返回 `moments_post_not_found`，最终在 4 次下滑、0 次动作后安全暂停。只读逆向 `dt-ai-helper 1.8.37` 的 `MomentMarketingAction._execute_limited_moments_marketing` 证明其随机互动按当前视口重新分组头像与菜单，处理结束或菜单坐标失效时使用单次 `{DOWN}` 小步前进，并不把局部对齐等同于整页翻帖。当前实现据此把滚动语义集中拆为 `advance_feed=-540` 与 `align_partial_post=-240`；只有已经锁定且菜单靠近底部的当前帖使用小步对齐，普通翻帖、精确窗口/页面/点击点归属和用户输入中断规则保持不变。定向回归、完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 已通过，新开发构建为 `20260806T0424Z`；真实点赞评论仍待用户快速复验。
- 2026-08-06 13:31 的连续点赞复验 run `fab79fb6-e555-450f-9747-2ca69e2c3ba8` 已真实完成并验证 2 个新点赞，证明局部对齐修复对短帖有效；同轮第 2 个候选因 `moments_menu_ambiguous` 在点击前安全跳过，随后遇到长视频帖时又在 8 次下滑后以 `moments_post_not_found` 暂停。现场截图与日志共同证明新版长帖会让头像/正文头部与底部三点菜单永远无法同屏，旧视觉模型把“头像与菜单同屏”当成所有帖子的必要条件，因此继续调整固定滚动距离无法根治。当前仅为纯点赞流程增加稳定菜单兜底：只在同一朋友圈窗口、连续两帧存在稳定可见菜单而没有完整帖子候选时锁定最下方菜单，实际动作前再次按窗口、菜单坐标和菜单像素哈希重锁，点击后仍通过菜单 `赞 -> 取消` 状态变化确认；AI 评论继续要求完整正文，不使用此兜底。菜单兜底遇到已点赞只记跳过，不计入目标，避免同一长帖重复凑数。专项回归、完整 `npm.cmd run check:self`、`git diff --check` 与 `npm.cmd run build:test` 均通过，新开发构建为 `20260806T0609Z`；跨屏长帖连续点赞仍待用户快速 live 复验。

### 2026-08-04 微信 4.1.11.55 内嵌朋友圈互动适配（只读预演通过，真实互动待验收）

- 只读实机观察确认新版主窗口的可访问结构为 `Window("微信") → Pane("Weixin") + Pane("MMUIRenderSubWindowHW")`，朋友圈不再依赖标题为“朋友圈”的独立顶层窗口。观察时微信停留在聊天页，右半边被微信开发者工具遮挡，因此本轮没有把该画面冒充朋友圈 live 验收，也没有执行真实点赞、评论或发布。
- 共享 surface profile 现同时支持 `standalone=朋友圈` 与 `integrated=微信`。内嵌页面证明使用整窗左上区域，要求 OCR 行唯一精确等于“朋友圈”且该行绿色选中背景比例达标；不再错误假设 `MMUIRenderSubWindowHW` 一定覆盖左侧发现菜单。帖子识别、滚动点、点赞菜单、评论编辑器、发送按钮和发布相机仍必须位于实时唯一 render pane 内。
- campaign 打开微信后会固定 `surfaceMode/PID/HWND/title/className`，每次 dry-run 都经 Base64 参数传到 CLI 和 probe；probe 生成的完整窗口与 render pane 身份继续绑定 observation v6，动作前重新核对 title、class、窗口矩形、root、render pane runtime/bounds 和内嵌页面证明。无效或损坏的 expected-window 参数立即阻断，不能退化为扫描任意微信窗口。
- 新版导航只接受唯一一个标题精确为“微信”的主窗口；多开或多个候选直接返回歧义，不按面积猜账号。OCR 入口点击、DPI fallback 和滚动在输入前复核命中 HWND/PID、窗口 title/class/矩形、前台状态与外部输入 tick；用户并发操作时安全停止。
- visual action 将 post/menu/avatar、弹层、评论 composer 和发送按钮限制在 live render pane；独立 popup 还必须由精确主 HWND owner 持有。页面或 pane 改变发生在不可逆动作前时返回 blocked；动作已经发生而结果无法确认时继续返回 `outcome_unknown`，不得自动补做。
- PowerShell 异步执行器在终止宽限期内未收到 child close 时会返回 `powershell_termination_unconfirmed`，并进入全局 quarantine，禁止启动后续 RPA；只有迟到的 close 得到确认后才解除，避免孤立执行器与新任务并发。
- 新增 standalone/integrated 双 profile、selected-green 页面证据、完整 expected-window 传递、render pane 越界、窗口篡改、正常/半露出滚动、无效锁和终止 quarantine 自检。`npm.cmd run check:self`、`npm.cmd run build:test` 与 `npm.cmd run build:delivery` 均已通过；这些结果仍不替代新版真实朋友圈验收。
- 下一次 live 验收前，用户需先把微信切到朋友圈并移开微信开发者工具等遮挡。只读识别通过后，再单独明确授权最多一条测试点赞和一条测试评论；未取得该授权前不得执行真实互动。

### 2026-08-04 微信窗口左上角统一锚定与前台交接（锚定与托盘恢复交互 live 通过）

- 对 `dt-ai-helper 1.8.37` 的补充只读逆向确认：其联系人触达、自动回复、朋友圈互动和发布都会先进入同一窗口初始化链，最终通过 Win32 `MoveWindow` 把微信移到 `(0,0)`，并按 DPI 使用稳定基准尺寸。这样做的核心是固定截图、OCR、模板和相对点击的坐标原点，降低窗口位置、尺寸与 DPI 漂移；不是微信协议要求。
- 当前项目把该原理收敛到共享微信适配层：精确锁定个人微信 PID/HWND 后，把主窗口锚到主屏工作区左上角，基准尺寸集中为 `880×560` 并按 `GetDpiForWindow` 缩放。联系人同步/主动触达、自动回复、朋友圈点赞评论和发布不再各自保存窗口坐标或重复判断版本。
- 后台计划默认要求系统连续空闲至少 15 秒才允许一次入口交接；入口完成后，截图、扫描和动作驱动均为被动核验。前台 HWND、窗口几何或外部输入 tick 任一变化就暂停/阻断，不再通过 `SetForegroundWindow`、`AppActivate` 或重复点击把微信抢回来；预演、检查或阻断结束后也不会自动把 AI 获客窗口拉到前台。长等待中的朋友圈下滑支持即时取消，停止后不会再多滚一次。
- 截图所示 `wechat_window_not_foreground` 的根因是用户点击本应用“检查/执行”按钮后，本应用自然成为前台，而旧动作又要求微信已经在前台。当前改为只接受该次可信按钮点击，并将控制权一次性交给安全预演冻结的同一微信 PID/HWND；交接后仍按 observation、窗口身份、帖子和输入租约逐项复核，不能扫描或切换到其他微信窗口。
- 自动回复视觉发送、主动触达搜索/草稿和发布文件对话框均绑定精确 PID/HWND 与输入租约；用户中途操作后停止且不自动补发。搜索与草稿流程只在剪贴板仍是自动化自己写入的值、输入租约未变化且精确微信仍在前台时恢复旧剪贴板；发现图片、文件或其他无法无损恢复的剪贴板格式时会在写入前阻断，避免覆盖用户内容。
- 14 组针对性检查通过；随后 `npm.cmd run check:self`、`npm.cmd run build:test` 与 `npm.cmd run build:delivery` 全部通过。该轮离线验证未操作真实微信、鼠标或键盘；这些结果不能替代新版微信左上角锚定、只读预演、一赞一评或发布的 live 验收。
- 2026-08-04 13:32 的首次只读验收不能作为当前源码结论：测试版 Electron 主进程启动于 09:51，而本节涉及的主进程/导航/共享窗口适配文件在 12:04–12:21 才更新，运行中的旧 IPC 仍会在按钮让本应用取得前台后直接启动 dry-run，因此以 `moments_window_not_foreground` 安全阻断。该次记录明确为 `real_action_attempted=false`，没有点赞、评论或发布；必须重启测试版加载当前源码后，再验收左上角锚定与只读预演。
- 2026-08-04 14:13 重启后进行第二次只读预演：微信从未运行状态启动后成功按共享布局固定到左上角，精确 PID/HWND、前台和稳定几何交接通过；随后因微信停留在普通聊天页、左侧二级菜单尚未出现“朋友圈”，导航以 `moments_entry_not_found` 安全停止。该结果确认锚定 live 通过，也暴露出新版必须先经过“发现 → 朋友圈”的两阶段入口；本轮仍未点赞、评论或发布。
- 对 `dt-ai-helper` 的补充静态逆向确认其发布与互动复用同一新版入口：先在一级侧栏做“发现”局部图标匹配，再在展开菜单中 OCR“朋友圈”，之后分别兼容独立窗口和内嵌窗格。当前实现只复用这一原理，不复制其 4.1.8 模板、右键或按键方案：使用本项目自有的 DPI 缩放图标像素门禁唯一证明“发现”，受控左击后重新绑定同一 PID/HWND/title/class/矩形/DPI，再要求 OCR 唯一精确命中“朋友圈”，第二次受控左击后必须看到唯一绿色选中态才算成功。
- 两次导航点击分别建立新的外部输入 tick 租约；任一阶段发生用户输入、窗口/前台变化、图标缺失或歧义、OCR 缺失或歧义时立即停止，旧 `Invoke-MomentsSidebarFallback` 仍不可进入 integrated 路径。补丁加入聊天页缺口、100%/125% DPI、稀疏/高填充/缺失/实心伪图标和租约顺序回归；最新 `npm.cmd run check:self`、`npm.cmd run build:test`、`npm.cmd run build:delivery` 均通过。由于主进程需重新加载导航脚本，最终“聊天页 → 发现 → 朋友圈”的只读 live 复验仍待下一次约 10 秒屏幕窗口。
- 2026-08-04 14:51 的第三次只读预演再次确认左上角锚定成功，但固定局部区域未能唯一证明未选中的“发现”指南针，返回 `moments_discover_entry_not_found` 并在首击前安全停止；没有点赞、评论或发布，测试版随后已退出，微信保持运行。该次失败只回传到界面，旧链路没有把候选像素指标写入诊断日志，因此不能把某一条阈值臆测成已证实根因。
- 当前后续补丁不再绑定“发现”的单个纵坐标：只扫描一级侧栏的窄主导航带，按活跃像素行分成多个图标候选，分别计算尺寸、填充、圆环、边角、对角线和绿色比例，只允许唯一符合指南针拓扑的候选进入受控点击；零个候选继续 `not_found`，多个候选按 `ambiguous` 停止。失败结果新增严格数值白名单诊断，不保存截图、原始像素、OCR 文本或联系人内容。行为测试覆盖纵向漂移、125% DPI 下固定像素尺寸、双指南针歧义、缺失/实心/密集伪图标和诊断字段脱敏；本轮 `npm.cmd run check:self`、`npm.cmd run build:test`、`npm.cmd run build:delivery` 与 `git diff --check` 均通过（后者仅有既有 LF/CRLF 提示）。下一次 live 可在失败时直接确认具体候选指标。
- 2026-08-04 15:17 的第四次只读预演在任何朋友圈点击前以 `personal_wechat_main_window_not_found` 停止；诊断事件明确记录 `real_action_attempted=false`，没有点赞、评论或发布。现场并非微信退出：个人微信 `4.1.11.55` 的真实主 HWND `201258` / PID `30076` 关闭到托盘后仍存在，但状态为 hidden、non-iconic；旧枚举在恢复分支之前就过滤了所有隐藏 HWND，因此把托盘状态误报为“未找到”。测试版随后已退出，微信进程保持原状。
- 无 exact HWND 的共享窗口自动发现现在必须唯一找到含 `MMUIRenderSubWindowHW` 后代的个人微信主窗；零个结构候选直接停止，多个结构候选按歧义停止，不再把同进程的可见图片查看器或其他大窗口拿去按面积决胜。托盘隐藏候选还必须同时满足主窗口几何、非空标题、`Qt*QWindowIcon`、无 owner、`WS_THICKFRAME` 且非 tool-window；这能排除现场同进程内尺寸达 `1536×808` 的隐藏托盘消息窗。恢复前、恢复后和最终布局后均重新证明同一 HWND/PID/process/title/class/owner/render-child/layout，恢复最多有界轮询 1 秒，任一证据变化继续安全停止。只读 inspect 探针已在大型托盘窗共存时唯一命中 `201258/30076`；完整 `npm.cmd run check:self`、`npm.cmd run build:test`、`npm.cmd run build:delivery` 与 `git diff --check` 通过，独立只读审查未发现剩余 P0/P1/P2。该探针刻意不显示微信，最终“托盘恢复 → 左上角 → 发现 → 朋友圈 → 生成预演”仍需下一次短 live 复验。
- 2026-08-04 15:41 的第五次只读预演确认托盘中的同一微信主窗已成功恢复并固定到左上角；随后在首个微信点击前以 `moments_discover_entry_not_found` 安全停止，测试版立即退出，没有点赞、评论或发布。数值诊断证明一级侧栏存在 4 个候选，未选中灰色“发现”是唯一具有明显指南针对角差的第 4 个候选（中心 `(47,323)`、`26×26`、`ringRatio=0.5122`、`diagonalContrast=0.2097`）。后续按真实 resolver 逐项复算确认，当时不只 `ringRatio>=0.52` 失败，固定中心理想值也会把 `centerX=47` 与 `expectedCenterX=37.5` 的 `9.5px` 差异误拒绝；先前“只有圆环阈值”这一归因不完整。
- 当时先把 `ringRatio` 下限从 `0.52` 收窄为 `0.50`，离线 resolver 临界正例及 dense、solid、缺失、纵向漂移和双候选反例均通过，但这只是一次临时适配，不能证明现场入口已跑通。16:34 的最新诊断再次得到 4 个候选，真实“发现”为中心 `(47,323)`、`24×24`、`ringRatio=0.32609`、`cornerRatio=0`、`diagonalContrast=0.19298`，说明继续调圆环阈值或中心容差只是在追逐微信抗锯齿和边框偏移。
- 2026-08-04 16:13 的第六次只读预演补充证明：第五次所谓“托盘恢复成功”只证明 Win32 HWND 已可见和几何正常，不能外推为微信客户区已可交互。现场仍出现右上角红叉残留，恢复后的同一 HWND `201258` 对中性客户区点击没有可见响应；本轮同样在任何朋友圈动作前停止，诊断记录为 `real_action_attempted=false`。先行尝试的 `SW_SHOWNOACTIVATE + SWP_NOACTIVATE` 单变量补丁没有消除症状，已完整撤回，反证问题不在提前激活本身。
- 当前源码改为复用本机微信开始菜单快捷方式的原生单实例激活合同：托盘隐藏且非最小化时，使用精确匹配进程的可执行文件和 `--scene=startmenu` 让微信自身恢复 Qt 窗口状态；只有真正最小化时才使用 `ShowWindowAsync(SW_RESTORE)`。前台交接也先请求同一原生激活，原 `AttachThreadInput` 仅保留为有界兜底；同一准备事务最多请求一次原生激活，精确进程路径不可用时直接停止，不回退到另一安装路径。没有增加新的业务门禁；同一 PID/HWND、恢复后身份和几何复核继续保留。`--scene=startmenu` 本身不能指定 PID，因此同一路径多实例仍是已知边界：后续 exact identity 会阻止错号 RPA 动作，但可能短暂唤醒另一实例并令本次准备失败。完整源码自检、测试版/交付版 renderer 构建与 `git diff --check` 已通过。
- 2026-08-04 16:33–16:34 的第七次只读复验已通过托盘恢复交互合同：测试前把同一微信主窗 `HWND 201258` 正常关闭到托盘，最新测试版经原生 `--scene=startmenu` 恢复后仍绑定同一 HWND，窗口位于左上角，右上角关闭按钮不再残留红色 hover；随后只点击微信左侧“聊天”中性入口，页面立即从通讯录切换为聊天列表，证明客户区真实可交互。测试版随即关闭；诊断以 `moments_discover_entry_not_found` 停止并明确记录 `real_action_attempted=false`，没有进入朋友圈，也没有点赞、评论、发送或发布。
- 当前门禁收敛删除了 `ringRatio` 与固定 `expectedCenterX` 两个像素理想值的硬阻断，二者仍可作为数值诊断；候选仍只能来自 DPI 缩放后的窄一级侧栏扫描区，并必须满足像素量、尺寸、填充率、宽高比、低边角占比和明显单对角线拓扑，且只能唯一命中。最新现场的聊天、联系人和立方体候选仍分别由 `diagonalContrast`、`cornerRatio`、`diagonalContrast` 独立拒绝；dense/solid 伪图标、双候选和扫描区外图标回归继续拒绝。点击“发现”后仍必须在同一 PID/HWND、前台和输入租约下重新证明唯一绿色选中“发现”及唯一 OCR“朋友圈”，所以一次可逆导航误点不能越过后续业务动作边界。针对性真实 matcher、完整像素 resolver、`npm.cmd run check:self`、`npm.cmd run build:test`、`npm.cmd run build:delivery` 与 `git diff --check` 均已通过；最终结论仍需一次约 10 秒的“发现 → 朋友圈 → 生成预演”只读 live 复验，且不包含点赞、评论或发布授权。
- 2026-08-04 16:53–16:57 的第八次只读复验已经证明新门禁可唯一点击真实“发现”，随后自动点击“朋友圈”并进入内嵌朋友圈页面；这两步不再返回 `moments_discover_entry_not_found`。第一次进入后，测试工具在流程尚未结束时读取后台窗口，恰好触发外部输入租约，按设计以 `moments_user_input_detected` 停止；排除该干扰后，从已选中朋友圈页再次预演返回 `moments_entry_not_found`，现场截图与窗口状态证明此时不是页面未打开，而是普通 OCR 偶发漏读绿色选中背景上的白色“朋友圈”。两次诊断均为 `real_action_attempted=false`，没有点赞、评论、发送或发布，测试版随后已退出。
- 第八次复验的 `moments_entry_not_found` 实际根因不是 Windows OCR 没返回文字，而是 live OCR 返回了带字符间空格的 `朋 友 圈`，嵌入 PowerShell 的正则误写为匹配字面反斜杠的 `\\s+`，普通和放大 OCR 都无法归一化。修正为真正的 `\s+` 后，被动实机证据稳定得到唯一“发现”、唯一“朋友圈”和绿色选中行；放大 OCR 仍只作为同一定位器的顺序 fallback，不叠加为第二份授权。
- 2026-08-04 17:31 的第九次只读预演已经通过入口并识别到帖子右侧菜单，但返回 `moments_post_not_found`。现场截图和真实 reader 证明 `MMUIRenderSubWindowHW` 仍近似覆盖整窗，而新版 feed 实际从 `x=374` 开始；旧算法按整窗比例找菜单与头像，属于适配坐标基准错误。当前共享适配器从唯一绿色“朋友圈”选中行动态取得 `contentLeft=绿色行右边界+1`，UIA render pane 继续负责 HWND/PID 和动作归属，帖子菜单、头像、正文只在动态 feed viewport 内解析。双宽绿色段、越界、剩余内容区不足继续 fail-closed，standalone 布局原样复用 render pane。
- 2026-08-04 17:48 的第十次只读预演已在微信 `4.1.11.55` 实机通过：executor 返回 `ok=true`、`real_action_attempted=false`，状态为 `prepared`；当前可见候选 `1`、接受 `1`、顶部拒绝 `0`，目标完整可见，动作序列仅为待确认的 `like`。快照把同一帖子正文、头像和菜单锁在 render pane 内，测试版随后关闭；本轮没有点击互动菜单、点赞、评论、发送或发布。离线同图生产 reader 的新 viewport 结果为 `menu=1/post=1`，旧整窗基准为 `0/0`。`npm.cmd run check:self`、`npm.cmd run build:test`、`npm.cmd run build:delivery` 与 `git diff --check` 均通过；下一步仍需用户在动作发生前单独确认一赞、一评或发布。
- 2026-08-04 17:57–18:00 的第十一次实机验收在用户明确确认“执行单次点赞”后完成。程序先重新生成预演，仍唯一接受同一条完整帖子（候选 `1`、接受 `1`、非半露出），再以同一 observation 检查互动菜单；菜单状态为“赞”，检查阶段 `real_action_attempted=false`。随后仅执行一次点赞，executor 返回 `ok=true`、`real_action_attempted=true`；动作账本状态为 `verified`、`no_op=false`，菜单从“赞”变为“取消”，证明新点赞已生效。没有评论、发送或发布，也没有第二次点击；测试版已关闭并确认无本仓库 Electron 进程残留。
- 2026-08-04 18:05–18:07 的第十二次实机验收在用户明确授权“按对方朋友圈正文直接评论”后完成。为复用既有 AI 评论链路，本轮把连续互动临时设为目标 `1`、关闭点赞、开启 AI 评论且偏好留空；程序唯一观察当前完整帖子，DeepSeek 生成 `32` 字评论，再用同一 `post_fingerprint` 重新预演后发送。comment executor 返回 `ok=true`、`real_action_attempted=true`，动作账本为 `verified` / `send_verified`，`verification_mode=composer_closed_or_send_inactive_v1`；campaign 最终 `completed=1`、`commented=1`、`comment_skipped=0`、`outcome_unknown=false`。没有再次点赞、没有第二条评论或发布；测试版随后关闭并确认无本仓库 Electron 进程残留。
- 2026-08-05 13:32 的发布 attempt `f9653bce-1529-4bd7-a90c-fe325a4310c9` 在用户已打开新版内嵌朋友圈时返回 `moments_discover_open_timeout`；账本为 `stage=moments_open`、`action_attempted=false`、`outcome_unknown=false`，没有打开相机、选择文件或点击发表。根因是入口判断先硬性依赖一级侧栏“发现”小图标的单帧选中态，再检查更直接的“朋友圈”绿色选中行；前者瞬时漏判会触发一次多余的“发现”点击，并把已打开页面带入错误的转换等待。当前只收敛这一重复前置门禁：当同一精确微信 HWND 中“朋友圈”文字唯一、绿色选中且动态内容边界成立时，直接判定页面已打开；多候选、未选中、边界未证明、窗口身份变化和最终发表确认继续拒绝。专项真实 PowerShell 决策测试覆盖正例、边界缺失与歧义反例，完整 `npm.cmd run check:self` 与 `npm.cmd run build:test` 通过；真实发布仍待用户重新启动后验收。
- 2026-08-05 15:47 的发布 attempt `56c8a97c-dc83-444f-9d1f-12c1aec8d9fd` 从普通页面成功点击“发现”后返回 `moments_discover_open_timeout`；账本仍为 `stage=moments_open`、`action_attempted=false`、`outcome_unknown=false`，未点击“朋友圈”、未选图、未输入正文、未点击发表。生产识别器离线复算现场截图确认唯一未选中的“朋友圈”入口已经出现，但选中后的“发现”图标因绿色背景导致活跃像素超过旧图形阈值；导航器又重复要求该旧入口证明，因而忽略了更直接的唯一“朋友圈”证据。当前把“发现”图标证明限定在首次点击“发现”之前：该受控点击完成后，同一冻结 PID/HWND/窗口几何内的唯一未选中“朋友圈”OCR 即可进入受控点击；点击朋友圈后只接受“唯一选中行 + 内容区边界”作为成功，点位归属、最新输入租约、歧义拒绝和最终发布门禁保持不变。旧自检中保护错误门禁的断言已反转并增加真实 PowerShell 状态决策反例；完整 `npm.cmd run check:self`、发布驱动/视觉专项检查、`npm.cmd run build:test` 和 `git diff --check` 均通过，待用户用开发构建 `20260805T0802Z` 从普通页面复验。

- 2026-08-05 后续发布 attempt `687499fe-3d29-492e-8b5d-48e481fd36be` 已成功进入独立编辑窗并上传 1 张图片，随后在 `stage=content_input` 以 `moments_publish_button_not_found` 停止；`action_attempted=false`、`outcome_unknown=false` 且无 marker，正文和“发表”均未操作。现场截图离线复算确认新版编辑窗约 `453×601`，绿色“发表”位于相对 `left≈81/top≈521/140×38`，旧定位器仍要求按钮位于右半区；同时普通 Windows OCR 会漏掉白字绿底的“发表”，属于旧布局假设与 OCR-only 技术债叠加。当前改为仅在精确绑定的 composer 窗口底部 20% 识别唯一、尺寸/比例/填充率均符合的微信绿色主操作按钮；双候选、中下部/上方绿块和小绿块均拒绝，OCR 与视觉冲突同样拒绝。填正文前的按钮预识别属于可逆阶段多余门禁，已移除；按钮像素扫描只在有界搜索、最终 fresh check 和 marker 后 rebound 三处启用；不可逆点击前的正文完整回读、素材 manifest、前台窗口/PID/点位归属、durable marker、marker 后 fresh observation 与按钮 rebound 全部保留。真实现场截图命中唯一候选，6 组 resolver 反例、完整 `npm.cmd run check:self` 和 `npm.cmd run build:test` 通过；待用户关闭旧编辑窗并用开发构建 `20260805T0830Z` 复验。
- 2026-08-05 最新发布 attempt `a0e1c330-69c1-4886-9d59-04a47e54eb6d` 已完成图片选择并进入正文编辑，但在 `stage=content_input` 以 `moments_publish_content_readback_mismatch` 停止；`action_attempted=false`、无 marker，未点击“发表”，不存在重复发布风险。现场截图与 UTF-16 结构复核证明旧的整段 `KEYEVENTF_UNICODE` 输入会被微信 `4.1.11.55` 富文本编辑器错误处理：原本分散的 `📢、🤖、🛠️、👇` 被按原顺序集中到正文开头，段落换行也被吞掉；完整回读门禁因而正确拒绝错误正文。当前正文改为一次性 `UnicodeText` 剪贴板粘贴：操作前捕获用户剪贴板，逐字校验待粘贴值，只在同一前台 composer 内同步执行 `Ctrl+V`，并在 `finally` 恢复原剪贴板；输入后和不可逆点击前仍分别 `Ctrl+A/C` 做 Ordinal 完整回读。回读由“固定等待 80ms 后把无文本当空串”改为唯一 sentinel 加有界轮询，要求非空的阶段不会再把剪贴板尚未就绪误判为正文不一致；若所有轮询均为读取错误，也不会伪装成空编辑框后覆盖旧草稿。新增回归覆盖中间/末尾 supplementary emoji 错序、VS16 丢失、连续空行被压平、延迟回读与连续读取错误；朋友圈专项、完整 `npm.cmd run check:self` 和测试版 renderer 构建均通过，开发构建号为 `20260805T0905Z`，真实点击发表仍待用户验收。
- 2026-08-05 后续发布 attempt `1235f61e-54f7-41e5-98a0-c8a8889e096b` 的图片与正文均已正确进入编辑窗，却仍在 `stage=content_input` 复用同一 `moments_publish_content_readback_mismatch` 提示停止；账本再次确认 `action_attempted=false`、无 marker，未点击“发表”。本轮截图中正文已退出绿色全选状态，而该动作只会发生在第一次 Ordinal 完整回读通过之后；生产等价 Windows OCR 对同一截图离线复算又证明，预期 24 字符锚点只连续命中前 14 个字符。根因因此不是 Unicode 正文再次损坏，而是强逐字校验通过后仍叠加了一条较弱的“整窗 OCR 必须包含正文锚点”重复门禁，并把 OCR 漏读误报为完整回读不一致。当前删除该重复 OCR 硬阻断及其无消费者 observation；第一次完整剪贴板回读、`prepublish_verification` 的同窗观察与按钮定位、点击前第二次完整回读、素材 manifest、窗口/PID/前台/输入租约、fresh button、durable marker 和结果未知防重发均保持不变。回归测试先在旧实现上按预期失败，最小修复后朋友圈专项与完整 `npm.cmd run check:self` 全部通过，测试版 renderer 构建号为 `20260805T0937Z`；真实发表仍待用户重启后验收。
- 2026-08-05 最新发布 attempt `c08b95ad-5139-4870-a6ce-2a098bfc079e` 已通过图片、正文和第一次完整回读，随后在 `stage=prepublish_verification` 以 `moments_publish_button_not_found` 停止；`action_attempted=false`、`outcome_unknown=false`、marker 目录为空，仍未点击“发表”。失败后的现场截图中绿色按钮刚从编辑窗底边露出；生产原样 resolver 对 composer 可见裁片离线复算得到唯一候选 `left=82/top=625/140×28`、`greenRatio=0.948`、`aspectRatio=5.0` 并返回 `ok=true`，证明按钮裁切、颜色和 OCR 都不是拒绝根因。旧有界搜索的每一轮顺序为“先观察、未找到再滚动”，第 12 次观察后仍执行第 12 次滚动，却随即退出，漏掉滚动后已经出现按钮的最终一帧；40.847 秒耗时也与多轮 OCR 和 12 次滚动吻合。当前只修正这一处时序：最大滚动次数仍为 12，但观察次数为 13，最后一次滚动后只做被动观察，不新增滚动或点击；唯一按钮、窗口/PID/点位归属、点击前第二次正文完整回读、素材 manifest、fresh button、durable marker 和结果未知防重发均保持不变。底边裁切真实 resolver 用例与“最终滚动后必须再观察”回归先在旧实现上按预期失败，修复后朋友圈专项、完整 `npm.cmd run check:self`、语法检查和 `npm.cmd run build:test` 通过，开发构建号为 `20260805T1039Z`；真实发表仍待用户重启后验收。

### 下一步微信自动流程（按顺序、模块保持独立）

1. 主动触达先运行；每完成一个可验证的联系人发送点，允许安全让出微信控制权。
2. 收到客户新消息时，主动触达暂停，自动回复优先执行；回复确认完成后从原联系人进度继续。
3. 只有主动触达正常完成，才启动朋友圈点赞评论，每天目标 10 条；暂停、失败或 `outcome_unknown` 不自动进入下一步。
4. 点赞评论正常完成后，再进入独立的朋友圈发布模块。单条发布已实现内容指纹、发布前两步确认、结果验证和结果未知不重发；在真实测试账号验收通过前，不接入自动串联。

各模块继续独立保存状态、独立失败和独立手动运行，只由轻量调度器串联顺序；打开软件仍只初始化和恢复状态，不直接抢占微信。

### 2026-07-30 千人联系人性能修复（待异机验收）

- 异机诊断包显示同步联系人 `3738` 人、主动触达冻结名单 `2246` 人。触达名单会排除微信号缺失、姓名重复、身份重复、账号归属缺失或混杂、停用及用户手动移出项；旧诊断包仅保留总数，不能反推各原因人数。
- 主动触达页新增“未触达原因”聚合，只显示原因和人数，不写入联系人明文；新任务诊断新增 `classification.summary`，记录总数、可触达数、排除数和各原因计数。
- 联系人同步表格改为每页 `50` 人；主动触达预览默认最多渲染 `50` 人并按需继续显示，避免一次创建数千行 DOM。
- 任务事件不再在每次状态变化时广播全部联系人结果，只发送当前批次最多 `50` 条增量；完整任务快照仍保留在状态查询和磁盘文件中，未改变恢复与发送结果语义。
- 普通联系人状态读取改为主进程直接读取本地状态和联系人缓存；只有手动路径验证、恢复自动识别和真实同步才启动联系人执行器，避免登录后重复启动 Electron/PowerShell 查询。
- 联系人状态和任务增量专项 self-check、语法检查及测试版 renderer 构建通过；2026-07-31 本分支完整 `check:self`、测试 renderer 和测试便携包自动检查全部通过。自动检查不替代真实微信验收；当前测试包仍需在低配置异机以千人名单验证 CPU、内存、翻页和持续任务更新。


### 离线安装与覆盖升级

- 安装器使用稳定产品 ID `com.aihuoke.desktop`，按当前 Windows 用户安装到 `%LOCALAPPDATA%\Programs\AI获客`；同一产品的新安装器直接覆盖程序文件，不依赖云服务器或联网更新。
- 用户仍需手动把完整新安装器传到目标电脑并双击运行。覆盖升级不需要先卸载，也不需要删除旧目录。
- 交付版运行数据继续保存在 `%APPDATA%\xiaoxi-active-touch-delivery\data`，不放在安装目录内。覆盖升级和普通卸载均保留 API Key、联系人、AI 专家资料、任务状态和诊断日志。
- 安装器写入桌面快捷方式、开始菜单和 Windows“已安装的应用”卸载入口。卸载只移除程序；若需要清空业务数据，必须另行明确确认，不能由普通卸载隐式完成。
- 2026-07-24 已在开发机完成首次静默安装、再次覆盖安装、快捷方式与卸载入口检查、卸载后数据保留、最终重新安装的完整生命周期验证；安装后的主程序与同 commit 便携包主程序 SHA256 一致。
- 当前安装器未做商业代码签名，Windows 可能显示“未知发布者”；交付时必须同时提供版本清单和 SHA256，不能把未签名描述成已签名。

### 2026-07-24 异机日志复盘与待验收修复

- 异机日志证明 DeepSeek 单次请求耗时约 `0.8–1.7s`，自动回复的主要等待来自本地视觉扫描，而不是网络或模型。
- 视觉适配器每次只返回最新客户气泡，导致模型请求始终只有系统资料和当前消息；当前源码改为按联系人在内存中保留最近 12 轮，不把客户原文写入状态文件或诊断日志。
- `current_transition_unresolved / second_frame_unstable` 曾让同一条消息额外等待 `16–21s`；当前改为 `750ms` 快速复核，仍保留同一窗口、客户气泡、稳定 occurrence 和 exactly-once 约束。
- 主动触达删除了搜索会话前、搜索脚本内和草稿脚本内重复执行的窗口拉起步骤；草稿输入改为绑定搜索得到的 PID/HWND。搜索、草稿归属、发送前会话复核和发送结果三态仍保留。
- 异机曾出现 `EPERM rename touch_task.json.tmp`；任务状态原子写入现使用唯一临时文件，并对 Windows 短暂占用做有限重试。
- 联系人目录自动发现新增 `Documents\xwechat_files`、盘符根目录 `xwechat_files`、OneDrive Documents 和 `INI key=value` 解析。
- 上述修改已通过源码自检，尚需新便携包完成一轮异机上下文、速度和连续回复验收后才能更新为已验收。

### 2026-07-24 异机日志第二轮修复

- 异机日志确认主动触达第三位联系人并非“电脑带不动”或 DeepSeek 超时，而是 Windows 在替换共享运行锁 `wechat-operation.lock.json` 时短暂返回 `EPERM`。主进程高风险状态文件现统一使用唯一临时文件、落盘同步和有界重试；替换失败不会提前污染内存状态。
- 未完成主动触达任务新增来源构建号。覆盖升级后会保留旧任务及发送证据，但自动标记为“上一版本任务”并暂停，不能由新版本直接继续；结束旧任务后才能创建新任务。
- 主动触达日志新增搜索结果打开、标题读取、会话确认、草稿输入和输入尝试次数等子阶段耗时，后续异机慢速问题可直接定位到具体适配步骤。
- 自动回复继续保留最近 12 条上下文；DeepSeek 结构化回复从最多三次请求收敛为一次正常请求加一次增强恢复，仍失败则使用既有兜底和人工提醒。
- 上述修改已通过完整源码自检及测试版 renderer 构建；新的安装包和异机真实微信验收尚待完成，因此本节不把新包标记为已验收。

### 2026-07-29 朋友圈每日自动化（待实机计时验收）

- 朋友圈运营页新增每日计划，可自行设置每日目标、开始时间、点赞/AI 评论组合和评论偏好；默认关闭，默认目标 20 条、开始时间 09:00，不写死用户实际计划。
- 计划只使用应用进程内的单个计时器，不依赖 Windows 计划任务、云服务器或常驻后台服务。应用在计划时间保持打开时自动执行；2026-07-31 已取消“错过时间后下次启动立即补做”，改为启动只恢复状态并显示“待续跑”，由用户确认继续。
- 每日计数只接受本轮实际验证成功的新动作。历史已点赞、检查过的帖子和跳过项不冒充完成；同一帖子当天只计一次，发送结果不确定时当天暂停且不自动重发。
- 自动回复或主动触达正在占用微信时，每日计划延后一分钟再试，不争抢窗口；用户手动暂停或结束后当天不再自动恢复，次日按计划继续。
- 每日状态继续保存在 `data/moments/state.json` 的独立 `moments_daily_automation` 节点，包含日期、完成数、检查数、去重指纹、暂停原因和下次运行时间；诊断日志记录配置、调度、启动、进度、忙碌重试和次日重置。
- 每日计划时钟、跨重启、跨日重置、忙碌重试、结果未知、人工暂停、磁盘写入失败恢复和界面晚到状态保护均有源码检查；2026-07-31 当前安装版已观察到一次“启动无微信副作用”，真实微信的计划到点、完成后当天不重复和跨重启恢复仍待继续验收。

## 当前能力与验证矩阵

| 能力 | 当前实现 | 历史证据 | `4.1.11.55` 本机 live | 新便携包 | 当前结论 |
|---|---|---|---|---|---|
| 联系人同步 | 已实现；联系人清单供自动回复和主动触达只读使用 | 历史本机同步跑通 | 已验证 | 结构自检通过，待实机 | 当前测试包已包含，仍需 exact-build 与异机验收 |
| 自动回复 | 已实现稳定 occurrence、持久去重、状态 v3、识别波动等待、最小发送契约及禁止猜测客户称呼 | 旧源码版和旧便携包曾通过本机实发；异机复测暴露过硬 OCR 门禁 | **已验证（2026-07-23）** | **结构自检通过，待实机** | 当前测试包已包含；仍需本包真实回复与异机验收 |
| 主动触达 | 已实现任务冻结、AI/固定话术准备、共享发送事务和发送结果持久化；2026-08-13 修复启动点击与 15 秒静默门禁冲突 | 旧基线曾完成单联系人真实发送；2026-07-22 修复线完成 4 联系人连续实发；2026-07-23 当前共享适配器完成单联系人回归 | **用户已使用真实微信复验（2026-08-13）** | **源码与构建自检通过** | 当前机器和账号已验收；异机验收仍待完成 |
| 朋友圈 | 已实现自动打开、逐帖识别、连续点赞、自动下滑、去重账本、AI 评论和独立单条发布；逐帖互动与发布均支持 standalone/integrated 双模式，窗口布局与 feed viewport 由共享适配器管理；发布继续支持 1–9 张图片或单视频、两步确认、内容指纹、点击前 marker、发布后精确候选核验和结果未知不重发 | 2026-07-27 连续点赞完成 5/5；2026-07-29 点赞＋AI评论单目标闭环；2026-08-04 后 `4.1.11.55` 多轮新版实测；2026-08-06 单条图文真实发表并自动 `verified` | **用户已确认新版真实点赞、AI 评论和单条图文发布动作成功；最近一次点赞/评论成功后的结果返回曾分别误报 `moments_like_outcome_unknown` 和 `moments_comment_readback_seed_timeout`** | **结果回读修复已有专项回归；V1.0 便携包和安装包必须从同一干净提交生成并复验** | 动作链路已有 live 证据；本次修复要求点赞连续两帧“取消赞”证据，评论发送后获得独立有界确认时间窗。exact-build 结果返回、视频、不同 DPI 和每日计划仍待验收 |
| 日志诊断 | 已实现统一 JSONL 总账、脱敏、轮转、异常捕获和一键导出；入口移入吐槽中心底部 | 2026-09-07 完整测试版脱敏上报及升级数据保留通过；9月8日新入口进入1.0.2安装版 | 不涉及真实微信动作 | 新旧能力已进入测试安装版 | 自动诊断开关与用户主动提交反馈独立；新反馈能力见下表 |

说明：`self-check`、renderer 构建或便携包自检通过只能证明静态契约和包结构，不得替代表中的 live 验证。

### 桌面公共体验与用户支持

| 能力 | 当前实现与验证 | 部署 / 包状态 |
|---|---|---|
| 角色形象与名字 | 3 位员工、9 种形象及主色/纹理联动；偏好原子持久化与失败保护自检通过；真实 Electron 的 9 种形象及紧凑窗口已检查 | 当前开发版实现；本轮未打包 |
| 更新公告与使用教程 | 签名公告缓存最近收到的 20 条、已读状态、30 分钟元数据刷新；离线教程与跳转入口已实现，公告专项及 Electron 教程检查通过 | 公告复用现有签名清单；新客户端界面本轮未打包 |
| 吐槽中心与处理进度 | 独立草稿、不可变提交快照、持久发送队列、私有回执查询和后台处理状态；本地关键自检与隔离 HTTP 联调通过 | 2026-09-08 服务已部署；实际1.0.2安装版测试反馈完成提交、后台处理与客户端已解决闭环 |

### AI获客 2.0 内容能力矩阵

| 能力 | 源码与本机证据 | 固定运行时 / 正式包 | 当前结论 |
|---|---|---|---|
| 产品详情图 | 安全 sidecar、桌面入口、离线工作流和 AI 安全配置已实现；两把 Key 由 `safeStorage` 加密，固定官方端点与模型，单击直接生成并保留后台单任务防重；`550 passed, 1 skipped, 134 subtests` 与双视口无头 Chromium 回归通过 | 同提交固定运行时、便携包和安装程序已重建并通过包内自检；用户本人 Key 真实 AI 精修生成通过 | **产品详情图已完成并通过用户验收**；净机测试仅作为非阻断补充 |
| 素材索引与任务底座 | 原片原地索引、文件夹批量续跑及暂停/恢复/取消、缓存设置、成片登记已实现；内容引擎 Python 测试和 Electron bridge 自检通过 | 同提交固定运行时已进入正式便携包与安装程序；包内 health/shutdown 自检通过 | 首期底座可进入覆盖安装验收，原片不复制不删除 |
| 制作统计与历史导航 | 业务制作先聚合再筛选分页；待处理、历史、归档及内部步骤分开；按精确任务恢复原制作与候选。聚合和超过 500 条候选的定向检查通过 | 本轮源码改动，未打包 | 统计不再把原始任务行当作独立制作；旧失败和归档记录保留 |
| 供应商实际请求用量 | 新增持久追加账本与任务关联查询，记录实际请求结果及已返回的 token、字符、音频时长等指标；缺失或中断保持未知 | 本轮源码改动，未打包 | 已完成1条36.829秒真实视频及8轮用量汇总：131次LLM、7次TTS、7次ASR；金额须供应商账单确认，不由调用次数推定或宣称降本比例 |
| 媒体元数据与版权状态 | ffprobe 适配、时长/尺寸/帧率/音轨、文件可用性与素材版权状态已实现 | 本轮不捆绑 ffprobe；净机能力会显示不可用 | 开发机可分析，净机媒体分析尚未交付；人物/声音同意不得混作版权证明 |
| 课程拆条 | 已实现带时间戳语义片段、30～90 秒完整观点候选、5 条候选/前 2 条推荐、竖屏裁切、课件聚焦与课堂全景画中画、降噪响度、烧录字幕、SRT 与源时间码追溯；指定真实课程原片完成 8 秒渲染冒烟 | 固定运行时尚未从本次提交重建；开发机 FFmpeg 可用，百炼 Key 未配置 | **源码与渲染链路已实现，AI 内容验收未完成**；不能把 8 秒渲染冒烟写成 5 条真实课程片通过，跨素材 B-roll 自动编排仍是后续增强 |
| 智能混剪 | 保留旧 V1 项目/槽位接口；新增“开场—过程—结果”语义片段池、老师连续原声、最多 300 条有界高分组合、精确源区间、受控预览与内部验收队列；自动回归覆盖去重、素材不足、恢复和区间配方 | 固定运行时尚未从本次提交重建；FFmpeg/ffprobe 仅在开发机显式可用 | **源码闭环，真实 30 条批量验收未完成**；百炼真实语义标签、字幕误差、画面重复度和 100/200 条扩展必须继续用真实素材验证 |
| AI 生成视频 | 导航保留后续入口 | 未接入供应商 | 尚未实现 |
| 渠道运营 | 导航和“不保存密码/验证码/Cookie/设备登录态”的边界已明确 | 无发布服务或平台授权适配器 | 学员、发布、AI 检查、回执、线索与数据仍为占位 |

2026-07-22 自动回复本机验收记录：测试联系人 `A测试客户` 产生 3 个不同的稳定 occurrence 指纹，账本均为 `sent_verified`，没有 pending；用户确认回复行为符合预期并验收通过。随后构建 `20260722T0603Z`，从 `C:\tmp` 全新解压目录独立启动，用户再次确认便携版自动回复验收成功。

2026-07-23 自动回复本机验收记录：当前源码识别真实微信主窗口后恢复健康轮询；测试联系人 `A测试客户` 连续发送不同咨询内容，系统分别生成一次上下文相关回复，未出现重复发送。首次复测发现模型从“你好呀”猜测错误称呼，随后已增加“自动回复不使用姓名或昵称”的提示词约束及输出清理；重启最新版后用户再次完成验收，截图中的回复均只使用通用“您好”。验收结束时状态为 `paused`、扫描健康为 `healthy`、连续失败为 0、无 pending observation 或 handoff。

2026-07-22 主动触达本机验收记录：用户明确重新发起 4 联系人连续任务；`A测试客户`、`玺联惠客户经理炎燃19946167505`、`玺联惠清洁产品森妮19101706971`、`向锦涛` 均为 `sent_verified`，任务 `4/4` 正常完成。每次发送均记录发送前精确草稿与发送后输入框清空验证；用户确认验收成功。该结论只覆盖当前源码工作区，不自动放行尚未生成的新便携包。

2026-07-23 主动触达本机回归记录：当前源码通过微信号精确搜索定位 `A测试客户`，共享适配器完成写入并真实发送单条触达文案。此前阻断根因是新版微信 UIA 无标题时使用整块视觉标题像素哈希，输入草稿导致像素轻微变化并误报 `atomic_conversation_changed`；现仅在“微信号精确搜索 + 视觉标题”组合下不再把原始像素哈希漂移作为硬阻断，仍保留同一 PID/HWND、精确草稿、输入框归属和前台窗口校验。用户根据微信聊天中的已发送气泡确认验收成功。

2026-07-23 异机主动触达复测记录：正式便携包能够启动并进入任务，但桌面上已有可见个人微信主窗口时仍显示“未找到微信聊天主窗口”。排查发现窗口适配层把“固定位置/尺寸未完全生效”等不同底层原因统一折叠成 `wechat_window_not_found`，同时轻量发现器仍把微信窗口类名或样式位作为主窗口硬条件。当前源码已改为以个人微信进程归属、可见性和可用聊天窗口尺寸发现窗口；固定左上角和标准尺寸只做优先适配，失败时允许使用当前实际窗口几何；底层原因不再统一伪装成“未找到”。该修复仍需生成新包并在原异机复测。

2026-07-23 异机兼容专项复查记录：主动触达现复用联系人同步模块的 `Weixin.exe` 查找结果，避免微信关闭时只搜索旧 `WeChat.exe` 路径；`powershell_timeout`、`powershell_failed`、窗口不可用和多窗口等错误保留真实原因。精确搜索无结果、搜索结果未打开或客户会话未定位属于联系人级失败，当前联系人会被隔离跳过而不会暂停全部任务；窗口、账号或发送结果不确定仍会全局暂停。真实触达每完成 50 人自动暂停一次，必须由用户点击继续后才处理下一批。

2026-07-23 异机自动回复复测记录：窗口识别已恢复，但目标电脑出现 `unread_contact_unresolved`。根因是视觉驱动能够发现微信未读红点，后续“按红点打开会话并校验标题”的代码也已存在，但候选选择阶段只统计红点后直接返回，导致 OCR 无法读取侧栏联系人名时兜底链路永远不可达。当前已恢复红点候选：先点击微信自身未读红点，打开后再用已同步联系人名单核对聊天标题，核对通过才读取消息和发送；未知或不在名单中的会话不会发送。`unread_contact_unresolved` 本身从未触发主进程暂停，截图中的 `paused_by_user` 只可能来自暂停 IPC，两者是相邻但独立的状态。

2026-07-23 异机红点兜底复测记录：程序已正确打开 `A测试客户`，但打开后的标题 OCR 因首字或末尾字漂移触发 `conversation_title_mismatch`。原标题模糊匹配要求首字和末两字保持不变，对异机 OCR 过严。当前在原精确/保守模糊匹配之后增加“唯一最近联系人”匹配：名称至少四个字符、长度差不超过二、编辑距离在名称长度的 40% 内，并且只有唯一最小距离候选时才通过；并列相似联系人仍返回歧义且不发送。

2026-07-23 异机标题复测记录：目标电脑仍返回 `conversation_title_mismatch`，说明其标题 OCR 偏差已超出有限错字范围，不能继续依靠放宽字符串相似度。截图同时证明红点点击后 `A测试客户` 侧栏行已呈微信绿色选中状态。当前证据优先级改为：红点消失后，若侧栏只有一个已同步联系人行被微信标记为选中，则直接使用该已解析联系人；只有选中行证据不可用时才回退标题 OCR。选中行必须已映射到同步名单且唯一，不会在两个相似联系人之间猜测。

2026-07-23 自动回复门禁最终收敛：异机继续出现 `conversation_title_mismatch`，证明联系人标题 OCR 和同步名单不适合作为红点自动回复的授权条件。红点路径改为消息驱动：点击微信真实未读红点后，只要求窗口 PID/HWND 不变、最新气泡为客户发入、同一气泡证据稳定、草稿与发送按钮归属正确，并继续执行 exactly-once 去重；联系人标题仅保留作诊断，不再阻断。无法映射到同步联系人的会话使用窗口和红点行证据生成内部匿名身份，OCR 名称不进入称呼或提示词。主动触达仍必须严格确认联系人，本次放宽不影响主动触达。

2026-07-23 异机消息文本复测记录：客户原文“我需要清洁设备”被聊天气泡 OCR 识别成“我需要尚吉设备”，错误文本随后进入 DeepSeek。根因不是模型提示词，而是红点兜底在打开会话后覆盖了点击前的侧栏预览，并在第二帧确认后再次采用气泡 OCR。当前红点候选会在点击前保存同一行侧栏预览；两帧客户气泡稳定后，再以完整、非截断且首尾锚点一致的侧栏预览纠正孤立字形漂移。未增加“清洁/尚吉”等业务词硬编码。

2026-07-23 异机 OCR 根因复核：上一次侧栏/气泡交叉选择仍未解决“清洁→尚吉”，说明两路整窗 OCR 可能同时漂移，上一结论不是最终根因。使用用户现场截图直接复现实验：整窗/2 倍识别仍丢字或得到“尚吉”，4 倍出现新的过度平滑错字；按已定位客户气泡精确裁剪后做 3 倍 OCR，完整得到“我需要清洁设备”。当前改为两阶段 OCR：整窗只定位最新客户气泡，消息正文必须从该气泡的紧边界做 3 倍复识别；只有复识别结果与整窗结果保持首尾锚点、长度和有限编辑距离一致时才采用。结果同时记录 `rawMessage`、`refinedMessage` 和 `messageOcrSource`，便于后续异机诊断。

## 当前修复基线

- 2026-08-05 朋友圈发布导航复测：attempt `9530c126-9128-4be5-bcba-be247752b2e7` 与 `c7275f89-255d-4a78-9c6c-be10a30b68ce` 均在真实动作前返回 `moments_discover_entry_not_found`，`action_attempted=false`。根因是新版 MMUI 首帧重绘时“发现”像素证据可能短暂为空，而入口逻辑只观察一次；现改为最多 6 次、每次 250ms 的纯观察，期间若严格证明朋友圈已打开则直接成功，若唯一“发现”证据出现才进入点击。窗口 PID/HWND/类名/几何唯一性、多候选拒绝及点击前新鲜输入租约均保留；目标自检、完整 `check:self` 和 `build:test` 已通过，真实发表仍待用户复验。
- 同日后续 attempt `40d28dd5-f767-4200-aa05-06c8cdfbbb4b` 已通过首帧“发现”识别，但在点击“发现”后返回 `moments_discover_open_timeout`，仍为 `action_attempted=false`。代码复核确认新版微信会直接恢复上次打开的朋友圈页面，而过渡循环先要求“发现”图标继续保持选中，导致已经成立的“唯一选中朋友圈 + 内容边界”强证据无法被采用。现将直接朋友圈页面证明提前到该适配器门禁之前；未证明页面时仍保留唯一“发现”与唯一“朋友圈”约束。目标自检、完整 `check:self` 和 `build:test` 已通过，真实发表仍待用户复验。
- 同日发布驱动复测 attempt `a11658cd-acae-428a-b614-33fa04c877ad` 已通过导航，但在 `baseline_observation` 返回 `moments_publish_integrated_surface_not_proven`，仍为 `action_attempted=false`，失败发生在相机点击前。根因是导航与发布对同一页面使用了矛盾合同：导航接受“唯一选中朋友圈 + 内容边界”，共享发布页面 resolver 却再次要求“发现”图标唯一且选中。现统一共享页面证明，将“发现”限定为进入页面时的导航定位器；发布、滚动与互动的页面身份只要求精确微信窗口/渲染区、唯一选中朋友圈和内容边界。动态 PowerShell 回归覆盖“发现未选中、形状变化或双候选但朋友圈页面成立”应放行，并继续拒绝重复朋友圈、朋友圈未选中和内容边界失败。文件选择框关闭后的独立编辑窗口绑定也由单帧判死改为最多 30 次、每次 200ms 的有界观察；只重试“尚未出现/尚未前台”，多候选、归属不符等身份错误仍立即拒绝。完整 `check:self` 与 `build:test` 已通过，真实发表仍待用户复验。
- 同轮下游审计还移除了两个没有实际保护作用的重复门禁：正文输入后不再把“发表按钮暂时不可见”误报成“素材变化”，素材源文件仍在不可逆点击前重新校验 manifest；发布后不再要求微信帖子暴露本地暂存文件名，也不要求折叠长文在屏幕上出现完整正文。正文输入先 `Ctrl+A`，通过临时复制在输入前检查旧草稿、输入后逐字符核对完整正文（含 emoji、标点和换行），并在每次核对后恢复用户原剪贴板；marker 后的第二次“发表”OCR 允许边框轻微抖动，但要求新旧按钮中心相互落在对方边界内且点击点仍属于同一窗口。相机入口的 input tick 现在在 UIA/视觉取证之前取得并原样沿用到点击，取证期间有外部输入时不会拿旧坐标继续操作；durable marker 一旦写入便不自动删除，后续任何不确定状态都交由用户明确标记“已发表”或“未发表”，避免误删 marker 后重复发布。自动结果确认只接受唯一完整帖子内的正文锚点与“刚刚”、composer HWND 消失、feed 改变、最终 manifest 有效、连续两帧同一候选，以及可逐项证明的 UIA 素材证据；新版独立 composer 当前只能提供较弱的视觉素材存在证据，因此真实点击后会诚实进入 `outcome_unknown` 交由用户在微信快速确认，竖图/长文菜单不在视口时同样如此，且不会自动补发。
- 当前源码目标为微信 `4.1.11.55`；`4.1.11.54` 只保留历史实测证据，`4.1.11.24` 不再列为当前支持承诺。新版朋友圈帖子预演、真实点赞、按原帖正文生成的 AI 评论和单条图文发布均已有 live 证据；本次点赞/评论结果回读修复仍需在 V1.0 exact-build 上复验。
- 自动回复和主动触达复用同一发送结果语义：`not_attempted`、`sent_verified`、`outcome_unknown`。结果未知不得自动补发。
- 自动回复只保留四条发送底线：目标窗口/会话不变、草稿精确一致、发送按钮属于目标微信窗口、同一 occurrence 最多发送一次；不再用二次 OCR 完全一致、整窗九点无遮挡或固定频率熔断阻断正常消息。
- 自动回复状态升级到 v3：保留 `processed` exactly-once 账本、人工交接和 `outcome_unknown`；丢弃旧包的临时 OCR observation、普通 sent guard、扫描波动和频率事件。
- 识别波动采用“保留待处理、隔离当前对象、继续监听”，不得把观察不确定升级为整机宕停，也不得在证据不足时发送。
- 微信窗口、DPI、坐标和识别配置归共享适配层；联系人任务、自动回复和朋友圈分别保存自己的业务状态与账本。
- DeepSeek“测试连接”必须同时跑通主动触达文案和自动回复结构化文案；简单 ping 不算能力可用。

## 实机放行顺序

1. 在新状态目录启动自动回复，确认界面明确显示“监听中”。
2. 测试联系人发送一条唯一消息；30 秒内只能回复一次，再空跑 90 秒不得出现第二条。
3. 发送第二条不同消息，仍只回复一次；重启应用后再测一条，验证 pending 不丢失、已发送不重复。
4. 主动触达先向 `A测试客户` 发送一条，再对三名明确标记的测试联系人连续执行。
5. 从同一干净 commit 生成测试 ZIP，在本机完整解压复测。
6. 在另一台电脑重新配置 Key、AI 专家资料并同步联系人，重复单消息自动回复与单联系人主动触达闭环。

任一步出现重复发送、错误联系人、发送结果未知、状态与界面不一致或需人工停止，均不得升级发布状态。

## 数据与密钥边界

- 测试版数据根目录：`%APPDATA%\xiaoxi-active-touch-test\data`。
- 交付版数据根目录：`%APPDATA%\xiaoxi-active-touch-delivery\data`。
- `contact_sync/`、`active_touch/`、`auto_reply/`、`moments/` 独立保存业务状态；`wechat_adapter/` 只保存共享适配信息。
- 角色名字与形象保存在运行根目录 `role-preferences.json`；反馈草稿、固定提交快照、加密回执凭据和投递状态保存在独立 `feedback/state.json`。公告签名缓存与已读状态位于 `cloud-maintenance/state.json`，自动诊断队列另存 `cloud-maintenance/outbox.json`；反馈不读取或修改自动上传授权开关。
- 朋友圈发布使用 `moments/publish-state.json`、`moments/publish_markers/` 和按 attempt 隔离的临时 `moments/publish_staging/`，不与 `moments/state.json` 中的逐帖互动或每日计划做并发 read-modify-write；状态和 marker 只保存指纹、计数与结果，不保存正文或绝对素材路径。素材快照在已验证、点击前失败或人工解决未知结果后删除；结果未知期间保留对应 attempt 的快照，避免未退出执行器失去已确认素材。
- 产品详情图与内容底座分别保存于 Electron `userData/product-detail/` 和 `userData/content-engine/`；数据库、上传、输出、缓存不得写入安装目录。
- 素材数据库只保存原文件位置、指纹、媒体信息和版权/使用权状态；原片保持在用户硬盘原位置。renderer、公开错误和诊断日志不得得到或记录绝对路径。
- 内容 sidecar、任务状态和后续渲染资源与微信运行协调器相互独立，不占用微信 RPA 运行锁，也不得阻断联系人、自动回复、主动触达或朋友圈状态读取。
- `production_summary.py` 从内容数据库只读汇总业务制作，不改写原任务成功/失败；归档仅保存对应批次的归档标记。实际供应商请求记入内容引擎数据目录 `provider-usage.jsonl`，按 `call_id` 合并事件查询，不向用户显示或记录请求正文、密钥及原始路径。
- DeepSeek Key 使用当前 Windows 用户的 DPAPI/`safeStorage` 加密，不能随 ZIP 或源码迁移；换电脑必须重新填写。
- 产品详情图已接入上述桌面 Key 存储：DeepSeek 与 APIMart Key 分别由当前 Windows 用户的 `safeStorage` 加密；只在两把 Key 齐全、用户主动点击“AI精修”并确认预计屏数与可能计费后新建任务。用户本人 Key 的真实生成已于 2026-08-03 验收通过；2026-08-29 的本轮修复只做离线回归，没有再次调用付费接口。
- 统一诊断日志位于交付版 `%APPDATA%\xiaoxi-active-touch-delivery\data\logs\diagnostics.jsonl`，单文件达到 5 MB 后轮转，最多保留 5 份历史文件。
- 日志用同一 `trace_id` 串联一次调用的开始、结束、耗时和错误；记录构建号、系统、显示器/DPI、子进程退出、扫描、AI请求、微信适配与任务状态迁移。
- DeepSeek Key、客户消息原文、联系人明文和 AI 专家资料原文不写入日志；需要关联时只保存长度与不可逆摘要。
- “日志诊断”页面可以分别查看自动回复脱敏链路、近期异常和近期运行事件，打开日志目录并导出 ZIP；导出包只包含统一诊断日志、自动回复结构化脱敏日志和摘要，不包含业务状态、联系人数据库、消息原文、专家资料或密钥。

## 2026-08-03 产品详情图收尾清理

用户确认真实 AI 精修验收通过并授权“该收尾的收尾、该删的删”后，已完成以下清理：

| 对象 | 收尾结果 |
|---|---|
| 桌面临时构建工作树 `xiaoxi-active-touch-build-40fcf89` | 最终运行时提升并复验后，通过 Git 正式移除；主项目依赖和浏览器链接未受影响 |
| 下载修复干净构建工作树 `C:\tmp\xiaoxi-release-f25aa0e` | 安装器发布并完成哈希核对后，通过 Git 正式移除；复用的主项目依赖、Python 环境和 Playwright 浏览器均保持完好 |
| 历史任务分支与联系人交付工作树 | 用户复核清单并确认后，删除三个已完整进入 `main` 的产品/内容任务分支；联系人性能能力及 Windows 自检兼容已由 `main` 的等价或更优提交覆盖，因此未重复合并旧分支，安全移除 `C:\tmp\xiaoxi-contact-delivery-20260730` 及其分支，释放约 `0.49 GiB` 历史发布物 |
| `desktop/.build/` | 删除旧运行时备份、PyInstaller、pytest、集成测试、日志和补丁中间物；仅保留最终两套固定运行时及清单、Playwright 浏览器和 Python 构建环境 |
| `release/` | 删除旧便携目录、ZIP、历史归档和旧验收包；仅保留用户验收通过的最终安装器与版本清单 |
| 主工作区体积 | 从约 `29.34 GiB` 降至约 `2.60 GiB`，释放约 `26.74 GiB`；加上桌面临时工作树约 `2.89 GiB`，本次合计释放约 `29.63 GiB` |

最终安装器对应提交 `f25aa0e`，构建号 `20260803T0835Z`，大小 `326,841,584` 字节，SHA256 为 `dde84693543857141f78465d3956b0b3e277897d58563c86a8bdf8132bd63afe`。安装器从干净工作树重建，通过完整源码自检、真实 Electron 下载回归、无头 Chromium 工作台回归、固定运行时检查和包内自检；用户随后完成覆盖安装并确认图片成功下载到桌面。

原“产品详情图”仓库中的本次 APIMart 防重复付费修复已独立提交为 `95c15c7`，并将该仓库的本地 `main` 快进到同一提交。当前 AI获客仓库只保留 `main` 分支和主工作树。为避免越权清理，用户原有未提交的 V2.0 Word 文档删除状态，以及原“产品详情图”仓库中来源混合的其他既有 dirty 改动仍明确保留。产品详情图收尾当时朋友圈发布仍是下一任务；本次后续任务已经完成首版源码实现，但不追溯改变当时的安装器或验收结论。

## 2026-08-08 V1.0 品牌与朋友圈前台恢复

- 连续互动在可逆的帖子观察阶段遇到 `moments_window_not_foreground` 时，不再直接把整轮暂停；控制器会使用预演时锁定的精确朋友圈 PID/HWND/形态重新聚焦一次，身份完全一致后只重读同一条帖子。动作点击、发送、结果未知和重复执行门禁均未放宽。
- 应用展示名、窗口标题、诊断包、便携包和安装包统一为“AI获客 V1.0版本”，npm 版本为 `1.0.0`。`appId`、安装目录和用户数据目录保持原值，覆盖升级不会仅因改名迁移或清空历史任务进度。
- 共享品牌配置已经纳入便携包应用源码；朋友圈专项、完整 `npm.cmd run check:self`、测试 renderer 和交付 renderer 构建均通过。
- 联系人同步的两个固定运行时已从同仓库可信提交恢复：`xiaoxi-contact-helper.exe` SHA256 为 `F9C90AEC8589AC11A93DB7ACFBC9B3B92C0C9C2A3B9175642829FBA2E0F12EEB`，`xiaoxi-db-decrypt.exe` SHA256 为 `2E6D190F3A0F33112CD4B7BAEADEA9947CB70688A94F3787A6BE236287DC1815`。正式 ZIP/安装器只允许从包含本节修复的同一干净提交生成，并以包内版本清单和最终 SHA256 为准。
- 朋友圈大文件中的 PowerShell 视觉执行器维持现有业务边界，本次不为追求形式拆分而在发布前做高风险重构；把读取、定位、执行、回读进一步拆成独立模块列为 V1.1 技术债。

## 知识收尾状态

| 事实面 | 状态 | 说明 |
|---|---|---|
| 代码 | changed-and-verified | 产品详情图与内容素材底座保持既有结论；朋友圈互动锚点/同屏候选、主动触达静默期修复、旧智能混剪 V1 及本轮 AI 长课/批量混剪均有对应自动回归；渠道服务仍未实现 |
| 运行态 | partially-verified | 产品详情图已完成用户验收；主动触达修复、朋友圈点赞/AI 评论和单条图文发布均有真实微信证据。创作工作台已完成原有 8 秒 FFmpeg 冒烟、1 次真实百炼动效编导、15.744 秒 Remotion 样片、正式混合渲染接入和固定开发运行时 composition smoke；真实 3×3 因旧库存无动效计划而停止，5 条课程片、30 条混剪、手机观感及商业安装包仍待验收 |
| 文档 | changed-and-verified | 当前说明已区分真实微信验收、源码回归和待验收的智能混剪运行时，不把测试结果冒充用户验收 |
| 规则 | verified-current | `AGENTS.md` 继续作为仓库规则来源，本轮未放宽真实发送、发布、密钥或清理边界 |
| 记忆 | not-applicable | 未写入或修改平台生成记忆 |
| 工作区 | changed | 本轮创作工作台位于 `codex/creative-workbench-ai-editing` 开发分支；未删除原素材、旧运行时或历史发布物，尚未从 dirty worktree 生成对外发布包 |

## 2026-08-06 朋友圈“发现”入口整栏粘连修复

- 真实失败 run `76396bad-10cd-4bf7-a8e8-3e679f5edf21` 在任何帖子扫描或点赞动作之前停止；两次入口观测都只得到一个约 `55 x 325` 的超高候选，证明新版微信把相邻侧栏图标的行投影连接成了整栏，并非用户未打开微信。
- 入口适配器保留原有通用形状识别；仅在没有独立形状命中时，按绿色像素的纵向投影恢复唯一的选中“发现”图标，不使用固定纵坐标，也不放宽重复候选约束。
- 新增整栏粘连回归，覆盖 `1.25x` DPI 下“普通行分组失败、绿色恢复成功”的场景；朋友圈视觉、导航、连续互动专项自检和完整 `npm.cmd run check:self` 均通过。
- 测试版 renderer 已重建，构建号 `20260806T0638Z`；真实微信入口与后续连续点赞仍待用户复测，不能以静态自检替代 live 结论。
- 构建 `20260806T0638Z` 的真实 run `1140cff5-fd8d-4b7b-a6a1-f8b44bd42267` 已证明整栏分割恢复生效，但新版选中图标为高绿色占比的近实心圆，旧版“指南针斜线”拓扑继续误拒绝；同时“朋友圈”入口 OCR 仍只接受完全等于三个字的标签。现允许尺寸、圆角和绿色占比均成立的新版选中图标，并接受唯一、长度不超过 8 的带装饰短标签（例如 `朋友圈 >`），仍拒绝长行模糊命中和重复候选。完整 `check:self` 与 `build:test` 已通过，新构建号为 `20260806T0700Z`，待 live 复测。
- 构建 `20260806T0700Z` 的聊天页 run `b9296c10-7116-4bfe-9c65-66f977695e9b` 暴露上述绿色兼容被放在通用匹配器中，导致 `y≈142` 的选中聊天图标与 `y≈323` 的灰色发现指南针同时命中。现已收窄为：通用匹配器继续只认指南针拓扑；只有整栏粘连、通用匹配结果为零时才启用选中绿色恢复。现场指标回归证明聊天图标不匹配、发现指南针匹配、粘连场景仍可恢复。完整 `check:self` 与 `build:test` 通过，新构建号 `20260806T0711Z`，待 live 复测。

## 2026-08-06 朋友圈连续点赞菜单识别与共享最大化

- 真实失败 run `0d9e5d19-6704-4fc7-a8d2-2f8bbad4ac0a` 已成功进入朋友圈、检查 8 个位置并下滑 9 次，但目标仍为 `0/2`；日志中有 6 次 `moments_menu_surface_ambiguous` 和 2 次 `moments_observation_snapshot_invalid`，全部发生在真实点赞动作之前，`real_action_attempted=false`。微信帖子右下角的灰色 `••` 是点赞/评论菜单入口，不是错误提示。
- 菜单几何门槛原先按整块朋友圈视口宽度计算；窗口变宽后，最小允许宽度会大于最大允许宽度，导致已打开的唯一菜单被必然拒绝。现改为按微信弹层自身的 DIP 尺寸与当前 DPI 判断，保留唯一菜单、目标单元格 OCR/视觉签名、精确 PID/HWND、点击归属和动作后稳定结果证明；多候选、仅有 `••` 锚点或语义不成立时仍不点击。
- `menu_only` 观测的嵌套边界现统一按 `left/top/width/height` 规范化后生成指纹，消除 PowerShell Hashtable 属性顺序与 JavaScript 对象顺序不同造成的假快照漂移；评论仍不允许在缺少帖子正文时使用该降级路径。
- 共享微信窗口契约由固定 `880×560` 左上角布局改为 `maximized_work_area`：使用 Windows `IsZoomed` 与当前 HWND 所在显示器的工作区共同验证最大化，并返回真实坐标、尺寸和 DPI。主动触达、自动回复以及朋友圈打开、互动和发布均复用同一契约，同时继续要求精确 PID/HWND、窗口身份、前台状态与用户空闲检查。
- 朋友圈视觉、动作、导航及共享微信适配专项自检和完整 `npm.cmd run check:self` 均通过；测试版 renderer 已重建，构建号 `20260806T0802Z`。最大化及连续点赞仍需用户在该 exact build 上完成真实微信复测，静态自检不能替代 live 结论。
- 运行悬浮窗暂未叠加到最大化微信上：朋友圈当前使用屏幕截图和窗口点归属证明，覆盖式置顶窗会污染 OCR 或触发窗口被遮挡。后续若实现，应使用不抢焦点、位于微信边界之外或截图时自动隐藏的共享进度窗。

## 2026-08-06 同版本双账号朋友圈双形态适配

- 实机现象确认：同一台电脑、同一微信版本，不同账号可能分别出现主窗口内嵌朋友圈与独立朋友圈弹窗；因此不再用版本号、账号缓存或单一布局假设决定流程，而是在每次动作前按当前 PID/HWND、标题、窗口类、前台归属、几何和 DPI 现场判型。
- 上一节“一律最大化”的共享布局结论由本节取代：内嵌形态使用左上角稳定布局；独立弹窗保留微信原生位置和尺寸，只精确切到前台，不把弹窗强制并入主窗口布局，也不在微信画面上覆盖悬浮窗。
- 预演后执行统一改为精确窗口交接：内嵌与弹窗都只复核并聚焦预演锁定的同一窗口，不会再次点击“发现”或“朋友圈”，也不会为了切换形态而重排窗口。
- 修复弹窗出现后仍先抓主窗、OCR 期间输入租约被重置、弹窗只按 HWND 判稳、内嵌过早成功遗漏慢弹窗等问题；唯一且稳定的独立弹窗优先，只有没有独立弹窗时才接受内嵌页面，重复或身份漂移仍安全停止。
- `npm.cmd run check:self`、`npm.cmd run build:test`、`npm.cmd run build:delivery` 均通过；双账号真实微信验收仍由用户完成，Codex 未执行真实点赞、评论或发布。
