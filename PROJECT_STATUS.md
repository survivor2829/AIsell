# 项目状态

> 最后更新：2026-07-10

## 当前主线

客户版主动触达 V1 已从“调试验收页”收敛为一键任务流：

```text
打开软件
-> 自动进入工作台
-> 同步微信联系人
-> 主动触达填写话术
-> 启动程序
-> 悬浮窗显示进度
-> 微信逐个打开会话并写入草稿
```

## 已完成

- [x] Electron + React + Vite 桌面框架。
- [x] 个微Agent 导航：自动回复、同步联系人、主动触达、朋友圈点赞评论。
- [x] 本地登录页不再阻断客户主流程，默认进入工作台。
- [x] 微信联系人同步入口，联系人写入 `contacts.json`。
- [x] 主动触达客户版极简页面：话术输入、触达人数、状态卡、联系人预览。
- [x] `{称呼}` 模板变量，优先备注，其次昵称/名称。
- [x] 右下角 `启动程序` 创建任务并隐藏主窗口。
- [x] 右侧悬浮窗展示当前联系人、下一位、进度、暂停原因和操作按钮。
- [x] 任务进度持久化，暂停后可继续。
- [x] 微信窗口驱动兼容 `Weixin`、`WeChat`、`WeChatAppEx`。
- [x] V1 只写草稿并做预检，不自动真实发送。
- [x] 用户可在账号管理填写自己的 DeepSeek API Key；Key 只由主进程使用，并以 Windows DPAPI 加密保存。
- [x] 自检通过：`contact_sync` 和 `active_touch`。

## 当前边界

- 不做批量真实发送。
- 不做腾讯云 SCF、PostgreSQL、服务端网关、许可证和服务端额度系统；这些方案延期。
- 不做多账号并发。
- 不做自动回复和朋友圈真实操作。
- 不把微信/RPA 操作放进 renderer，仍通过主进程 IPC 调本地执行器。

## 下一步

1. 继续实测主动触达任务：重点观察微信窗口恢复、联系人匹配、草稿输入失败样本。
2. 收紧联系人同步过滤：只进入个人联系人，排除群聊、公众号、服务号和特殊账号。
3. 把当前 portable 包打包流程固化，避免以后手动同步 `release/resources/app`。
