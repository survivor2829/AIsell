# 项目状态

> 最后更新：2026-07-11

## 当前主线

客户版主动触达 V1 已从“调试验收页”收敛为一键任务流：

```text
打开软件
-> 自动进入工作台
-> 同步微信联系人
-> 主动触达填写话术
-> 启动程序
-> 悬浮窗显示进度
-> 微信逐个打开会话并写入草稿
```

## 已完成

- [x] Electron + React + Vite 桌面框架。
- [x] 个微Agent 导航：自动回复、同步联系人、主动触达、朋友圈点赞评论。
- [x] 本地登录页不再阻断客户主流程，默认进入工作台。
- [x] 微信联系人同步入口，联系人写入 `contacts.json`。
- [x] 主动触达客户版极简页面：话术输入、触达人数、状态卡、联系人预览。
- [x] `{称呼}` 模板变量，优先备注，其次昵称/名称。
- [x] 右下角 `启动程序` 创建任务并隐藏主窗口。
- [x] 右侧悬浮窗展示当前联系人、下一位、进度、暂停原因和操作按钮。
- [x] 任务进度持久化，暂停后可继续。
- [x] 微信窗口驱动只绑定个人微信主进程 `Weixin`、`WeChat`。
- [x] V1 只写草稿并做预检，不自动真实发送。
- [x] 内部开发版单联系人真实发送已完成账号、窗口、会话、幂等和结果气泡校验，并由用户使用测试微信人工验收通过。
- [x] 客户版与内部开发版继续物理隔离；客户 preload、IPC、renderer 和 release 不包含开发真发入口。
- [x] 用户可在账号管理填写自己的 DeepSeek API Key；Key 只由主进程使用，并以 Windows DPAPI 加密保存。
- [x] 自检通过：`contact_sync` 和 `active_touch`。

## 当前边界

- 客户版不做真实发送；内部开发版仅允许用户明确选择一个联系人进行验收。
- 不做批量真实发送，且内部单人链路不会自动升级为批量任务。
- 不做腾讯云 SCF、PostgreSQL、服务端网关、许可证和服务端额度系统；这些方案延期。
- 不做多账号并发。
- 不做自动回复和朋友圈真实操作。
- 不把微信/RPA 操作放进 renderer，仍通过主进程 IPC 调本地执行器。

## 下一步

1. 从已验收的单联系人链路提取唯一发送执行器，在独立受控试用版中接入最多 50 人一批的任务授权。
2. 修正联系人预览只显示前 8 人的问题，并冻结账号、联系人身份、顺序和完整文案。
3. 将联系人同步 helper 和 portable ZIP 构建固化，完成无 Python、Codex、Node 和 `dt-ai-helper` 的干净 Windows 验收。
