# DeepSeek 与统一 AI 供应商网关

测试版通过已认证的服务端供应商网关调用 DeepSeek、火山方舟/语音和 APIMart；本项目不再迁移或依赖百炼网关能力。客户端不再读取本机历史 Key 作为回退。本文说明云端能力预检和失败边界；功能是否通过实机验收仍以 `PROJECT_STATUS.md` 为准。

## 测试版统一网关

- 公网入口沿用测试维护服务的 HTTPS 443；`/v1/provider-gateway/` 只转发到服务器回环的独立网关，不开放新的公网端口。
- 客户端先提交当前软件的签名授权码和安装元数据，成功后得到短期会话令牌及能力清单；没有有效会话不能访问供应商路由。
- 网关只允许固定的供应商路径，不接受客户端传入的上游 URL；供应商 Key 只存在服务器受保护的环境文件中，不进入客户端包、日志或诊断。
- 当前服务器组件已部署；Provider 能力是否可用以鉴权后的 `/capabilities` 和受控真实调用为准。通用 `VOLCENGINE_API_KEY` 只兼容 Ark，不证明 TTS/ASR 可用；TTS 使用专用凭据，ASR 使用专用 API Key 或旧版 APP ID + Access Token。两者不能互借密钥，ASR 还须开通对应资源。
- 健康检查按协议和能力判断，版本标识或超时元数据缺失不会关闭全部 AI。上游请求默认180秒（配置上限180秒）、维护转发240秒、客户端网关270秒；同步结果未知保留请求记录，不因超时直接重复付费调用。
- 付费工作前核对会话能力；服务重启导致会话失效时通过不计费的会话接口恢复，并将新凭据／能力同步至空闲内容引擎。合并只覆盖同一认证主体、目标、正文及影响结果参数一致的在途请求，不能替代跨重启结果核对。
- 交付版仍关闭此测试网关配置，正式域名、正式证书、正式授权和正式 Provider 额度需另行验收后再启用。

## 密钥边界

- 客户端不显示供应商密钥输入入口；只显示“云端智能服务可用／暂不可用”。
- 历史 `safeStorage`（Windows DPAPI）密文暂时保留，但生产调用、自动回复和内容引擎均不再读取它们作为回退。
- 供应商凭据由受控运维通过 SSH 标准输入写入服务器，先备份再原子替换，文件保持 `root:ai-gateway`、`0640`。
- 客户换电脑或换 Windows 用户不需要重新配置供应商密钥。
- Key、密钥文件、完整请求日志和含密钥的 `.env` 都不得进入 Git、源码包、便携目录或 ZIP。

当前内部开发版的 APIMart 测试出站使用本机既有代理，经 SSH 回环转发到服务器网关。APIMart Key 仍只由网关读取；开发版启动入口负责建立和关闭转发，断线时会尝试重连。此线路依赖开发电脑在线，仅用于现有测试环境，不代表客户安装包或无人值守云服务已获得长期出站能力。

测试版历史密文位置（仅保留，不再作为调用来源）：

```text
%APPDATA%\xiaoxi-active-touch-test\data\deepseek-api-key.bin
```

交付版历史密文位置（如存在，仅保留，不再作为调用来源）：

```text
%APPDATA%\xiaoxi-active-touch-delivery\data\deepseek-api-key.bin
```

## 生产能力预检

网关验收不是简单 ping。必须先通过授权会话读取能力列表，再执行受控真实调用：

1. DeepSeek：完成一条最小文本生成并校验结构。
2. 火山方舟：完成一条最小模型调用并校验结构。
3. 火山 ASR：提交一段受控音频并确认服务返回。
4. 火山 TTS：合成一条短句并确认完整结束事件。
5. APIMart：只提交一项最小生图任务；结果不明时不得补发。

DeepSeek、火山方舟、火山 ASR、火山 TTS 与 APIMart 五项能力都成功后，测试版才可视为云端切换验收通过。HTTP 可达、环境变量非空或模型返回任意文本，都不能单独视为能力可用。结果不明时停止且不自动补发。该预检也不等于微信窗口和真实发送已经验收。

## 固定话术 fallback

主动触达任务中的话术由用户在启动前确认并随任务冻结。DeepSeek 生成失败时：

- 仅当前联系人进入 fallback，不因一次 AI 失败暂停整批任务。
- 使用冻结的固定话术，替换可确认的 `{称呼}`；称呼不可靠时使用通用开场，不编造姓名。
- fallback 文案仍执行与 AI 文案相同的清洗、长度和发送前会话核验，并以 `ai_status: fallback` 记录原因。
- 固定话术为空或校验不通过时跳过当前联系人，不生成临时营销内容，也不冒险发送。

自动回复的 AI 临时错误使用保守确认话术并触发人工跟进；网关不可用、能力缺失、鉴权失效或余额不足时，在处理当前来信后明确暂停等待恢复。自动回复的兜底记录不能被主动触达任务当作其发送结果。

## 当前服务边界

当前只部署了测试频道的独立网关和授权会话校验，不包含客户账户系统、远程停用、服务端额度结算或正式版运营能力。Provider 凭据仍需通过受控服务器运维写入，不能把本机加密文件直接复制到服务器。网关部署、客户端候选构建、安装包验收和真实 Provider/微信验收是四个独立状态，不能互相替代。
