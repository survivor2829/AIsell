# DeepSeek 与统一 AI 供应商网关

测试版优先通过已认证的服务端供应商网关调用 DeepSeek、火山方舟/语音和 APIMart；本项目不再迁移或依赖百炼网关能力。网关不可用或对应能力未配置时，保留原有本机加密 Key 回退。本文说明密钥、能力预检和失败回退；功能是否通过实机验收仍以 `PROJECT_STATUS.md` 为准。

## 测试版统一网关

- 公网入口沿用测试维护服务的 HTTPS 443；`/v1/provider-gateway/` 只转发到服务器回环的独立网关，不开放新的公网端口。
- 客户端先提交当前软件的签名授权码和安装元数据，成功后得到短期会话令牌及能力清单；没有有效会话不能访问供应商路由。
- 网关只允许固定的供应商路径，不接受客户端传入的上游 URL；供应商 Key 只存在服务器受保护的环境文件中，不进入客户端包、日志或诊断。
- 当前服务器组件已部署并通过健康检查；Provider 能力是否可用以 `/capabilities` 返回的布尔值为准。通用 `VOLCENGINE_API_KEY` 只兼容 Ark，不证明 TTS/ASR 可用；TTS 使用专用凭据，ASR 使用专用 API Key 或旧版 APP ID + Access Token。两者不能互借密钥。ASR 还须开通对应资源。2026-09-12 现场服务端 ASR 使用已有 APP ID + Access Token，未配置专用 TTS Key；真实成片配音使用本机已有加密语音 Key，未上传该 Key。
- 健康检查按协议和能力判断，版本标识或超时元数据缺失不会关闭全部 AI。上游请求默认180秒（配置上限180秒）、维护转发240秒、客户端网关270秒；同步结果未知保留请求记录，不因超时直接重复付费调用。
- 付费工作前核对会话能力；服务重启导致会话失效时通过不计费的会话接口恢复，并将新凭据／能力同步至空闲内容引擎。合并只覆盖同一认证主体、目标、正文及影响结果参数一致的在途请求，不能替代跨重启结果核对。
- 交付版仍关闭此测试网关配置，正式域名、正式证书、正式授权和正式 Provider 额度需另行验收后再启用。

## 密钥保存

- 用户只在应用侧栏的“API密钥”页面填写自己的 Key。
- 主进程使用 Electron `safeStorage`（Windows DPAPI）加密后写入运行根目录的 `deepseek-api-key.bin`。
- renderer、preload、开发者工具和普通状态接口只能看到是否已配置及掩码，不能读取明文。
- 加密内容绑定当前 Windows 用户；换电脑或换用户后必须重新填写，不能复制旧文件代替配置。
- Key、密钥文件、完整请求日志和含密钥的 `.env` 都不得进入 Git、源码包、便携目录或 ZIP。

测试版密钥位置：

```text
%APPDATA%\xiaoxi-active-touch-test\data\deepseek-api-key.bin
```

交付版密钥位置：

```text
%APPDATA%\xiaoxi-active-touch-delivery\data\deepseek-api-key.bin
```

## 生产能力预检

“测试连接”不是简单 ping。一次成功必须使用待保存或已保存的 Key 完成两条不发送微信的真实生成链路：

1. 主动触达：生成普通客户文案，并通过非空、清洗和长度校验。
2. 自动回复：生成严格 JSON 回复，校验 `reply`、`intent`、`intentReason`、`needsHuman`、`handoffReason` 的结构和回复长度。

只有两条链路都成功，界面才可显示连接正常。HTTP 可达、Key 已保存或模型返回任意文本，都不能单独视为能力可用。该预检也不等于微信窗口和真实发送已经验收。

## 固定话术 fallback

主动触达任务中的话术由用户在启动前确认并随任务冻结。DeepSeek 生成失败时：

- 仅当前联系人进入 fallback，不因一次 AI 失败暂停整批任务。
- 使用冻结的固定话术，替换可确认的 `{称呼}`；称呼不可靠时使用通用开场，不编造姓名。
- fallback 文案仍执行与 AI 文案相同的清洗、长度和发送前会话核验，并以 `ai_status: fallback` 记录原因。
- 固定话术为空或校验不通过时跳过当前联系人，不生成临时营销内容，也不冒险发送。

自动回复的 AI 临时错误使用保守确认话术并触发人工跟进；Key 缺失、无法解密、失效、余额不足或持久配置错误，在处理当前来信后明确暂停等待修复。自动回复的兜底记录不能被主动触达任务当作其发送结果。

## 当前服务边界

当前只部署了测试频道的独立网关和授权会话校验，不包含客户账户系统、远程停用、服务端额度结算或正式版运营能力。Provider 凭据仍需通过受控服务器运维写入，不能把本机加密文件直接复制到服务器。网关部署、客户端候选构建、安装包验收和真实 Provider/微信验收是四个独立状态，不能互相替代。
